---
phase: quick-260601-e6z
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - crates/chia-sdk-types/src/silent_payments/scalar.rs
  - crates/chia-sdk-types/src/silent_payments/tagged_hash.rs
  - crates/chia-sdk-types/src/silent_payments/paths.rs
  - crates/chia-sdk-types/src/silent_payments/mod.rs
  - crates/chia-sdk-utils/src/silent_payments/address.rs
  - crates/chia-sdk-utils/src/silent_payments/keys.rs
  - crates/chia-sdk-utils/src/silent_payments/labels.rs
  - crates/chia-sdk-utils/src/silent_payments/mod.rs
  - crates/chia-sdk-utils/src/silent_payments/error.rs
  - crates/chia-sdk-driver/src/silent_payments/mod.rs
  - crates/chia-sdk-driver/src/silent_payments/protocol.rs
  - crates/chia-sdk-driver/src/silent_payments/scanner.rs
  - crates/chia-sdk-driver/src/silent_payments/send_keys.rs
  - crates/chia-sdk-driver/src/silent_payments/types.rs
  - crates/chia-sdk-driver/src/silent_payments/block_tweak_data.rs
  - crates/chia-sdk-driver/src/action_system/spends.rs
  - crates/chia-sdk-driver/src/action_system/send_destination.rs
  - crates/chia-sdk-driver/src/action_system/relation.rs
  - crates/chia-sdk-driver/src/actions/silent_payment_send.rs
  - crates/chia-sdk-driver/src/driver_error.rs
  - crates/chia-sdk-bindings/src/silent_payments.rs
  - crates/chia-sdk-driver/tests/silent_payments_e2e.rs
  - .github/workflows/rust.yml
autonomous: true
requirements: [CLEANUP-07]

must_haves:
  truths:
    - "The ITEM-1 planning-residue grep returns 0 hits across all shipping AND #[cfg(test)] SP source (excluding .planning/)"
    - "SP DriverError #[error(...)] Display strings are terse one-line lowercase with no embedded API names; remediation prose lives in /// rustdoc"
    - "The scaffolding comment '(additional submodules appended in sorted order)' is gone from chia-sdk-types/src/silent_payments/mod.rs"
    - "Zero references to the renamed method finish_with_silent_payment_keys remain in crates/chia-sdk-driver/src"
    - "The scalar.rs reduction narrative correctly states quotient=2 and result=(2^256-1)-2r; the pinned [0xff;32] expected bytes are unchanged"
    - "The signed-vs-unsigned mod-r warning has exactly ONE canonical statement (scalar.rs); other sites are terse one-liners deferring to it"
    - "SilentPaymentPending lives in silent_payments/types.rs (not send_keys.rs); send_keys re-export in mod.rs no longer needs both pub(crate) use * and pub use {newtypes}"
    - "scripts/sp_descriptor_facade_drift.sh runs as a CI step in .github/workflows/rust.yml and fails the build on drift"
    - "A multi-input (2 distinct-puzzle-hash XCH) SP e2e test drives the real receiver grouping path and asserts exactly one DetectedSpCoin at the expected one-time puzzle hash, then spends it"
    - "The v1 multi-input constraint (distinct-puzzle-hash non-ephemeral XCH inputs, no CAT/DID/NFT/intermediate) is documented in SP rustdoc"
    - "Full CI workspace test suite, clippy --all-targets, fmt --check, and cargo machete are all clean with AND without --all-features"
  artifacts:
    - path: "crates/chia-sdk-types/src/silent_payments/scalar.rs"
      provides: "Canonical signed-vs-unsigned warning + corrected reduction narrative (quotient=2)"
      contains: "0xff"
    - path: "crates/chia-sdk-driver/src/silent_payments/types.rs"
      provides: "Relocated pub(crate) struct SilentPaymentPending alongside other crate-internal wire/state types"
      contains: "SilentPaymentPending"
    - path: "crates/chia-sdk-driver/src/driver_error.rs"
      provides: "Terse one-line lowercase SP error Display strings + remediation in rustdoc"
      contains: "SilentPaymentKeyNotSynthetic"
    - path: "crates/chia-sdk-driver/tests/silent_payments_e2e.rs"
      provides: "Multi-input (2-XCH-input) SP e2e test exercising the real receiver grouping path"
      contains: "fn test_simulator_e2e_multi_input"
    - path: ".github/workflows/rust.yml"
      provides: "CI step running scripts/sp_descriptor_facade_drift.sh"
      contains: "sp_descriptor_facade_drift.sh"
  key_links:
    - from: "crates/chia-sdk-driver/src/silent_payments/mod.rs"
      to: "send_keys::{SyntheticPublicKey, SyntheticSecretKey} + types::SilentPaymentPending"
      via: "re-export split after relocation"
      pattern: "pub use send_keys::\\{Synthetic"
    - from: "crates/chia-sdk-driver/tests/silent_payments_e2e.rs"
      to: "tweak_data_from_simulator_block + scan_from_tweaks"
      via: "multi-input farm -> extract -> scan"
      pattern: "tweak_data_from_simulator_block"
    - from: ".github/workflows/rust.yml"
      to: "scripts/sp_descriptor_facade_drift.sh"
      via: "CI run step"
      pattern: "sp_descriptor_facade_drift\\.sh"
---

<objective>
Close the editorial / hygiene tech-debt found in the v1.0 milestone code review of the CHIP-0057 silent-payments surface. Seven items, NO architecture or behavior changes:

1. Strip planning-process vocabulary from doc-comments and inline comments (shipping AND test source).
2. Trim multi-line SP `DriverError` Display strings to the repo's terse one-line lowercase convention.
3. Remove a scaffolding comment + fix 3 stale `finish_with_silent_payment_keys` references.
4. Reduce doc bloat, dedupe the signed-vs-unsigned mod-r warning to one canonical site, and FIX the wrong scalar.rs reduction narrative (quotient is 2, not 1).
5. Relocate the crate-internal `SilentPaymentPending` struct out of `send_keys.rs` and simplify the mod.rs re-exports.
6. Wire `scripts/sp_descriptor_facade_drift.sh` into CI.
7. Add a multi-input SP e2e test driving the real receiver grouping path + a constraint doc note + a deferred follow-up design note.

Purpose: The milestone audit (`v1.0-MILESTONE-AUDIT.md`, status PASSED) flagged these as cosmetic/hygiene debt to reconcile before upstream merge. They do not affect requirement coverage but the SP surface should read as standalone API documentation matching the repo norm (compare `primitives/cat.rs` ~16% comment density) and the descriptor-drift guard + multi-input flow should actually be enforced by tests/CI.

Output: A cleaner, drift-guarded, better-tested SP surface. Zero new deps, zero new `#[allow]`, no behavior change. The only pre-existing `#[allow]` (function-scoped `clippy::similar_names` on `scan_from_tweaks`) may stay.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/v1.0-MILESTONE-AUDIT.md

<critical_constraints>
From CLAUDE.md and the milestone audit — these are HARD gates:

- Rust 1.90.0, edition 2024. No nightly-only features. No `unsafe`.
- NO new workspace dependencies. The drift script needs only `jq` + `bash` (both preinstalled on the `ubuntu-latest` GitHub runner) and `cargo machete` must stay clean.
- NO new `#[allow]` attributes. The ONE pre-existing function-scoped `#[allow(clippy::similar_names)]` on `scan_from_tweaks` (scanner.rs, from Plan 03-03) may stay — do not add others.
- All chip-0057-gated code MUST compile with AND without `--all-features` (CI builds each crate both ways).
- Workspace clippy is `deny clippy::all` + `warn pedantic` + `warn cargo`; locally `-D warnings` promotes pedantic to errors. Prefer LSP diagnostics for tight feedback; run clippy before reporting done.
- KEEP genuine spec citations: CHIP §NNN, BIP-352, BIP-340. Those are idiomatic and good — only strip GSD planning-process vocabulary.
- This is EDITORIAL + one test + one CI line. Do NOT change any AssertConcurrent cycle-binding logic, any scalar math, any pinned byte values, any public/crate signature semantics (Item 5 is a pure move).
</critical_constraints>

<residue_grep>
The ITEM-1 final gate. The SP source set is every `*silent_payments*` .rs file under `crates/`
PLUS `action_system/spends.rs`, `action_system/send_destination.rs`, `actions/silent_payment_send.rs`,
and `chia-sdk-bindings/src/silent_payments.rs`. The pattern (87 hits at plan time, must become 0):

```
grep -rnE '(Phase [0-9]|\b(RECV|SEND|ADDR|CRYPTO|GUARD|BIND|BRIDGE|POLISH|CLEANUP|SIM)-[0-9]|Pitfall [0-9]|Plan [0-9]{2}-|\bD-[0-9]|SC[0-9]\b)' <SP files>
```

Hits per file at plan time: protocol.rs 17, send_keys.rs 15, scanner.rs 12, actions/silent_payment_send.rs 11,
action_system/spends.rs 10, utils/labels.rs 6, utils/keys.rs 6, utils/mod.rs 5, bindings/silent_payments.rs 2,
driver/mod.rs 1. (address.rs, types.rs, send_destination.rs, scalar.rs, tagged_hash.rs, paths.rs = 0 already but
re-grep after edits since the dedup in Task 1 touches scalar.rs/paths.rs/mod.rs.)

Tokens to strip: "Phase N", requirement IDs (RECV-/SEND-/ADDR-/CRYPTO-/GUARD-/BIND-/BRIDGE-/POLISH-/CLEANUP-/SIM- + digits),
"Pitfall N", "Plan NN-MM", "D-NN", "SC N", and references to "ROADMAP" / "REQUIREMENTS.md". Includes test-module
inline section-header comments like `// ─── ADDR-01: ... ───` inside `#[cfg(test)]`.
</residue_grep>

<scalar_math_verified>
The scalar.rs narrative at ~lines 122-127 is WRONG. Verified math for input `[0xff; 32]` (= 2^256 - 1)
with BLS12-381 r = 0x73eda753...00000001:
- floor((2^256-1) / r) = **2** (the comment says 1 — WRONG)
- (2^256-1) mod r = 0x1824b159acc5056f998c4fefecbc4ff55884b7fa0003480200000001fffffffd
- That remainder == (2^256-1) - 2*r (NOT (2^256-1) - r as the comment claims)
- The pinned `expected: [u8;32]` bytes at ~lines 129-133 ARE the correct remainder — leave them UNCHANGED.
Only the prose comment is wrong. Rewrite it to: quotient 2, result is `(2^256-1) - 2r`, and that unsigned
reduction fired (reduced value differs from raw input). Keep it as the ONE canonical signed-vs-unsigned
warning site (Item 4 dedup).
</scalar_math_verified>

<error_convention>
Repo terse convention (driver_error.rs existing examples): `#[error("invalid mod hash")]`,
`#[error("missing hint")]`, `#[error("missing memo")]`, `#[error("non-standard inner puzzle layer")]` —
short, lowercase, no trailing period, no embedded API names. The 8 SP variants currently at
driver_error.rs:134-179 with multi-sentence messages to trim:
SilentPayment (keep — already terse `"silent payment error: {0}"`), SilentPaymentMultiPartyUnsupported,
SilentPaymentNoXchInputs, SilentPaymentMemoHintForbidden, SilentPaymentRequiresInputBinding,
SilentPaymentKeysNotRegistered, SilentPaymentRequiresXch, SilentPaymentKeyNotSynthetic (this one embeds
`StandardArgs::curry_tree_hash` — MUST move to rustdoc).
</error_convention>

<relocation_note>
ITEM 5 — `SilentPaymentPending` (send_keys.rs:28, `pub(crate) struct`) is consumed via the module-root
re-export `pub(crate) use send_keys::*;` (mod.rs:42) from two sites:
  - action_system/spends.rs:33 `Vec<crate::silent_payments::SilentPaymentPending>`
  - actions/silent_payment_send.rs:6 `silent_payments::SilentPaymentPending`
After moving the struct into types.rs, the module root must still re-export it `pub(crate)` so those
paths stay valid. Goal: send_keys re-export becomes ONLY `pub use send_keys::{SyntheticPublicKey, SyntheticSecretKey};`
(drop the `pub(crate) use send_keys::*;`), and types.rs gets added to the `pub(crate)`/`pub` re-export of
SilentPaymentPending. No consumer-site edits required (paths resolve through mod root either way). Pure move.
</relocation_note>

<e2e_structure>
Existing tests in tests/silent_payments_e2e.rs all use a SINGLE `sender.coin` from `sim.bls(1_000)`.
The multi-input test needs 2 distinct-puzzle-hash XCH inputs. Available helpers:
  - `sim.bls(amount) -> BlsPairWithCoin { coin, puzzle_hash, pk, sk, ... }` (chia-sdk-test/src/key_pairs.rs:61, simulator.rs:96)
  - call `sim.bls(...)` TWICE to get two distinct key pairs / puzzle hashes / coins
  - `Spends::new(ph)` + `spends.add(coin)` for each, BUT two distinct puzzle hashes => need both senders'
    keys in the pk_map and synthetic maps (keyed by each puzzle_hash).
  - finish MUST use `Relation::AssertConcurrent` (the runtime gate at spends.rs:629 errors with
    `SilentPaymentRequiresInputBinding` on >=2 non-ephemeral XCH inputs + Relation != AssertConcurrent).
  - extract via `tweak_data_from_simulator_block(&sim, height_before)` then `scan_from_tweaks(...)`.
  - assert exactly 1 detection, then spend it via `detected.onetime_sk.derive_synthetic()` + StandardLayer,
    mirroring test_simulator_e2e_unlabeled (lines 124-144).
Sign the multi-input bundle with BOTH senders' SKs: `sim.spend_coins(ctx.take(), &[sender_a.sk.clone(), sender_b.sk.clone()])`.
</e2e_structure>

<ci_step>
ITEM 6 — the `test` job in .github/workflows/rust.yml (runs-on: ubuntu-latest) is the right home. Add a step
after "Unused dependencies" (line 83) and before the publish steps (line 85). Style mirrors existing steps:
  - name: SP descriptor/facade drift
    run: bash scripts/sp_descriptor_facade_drift.sh
ubuntu-latest has jq + bash 4+ preinstalled (the script declares those deps). Script `set -euo pipefail`
already non-zero-exits on drift, failing the gate. Keep it minimal — no new install step needed.
</ci_step>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Strip planning residue, dedupe + fix scalar narrative, remove scaffolding, fix stale method refs</name>
  <files>
    crates/chia-sdk-types/src/silent_payments/scalar.rs,
    crates/chia-sdk-types/src/silent_payments/tagged_hash.rs,
    crates/chia-sdk-types/src/silent_payments/paths.rs,
    crates/chia-sdk-types/src/silent_payments/mod.rs,
    crates/chia-sdk-utils/src/silent_payments/address.rs,
    crates/chia-sdk-utils/src/silent_payments/keys.rs,
    crates/chia-sdk-utils/src/silent_payments/labels.rs,
    crates/chia-sdk-utils/src/silent_payments/mod.rs,
    crates/chia-sdk-utils/src/silent_payments/error.rs,
    crates/chia-sdk-driver/src/silent_payments/mod.rs,
    crates/chia-sdk-driver/src/silent_payments/protocol.rs,
    crates/chia-sdk-driver/src/silent_payments/scanner.rs,
    crates/chia-sdk-driver/src/silent_payments/send_keys.rs,
    crates/chia-sdk-driver/src/silent_payments/types.rs,
    crates/chia-sdk-driver/src/silent_payments/block_tweak_data.rs,
    crates/chia-sdk-driver/src/action_system/spends.rs,
    crates/chia-sdk-driver/src/action_system/send_destination.rs,
    crates/chia-sdk-driver/src/action_system/relation.rs,
    crates/chia-sdk-driver/src/actions/silent_payment_send.rs,
    crates/chia-sdk-bindings/src/silent_payments.rs
  </files>
  <action>
This is items 1, 3, 4 + the stale-method-ref fix. Editorial ONLY — no code/behavior/signature changes,
no pinned-byte changes. Rewrite each comment so it reads as standalone API documentation (purpose + the one
gotcha), matching the repo norm (~16% comment density like primitives/cat.rs).

ITEM 1 — Strip planning-process vocabulary across every SP file (shipping AND #[cfg(test)] inline comments):
- Remove "Phase N", requirement IDs (RECV-/SEND-/ADDR-/CRYPTO-/GUARD-/BIND-/BRIDGE-/POLISH-/CLEANUP-/SIM- +
  digits), "Pitfall N", "Plan NN-MM", "D-NN", "SC N", and references to "ROADMAP" / "REQUIREMENTS.md".
- Strip test-module section-header comments like `// ─── ADDR-01: ... ───` inside #[cfg(test)] — the tests
  are in-scope source. Where the header described what the test group covers, replace with a plain English
  one-liner if useful, else delete.
- KEEP genuine spec citations: CHIP §NNN, BIP-352, BIP-340. Do NOT touch those.
- Do NOT change test function names, assertions, or pinned values — only their surrounding comments.

ITEM 3 — In chia-sdk-types/src/silent_payments/mod.rs remove the scaffolding comment
`// (additional submodules appended in sorted order)` (~line 17). Fix the 3 stale references to the renamed
method `finish_with_silent_payment_keys` (now `finish_with_keys` + the private `sp_finish_branch`):
  - action_system/spends.rs:595 (rustdoc mentioning the old method name)
  - action_system/relation.rs:8 and relation.rs:28 (two rustdoc references)
Update each comment to the current method names (`Spends::finish_with_keys` for the public entry; mention the
private `sp_finish_branch` helper only where the prose was describing the internal pipeline). `grep -rn
finish_with_silent_payment_keys crates/chia-sdk-driver/src` must return 0 after this.

ITEM 4a — Reduce doc bloat in protocol.rs / scanner.rs / send_keys.rs (currently ~37-62% comment density).
Trim restated/obvious prose; keep the one real gotcha per item. Target the repo ~16% norm but do not delete
genuinely load-bearing CHIP-spec rationale.

ITEM 4b — Dedupe the signed-vs-unsigned mod-r reduction warning. It is currently restated ~4-5x verbatim
across scalar.rs, paths.rs, driver/silent_payments/mod.rs (lines ~20-26), protocol.rs, and the
`from_bytes_unsigned` doc. Keep ONE canonical statement in chia-sdk-types/src/silent_payments/scalar.rs.
Make every other site a terse one-liner that defers to it (e.g. "see `ScalarField::from_bytes_unsigned`
for why unsigned reduction is mandatory here").

ITEM 4c — FIX the incorrect scalar.rs reduction narrative at ~lines 122-127 (test
`from_bytes_unsigned_max_input_reduces_unsigned_not_identity`). The comment currently says
`floor((2^256-1)/r) = 1` and result is `(2^256-1) - r`. Both WRONG. Correct math (verified): quotient is 2,
and the reduced value equals `(2^256-1) - 2r`, which is exactly the pinned `expected` bytes. Rewrite the
comment to state quotient=2 and result `(2^256-1) - 2r`. LEAVE THE PINNED `expected: [u8;32]` BYTES AT
~lines 129-133 UNCHANGED — they are already correct.

After editing, do NOT touch napi/index.d.ts, napi/index.js, or any generated file. Run LSP diagnostics and
clippy on each touched crate before moving on.
  </action>
  <verify>
    <automated>
cd /home/kdc/chia-wallet-sdk
SP_FILES=$(find crates -path '*silent_payments*' -name '*.rs') ; SP_FILES="$SP_FILES crates/chia-sdk-driver/src/action_system/spends.rs crates/chia-sdk-driver/src/action_system/send_destination.rs crates/chia-sdk-driver/src/action_system/relation.rs crates/chia-sdk-driver/src/actions/silent_payment_send.rs crates/chia-sdk-bindings/src/silent_payments.rs"
echo "residue hits (must be 0):" ; grep -rnE '(Phase [0-9]|\b(RECV|SEND|ADDR|CRYPTO|GUARD|BIND|BRIDGE|POLISH|CLEANUP|SIM)-[0-9]|Pitfall [0-9]|Plan [0-9]{2}-|\bD-[0-9]|SC[0-9]\b)' $SP_FILES | wc -l
echo "stale method refs (must be 0):" ; grep -rn finish_with_silent_payment_keys crates/chia-sdk-driver/src | wc -l
echo "scaffolding comment (must be 0):" ; grep -rn "additional submodules appended" crates/chia-sdk-types/src/silent_payments/mod.rs | wc -l
cargo clippy -p chia-sdk-types -p chia-sdk-utils -p chia-sdk-driver --all-features --all-targets -- -D warnings
    </automated>
  </verify>
  <done>
    All three greps return 0. scalar.rs narrative states quotient=2 / result (2^256-1)-2r with the pinned
    bytes unchanged. The signed-vs-unsigned warning has exactly one canonical statement (scalar.rs) with
    terse deferrals elsewhere. Scoped clippy on the three crates is clean under --all-features. CHIP/BIP
    spec citations preserved.
  </done>
</task>

<task type="auto">
  <name>Task 2: Trim SP DriverError messages + relocate SilentPaymentPending</name>
  <files>
    crates/chia-sdk-driver/src/driver_error.rs,
    crates/chia-sdk-driver/src/silent_payments/send_keys.rs,
    crates/chia-sdk-driver/src/silent_payments/types.rs,
    crates/chia-sdk-driver/src/silent_payments/mod.rs
  </files>
  <action>
This is items 2 and 5 — small structural, no behavior change.

ITEM 2 — In crates/chia-sdk-driver/src/driver_error.rs, trim the multi-line SP `#[error("...")]` Display
strings (variants at lines ~134-179) to the repo's terse one-line lowercase convention (match the existing
`"invalid mod hash"` / `"missing hint"` style: short, lowercase, no trailing period). Move any remediation
prose into the variant's `///` rustdoc, NOT the Display string. CRITICAL: do NOT embed API names in the
user-facing message. Specifically:
  - SilentPaymentKeyNotSynthetic: currently a 4-line message embedding `StandardArgs::curry_tree_hash` and
    `SyntheticSecretKey::from_raw`. Trim Display to something like
    `#[error("registered silent-payment key is not synthetic for its coin")]` and move the full
    curry_tree_hash / public_key / derive_synthetic / from_raw explanation into a `///` rustdoc above the
    variant.
  - SilentPaymentMultiPartyUnsupported, SilentPaymentNoXchInputs, SilentPaymentMemoHintForbidden,
    SilentPaymentRequiresInputBinding, SilentPaymentKeysNotRegistered, SilentPaymentRequiresXch: collapse
    each multi-sentence message to one terse lowercase line; relocate the "why/what to do" prose to rustdoc.
  - SilentPayment(#[from] ...) is already terse (`"silent payment error: {0}"`) — leave it.
Do NOT change variant names, ordering, the #[cfg(feature = "chip-0057")] gates, or the #[from]. These are
chip-0057-gated; verify the crate still compiles WITHOUT --all-features too (the variants simply vanish).

ITEM 5 — Relocate the crate-internal `SilentPaymentPending` struct out of
crates/chia-sdk-driver/src/silent_payments/send_keys.rs (currently at line 28, mixed with the PUBLIC
SyntheticSecretKey/SyntheticPublicKey newtypes) into crates/chia-sdk-driver/src/silent_payments/types.rs
(where the other crate-internal wire/state types live). Pure move:
  - Cut the `pub(crate) struct SilentPaymentPending { ... }` block AND its rustdoc from send_keys.rs.
  - Move the relevant `use` imports it needs (PublicKey, SecretKey is NOT needed by it — check: it uses
    PublicKey, Bytes32, Memos, NodePtr) into types.rs (merge with existing imports; avoid unused-import
    warnings in either file — send_keys.rs may no longer need chia_protocol::Bytes32 / Memos / NodePtr).
  - Update the send_keys.rs module-level `//!` doc that referenced SilentPaymentPending (line ~3) — drop or
    re-point it (also subject to Item 1 residue rules but that's Task 1's gate; here just keep it accurate).
  - In crates/chia-sdk-driver/src/silent_payments/mod.rs simplify the re-exports: REMOVE
    `pub(crate) use send_keys::*;` (line 42) so send_keys only contributes the public newtypes via
    `pub use send_keys::{SyntheticPublicKey, SyntheticSecretKey};` (line 43, keep). Add SilentPaymentPending
    to the types re-export as `pub(crate)` so the existing consumer paths still resolve:
    e.g. keep `pub use types::{DetectedSpCoin, OutputMeta, TweakData};` and ADD
    `pub(crate) use types::SilentPaymentPending;`.
  - Consumers (action_system/spends.rs:33 via `crate::silent_payments::SilentPaymentPending`,
    actions/silent_payment_send.rs:6 via `silent_payments::SilentPaymentPending`) resolve through the module
    root either way — they should need NO edits. Confirm via compile.
No behavior change, no field changes, no visibility change on the struct itself (stays `pub(crate)`).
  </action>
  <verify>
    <automated>
cd /home/kdc/chia-wallet-sdk
echo "SilentPaymentPending in types.rs (must be >=1):" ; grep -c "struct SilentPaymentPending" crates/chia-sdk-driver/src/silent_payments/types.rs
echo "SilentPaymentPending NOT in send_keys.rs (must be 0):" ; grep -c "struct SilentPaymentPending" crates/chia-sdk-driver/src/silent_payments/send_keys.rs
echo "pub(crate) use send_keys::* removed (must be 0):" ; grep -c "pub(crate) use send_keys::\*" crates/chia-sdk-driver/src/silent_payments/mod.rs
echo "no API names in SP Display strings (must be 0):" ; grep -nE 'curry_tree_hash|from_raw|StandardArgs|public_key\(\)' crates/chia-sdk-driver/src/driver_error.rs | grep '#\[error' | wc -l
cargo build -p chia-sdk-driver
cargo build -p chia-sdk-driver --all-features
cargo clippy -p chia-sdk-driver --all-features --all-targets -- -D warnings
    </automated>
  </verify>
  <done>
    SilentPaymentPending lives in types.rs (not send_keys.rs); mod.rs no longer has `pub(crate) use
    send_keys::*` and re-exports SilentPaymentPending as pub(crate) from types. SP error Display strings are
    terse one-line lowercase with no embedded API names; remediation prose moved to rustdoc. chia-sdk-driver
    builds both WITH and WITHOUT --all-features; clippy clean.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Multi-input SP e2e test + constraint doc + follow-up note + CI drift gate</name>
  <files>
    crates/chia-sdk-driver/tests/silent_payments_e2e.rs,
    crates/chia-sdk-driver/src/silent_payments/mod.rs,
    .github/workflows/rust.yml
  </files>
  <behavior>
    - test_simulator_e2e_multi_input: build 2 distinct-puzzle-hash XCH inputs (two `sim.bls(...)` pairs),
      send to ONE unlabeled SP address with Relation::AssertConcurrent, farm, extract tweak data via
      tweak_data_from_simulator_block, scan, assert EXACTLY ONE DetectedSpCoin at the expected one-time
      puzzle hash, then spend the detected coin successfully (mirrors test_simulator_e2e_unlabeled structure).
    - Edge: the AssertConcurrent runtime gate (spends.rs:629) is satisfied (>=2 non-ephemeral XCH inputs +
      Relation::AssertConcurrent) — finish_with_keys must NOT error with SilentPaymentRequiresInputBinding.
  </behavior>
  <action>
This is items 6 and 7.

ITEM 7 — Add `fn test_simulator_e2e_multi_input` to crates/chia-sdk-driver/tests/silent_payments_e2e.rs.
Mirror the existing test_simulator_e2e_unlabeled (lines 63-147) but with TWO XCH inputs:
  - In setup, instead of the shared single sender, create two distinct pairs: `let a = sim.bls(600);`
    `let b = sim.bls(600);` (two distinct puzzle hashes, two coins). Keep one recipient
    SilentPaymentKeys/unlabeled address.
  - `let mut spends = Spends::new(a.puzzle_hash); spends.add(a.coin); spends.add(b.coin);`
    (Spends::new takes the change ph; both coins added as inputs.)
  - Apply ONE `Action::send(Id::Xch, SendDestination::SilentPayment(Box::new(recipient_address)), 1000,
    Memos::None)` (amount <= a.amount + b.amount minus any fee/change you choose; pick numbers that balance —
    e.g. send 1000, leaving change to a.puzzle_hash automatically via the action system).
  - Build pk_map / synthetic_public_map / synthetic_secret_map keyed by BOTH puzzle hashes (a and b), using
    from_synthetic_unchecked on the raw fixture keys exactly as the single-input tests do (the sim coins are
    curried over the raw pk, so the registered key IS the raw key).
  - `spends.with_silent_payment_keys(synthetic_public_map, synthetic_secret_map);`
  - `spends.finish_with_keys(&mut ctx, &deltas, Relation::AssertConcurrent, &pk_map)?;`  <-- MUST be
    AssertConcurrent (the gate requires it for >=2 non-ephemeral XCH inputs).
  - Sign with BOTH SKs: `sim.spend_coins(ctx.take(), &[a.sk.clone(), b.sk.clone()])?;`
  - Extract: `let tweak_data = tweak_data_from_simulator_block(&sim, height_before);`
  - Scan: `scan_from_tweaks(recipient.scan_sk(), recipient.spend_sk(), recipient.spend_pk(), &tweak_data,
    None, K_MAX_DEFAULT)` → `assert_eq!(detections.len(), 1)`, `detected.label.is_none()`, `detected.k == 0`,
    and `detected.amount == 1000`.
  - Spend the detected coin via `detected.onetime_sk.derive_synthetic()` + StandardLayer, then
    `sim.coin_state(detected.coin_id)` has `spent_height.is_some()` (mirror lines 124-144).
  RED first: write the test, run it, confirm it compiles and the assertions are meaningful (it should PASS on
  the existing real grouping path since this is exercising production logic — if it FAILS, that is a real
  finding; STOP and report rather than weakening assertions). Do NOT change AssertConcurrent cycle-binding
  logic or any production code to make it pass.

ITEM 7 (doc + follow-up) — Add a rustdoc note to the driver SP module
(crates/chia-sdk-driver/src/silent_payments/mod.rs `//!` header) documenting the v1 multi-input constraint:
multi-input SP bundles must consist of distinct-puzzle-hash, non-ephemeral XCH inputs with no
CAT/DID/NFT/intermediate coins, so the receiver's same-puzzle-hash + AssertConcurrent SCC group equals the
sender's exact input set (otherwise input_hash diverges and the coin is undetectable by the recipient).
Phrase it as standalone API documentation (no planning vocabulary — it must satisfy Task 1's residue grep,
which Task 1 re-runs; keep this note clean). ALSO record the deferred FOLLOW-UP design note as a one-line
code comment referencing the SUMMARY: the open question is "filter the AssertConcurrent cycle to the SP
XCH-input set vs. document the constraint" — a privacy/design decision (interacts with the no-SP-marker
on-chain-fingerprint goal) deferred OUT of this cleanup. Do NOT change the AssertConcurrent logic. Capture
the full follow-up rationale in the plan SUMMARY.

ITEM 6 — Wire scripts/sp_descriptor_facade_drift.sh into CI. In .github/workflows/rust.yml, add a step to the
`test` job AFTER "Unused dependencies" (line ~83) and BEFORE the publish steps (line ~85):
```
      - name: SP descriptor/facade drift
        run: bash scripts/sp_descriptor_facade_drift.sh
```
ubuntu-latest has jq + bash 4+ preinstalled (the script's only deps). The script uses `set -euo pipefail`
and non-zero-exits on drift, failing the build. Keep the addition minimal and consistent with the existing
step style — no extra install step.
  </action>
  <verify>
    <automated>
cd /home/kdc/chia-wallet-sdk
echo "new test exists:" ; grep -c "fn test_simulator_e2e_multi_input" crates/chia-sdk-driver/tests/silent_payments_e2e.rs
echo "CI drift step exists:" ; grep -c "sp_descriptor_facade_drift.sh" .github/workflows/rust.yml
echo "drift script passes locally:" ; bash scripts/sp_descriptor_facade_drift.sh && echo DRIFT_OK
cargo test --release -p chia-sdk-driver --all-features --test silent_payments_e2e -- test_simulator_e2e_multi_input
echo "constraint doc note present:" ; grep -ci "distinct-puzzle-hash\|input set\|input_hash diverges" crates/chia-sdk-driver/src/silent_payments/mod.rs
cargo clippy -p chia-sdk-driver --all-features --all-targets -- -D warnings
    </automated>
  </verify>
  <done>
    test_simulator_e2e_multi_input passes, driving 2 distinct-puzzle-hash XCH inputs through the real
    grouping path and asserting exactly one detection that then spends. The v1 multi-input constraint is
    documented in the driver SP module rustdoc and a one-line follow-up code comment references the SUMMARY.
    The drift script runs as a CI step in rust.yml and passes locally. clippy clean.
  </done>
</task>

</tasks>

<verification>
Run the full phase gate after all three tasks (mirrors CI exactly):

```bash
cd /home/kdc/chia-wallet-sdk

# 1. ITEM-1 residue grep across the full SP source set must be 0
SP_FILES=$(find crates -path '*silent_payments*' -name '*.rs')
SP_FILES="$SP_FILES crates/chia-sdk-driver/src/action_system/spends.rs crates/chia-sdk-driver/src/action_system/send_destination.rs crates/chia-sdk-driver/src/action_system/relation.rs crates/chia-sdk-driver/src/actions/silent_payment_send.rs crates/chia-sdk-bindings/src/silent_payments.rs"
grep -rnE '(Phase [0-9]|\b(RECV|SEND|ADDR|CRYPTO|GUARD|BIND|BRIDGE|POLISH|CLEANUP|SIM)-[0-9]|Pitfall [0-9]|Plan [0-9]{2}-|\bD-[0-9]|SC[0-9]\b)' $SP_FILES | wc -l   # == 0
grep -rn finish_with_silent_payment_keys crates/chia-sdk-driver/src | wc -l                                                                                       # == 0

# 2. Build each affected crate WITH and WITHOUT --all-features (chip-0057 gating)
cargo build -p chia-sdk-types -p chia-sdk-utils -p chia-sdk-driver
cargo build -p chia-sdk-types -F chip-0057 ; cargo build -p chia-sdk-utils -F chip-0057 ; cargo build -p chia-sdk-driver -F chip-0057

# 3. Full CI workspace test suite (the constraint's verification bar)
cargo test --release --workspace --all-features --exclude chia-wallet-sdk-napi --exclude chia-sdk-derive --exclude chia-wallet-sdk-py --exclude chia-wallet-sdk-wasm --exclude chia-sdk-bindings --exclude bindy --exclude bindy-macro

# 4. Lint gates
cargo clippy --workspace --all-features --all-targets
cargo fmt --all -- --files-with-diff --check
cargo machete

# 5. Drift guard passes
bash scripts/sp_descriptor_facade_drift.sh

# 6. The new multi-input e2e test passes
cargo test --release -p chia-sdk-driver --all-features --test silent_payments_e2e -- test_simulator_e2e_multi_input
```

Binding test suites (napi/pyo3/wasm) are NOT re-run: no binding test files are touched (the bindings facade
silent_payments.rs is comment-only edits in Task 1; the descriptor JSON is unchanged, so the drift script
stays green).
</verification>

<success_criteria>
- ITEM-1 residue grep returns 0 over all shipping AND #[cfg(test)] SP source.
- No `finish_with_silent_payment_keys` references remain in chia-sdk-driver/src; scaffolding comment gone.
- scalar.rs narrative corrected (quotient=2, result (2^256-1)-2r); pinned bytes UNCHANGED.
- Signed-vs-unsigned mod-r warning deduped to one canonical site (scalar.rs).
- SP DriverError Display strings are terse one-line lowercase with no embedded API names; prose in rustdoc.
- SilentPaymentPending relocated to types.rs; mod.rs re-export simplified (no double re-export of send_keys).
- scripts/sp_descriptor_facade_drift.sh wired into the CI test job and passing.
- test_simulator_e2e_multi_input passes via the real receiver grouping path (exactly one detection, spent).
- v1 multi-input constraint documented in SP rustdoc; deferred follow-up note recorded.
- Full workspace test suite green; clippy --all-targets clean; fmt clean; machete clean — WITH and WITHOUT
  --all-features. Zero new deps, zero new #[allow] (the one pre-existing scanner.rs allow may stay), no unsafe.
</success_criteria>

<output>
After completion, create `.planning/quick/260601-e6z-chip-0057-sp-code-review-cleanup-strip-p/260601-e6z-SUMMARY.md`.

In the SUMMARY, record the deferred FOLLOW-UP design note (Item 7) so it is not lost:
"OPEN QUESTION (deferred out of this cleanup): filter the AssertConcurrent cycle to the SP XCH-input set vs.
document the v1 constraint. This is a privacy/design decision — it interacts with the on-chain-fingerprint
no-SP-marker goal. v1 ships the documented-constraint approach (distinct-puzzle-hash non-ephemeral XCH-only
multi-input bundles); cycle-filtering is a candidate for a future design pass and was NOT changed here."
</output>
