---
type: quick
slug: 260530-dv1-trim-sp-crypto-helpers-from-prelude-clar
created: 2026-05-30
wave: 1
depends_on: []
files_modified:
  - src/prelude.rs
  - crates/chia-sdk-driver/Cargo.toml
autonomous: true
must_haves:
  truths:
    - "prelude::* no longer re-exports the 7 internal SP derivation helpers"
    - "prelude::* still re-exports the 7 user-facing SP driver symbols (incl. K_MAX_DEFAULT)"
    - "examples/silent_payment.rs still compiles (resolves K_MAX_DEFAULT via prelude glob)"
    - "tests/silent_payments_e2e.rs still compiles + passes under -F chip-0057 WITHOUT --all-features"
    - "Zero behavior change: all 7 removed helpers stay reachable via chia_sdk_driver::silent_payments::"
  artifacts:
    - path: "src/prelude.rs"
      provides: "Trimmed chip-0057 driver re-export block (7 user-facing names only)"
      contains: "pub use chia_sdk_driver::silent_payments::"
    - path: "crates/chia-sdk-driver/Cargo.toml"
      provides: "chip-0057 feature array with documented load-bearing dev-dep line"
      contains: "chia-sdk-test/chip-0057"
  key_links:
    - from: "examples/silent_payment.rs"
      to: "src/prelude.rs"
      via: "prelude glob import of K_MAX_DEFAULT"
      pattern: "K_MAX_DEFAULT"
    - from: "crates/chia-sdk-driver/tests/silent_payments_e2e.rs"
      to: "crates/chia-sdk-driver/Cargo.toml chip-0057 feature"
      via: "chia-sdk-test/chip-0057 enables chia_sdk_test::silent_payments::tweak_data_from_simulator_block"
      pattern: "chia-sdk-test/chip-0057"
---

<objective>
Fix two post-v1.0-audit code-review nits in the chip-0057 silent-payments surface. Pure surface/clarity work — NO behavior change, NO API removals from defining modules, NO new dependencies.

1. Trim 7 internal derivation helpers from the curated `prelude::*` (they stay fully reachable via `chia_sdk_driver::silent_payments::`).
2. Add a clarifying inline comment to the load-bearing `chia-sdk-test/chip-0057` dev-dep feature line so it does not look like dead weight (it is NOT removed — it enables the e2e test under non-all-features test builds).

Purpose: Bring the SP prelude surface in line with how Cat/Nft/Did expose only primitive + info types (not internal fns), and document a footgun in the Cargo manifest.
Output: Edited `src/prelude.rs` and `crates/chia-sdk-driver/Cargo.toml`. Two file-disjoint tasks.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md

# CLAUDE.md is authoritative: workspace lints (deny clippy::all + warn pedantic),
# no new workspace deps, chip-0057 is the only SP feature umbrella, per-crate
# builds must pass with AND without --all-features.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Trim 7 internal SP helpers from the curated prelude</name>
  <files>src/prelude.rs</files>
  <read_first>src/prelude.rs, examples/silent_payment.rs</read_first>
  <action>
In `src/prelude.rs`, edit the `#[cfg(feature = "chip-0057")] pub use chia_sdk_driver::silent_payments::{ ... };` block (currently lines 40-46).

REMOVE these 7 internal-helper names:
  - compute_input_hash
  - compute_shared_secret_from_tweak
  - derive_one_time_puzzle_hash
  - derive_onetime_pk
  - derive_onetime_sk
  - derive_output_tweak
  - puzzle_hash_for_pk

KEEP exactly these 7 user-facing names (preserve rustfmt/alphabetical ordering):
  - DetectedSpCoin
  - K_MAX_DEFAULT
  - OutputMeta
  - SilentPaymentScan
  - TweakData
  - scan_from_tweaks
  - tweak_data_from_block_spends

Resulting block should read:
```rust
#[cfg(feature = "chip-0057")]
pub use chia_sdk_driver::silent_payments::{
    DetectedSpCoin, K_MAX_DEFAULT, OutputMeta, SilentPaymentScan, TweakData, scan_from_tweaks,
    tweak_data_from_block_spends,
};
```

Do NOT touch the separate `chia_sdk_utils::silent_payments::{...}` block (lines 34-38) — leave it exactly as-is.

Rationale (already validated, do not re-derive): Cat/Nft/Did expose only their primitive + info types in the curated prelude, not internal derivation fns. The 7 removed helpers remain fully reachable via `chia_sdk_driver::silent_payments::` (and the umbrella `driver::silent_payments::`) — this ONLY drops them from `prelude::*`, it is not an API removal. `K_MAX_DEFAULT` MUST stay: `examples/silent_payment.rs` consumes it via the prelude glob at lines 92 and 182. No in-repo prelude consumer uses any of the 7 removed names (the binding facade reaches `compute_input_hash` via the full `chia_sdk_driver::` path, confirmed; the definitions live in `crates/chia-sdk-driver/src/silent_payments/protocol.rs` and are untouched).

After editing, run `cargo fmt --all` so the kept block stays in canonical rustfmt order; do NOT hand-reflow.
  </action>
  <verify>
    <automated>grep -c "compute_input_hash\|compute_shared_secret_from_tweak\|derive_one_time_puzzle_hash\|derive_onetime_pk\|derive_onetime_sk\|derive_output_tweak\|puzzle_hash_for_pk" src/prelude.rs | grep -qx 0 && echo "REMOVED-OK"</automated>
    <automated>grep -c "K_MAX_DEFAULT\|scan_from_tweaks\|tweak_data_from_block_spends\|SilentPaymentScan\|TweakData\|DetectedSpCoin\|OutputMeta" src/prelude.rs | grep -qx 7 && echo "KEPT-OK"</automated>
    <automated>cargo build -p chia-wallet-sdk --all-features</automated>
    <automated>cargo build --examples --features chip-0057</automated>
    <automated>test "$(grep -rc "pub fn compute_input_hash" crates/chia-sdk-driver/src/silent_payments/protocol.rs)" -ge 1 && echo "DEFS-UNTOUCHED"</automated>
  </verify>
  <done>
The 7 internal helper names are gone from `src/prelude.rs` (grep returns 0); the 7 user-facing names remain (grep returns 7); `chia-wallet-sdk --all-features` and `--examples --features chip-0057` both build (example resolves K_MAX_DEFAULT via prelude); `compute_input_hash` definition still present in `protocol.rs`.
  </done>
</task>

<task type="auto">
  <name>Task 2: Document the load-bearing chia-sdk-test dev-dep feature line</name>
  <files>crates/chia-sdk-driver/Cargo.toml</files>
  <read_first>crates/chia-sdk-driver/Cargo.toml, crates/chia-sdk-driver/tests/silent_payments_e2e.rs</read_first>
  <action>
In `crates/chia-sdk-driver/Cargo.toml`, find the `chip-0057 = [ ... ]` feature array (lines 23-28). The entry `"chia-sdk-test/chip-0057",` (line 27) LOOKS like dead weight because `chia-sdk-test` is only a dev-dependency — but it is load-bearing and MUST NOT be removed.

Why it is load-bearing: it enables `chia-sdk-test`'s own chip-0057 feature so the `#![cfg(feature = "chip-0057")]` integration test `tests/silent_payments_e2e.rs` — which imports `chia_sdk_test::silent_payments::tweak_data_from_simulator_block` (gated behind `chia-sdk-test/chip-0057`) — compiles under `cargo test -p chia-sdk-driver --features chip-0057` WITHOUT `--all-features`. (Confirmed: the e2e test has `#![cfg(feature = "chip-0057")]` at line 1 and `use chia_sdk_test::silent_payments::tweak_data_from_simulator_block;` at line 26.)

ADD a brief inline `#` comment on that line explaining its purpose. Keep valid TOML. The array should read:
```toml
chip-0057 = [
    "chia-sdk-types/chip-0057",
    "dep:chia-sdk-utils",
    "chia-sdk-utils/chip-0057",
    "chia-sdk-test/chip-0057", # dev-dep: enables chia_sdk_test::silent_payments for tests/silent_payments_e2e.rs under non-all-features test builds
]
```

Do NOT remove the line. Do NOT touch the other three entries in the array. Do NOT touch any other feature or dependency.
  </action>
  <verify>
    <automated>grep -q '"chia-sdk-test/chip-0057"' crates/chia-sdk-driver/Cargo.toml && echo "LINE-PRESENT"</automated>
    <automated>grep '"chia-sdk-test/chip-0057"' crates/chia-sdk-driver/Cargo.toml | grep -q '#' && echo "COMMENT-PRESENT"</automated>
    <automated>cargo test -p chia-sdk-driver --features chip-0057 --test silent_payments_e2e</automated>
  </verify>
  <done>
The `"chia-sdk-test/chip-0057"` line is still present (NOT removed) and now carries an inline `#` comment explaining its purpose; `cargo test -p chia-sdk-driver --features chip-0057` (NO `--all-features`) compiles and passes the e2e integration test — proving the line still does its job and the TOML is valid.
  </done>
</task>

</tasks>

<verification>
Whole-task regression gate (run after BOTH tasks land):

```bash
cargo build --release --workspace --all-features         # succeeds
cargo build --examples                                    # no features — succeeds (gated example skipped)
cargo clippy --workspace --all-features --all-targets     # clean (only the 2 pre-existing chia-sdk-daemon warnings allowed)
cargo fmt --all -- --check                                # clean
cargo machete                                             # clean
```

- ZERO new workspace deps.
- ZERO new `#[allow]` attributes.
- ZERO behavior change: the 7 removed names stay reachable via `chia_sdk_driver::silent_payments::` and `driver::silent_payments::`; their definitions in `protocol.rs` are untouched.
</verification>

<success_criteria>
- `grep -c` for the 7 removed names in `src/prelude.rs` returns 0.
- `grep -c` for the 7 kept names in `src/prelude.rs` returns 7.
- `cargo build -p chia-wallet-sdk --all-features` and `cargo build --examples --features chip-0057` both succeed.
- `"chia-sdk-test/chip-0057"` is still present in `crates/chia-sdk-driver/Cargo.toml` with an inline `#` comment.
- `cargo test -p chia-sdk-driver --features chip-0057` (no `--all-features`) compiles and passes the e2e test.
- Full regression gate (build release all-features, examples no-features, clippy, fmt, machete) is clean.
</success_criteria>

<output>
After completion, create `.planning/quick/260530-dv1-trim-sp-crypto-helpers-from-prelude-clar/260530-dv1-SUMMARY.md`
</output>
