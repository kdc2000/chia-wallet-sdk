---
quick_id: 260603-c93
type: execute
status: complete
commit: 69de07e3
completed: 2026-06-03
files_modified:
  - crates/chia-sdk-driver/src/silent_payments/mod.rs
  - crates/chia-sdk-driver/src/silent_payments/types.rs
  - crates/chia-sdk-test/src/silent_payments/mod.rs
  - crates/chia-sdk-driver/src/silent_payments/send_keys.rs
  - crates/chia-sdk-driver/src/driver_error.rs
  - crates/chia-sdk-driver/src/actions/silent_payment_send.rs
  - crates/chia-sdk-driver/src/action_system/spends.rs
  - CLAUDE.md
---

# Quick Task 260603-c93: Reword CHIP-0058 -> future-protocol + remove v1 framing Summary

Editorial prose-only reword across the silent-payments surface (8 files): replaced speculative "CHIP-0058" references with generic future-transport-protocol wording and removed inappropriate "v1" SDK-version-roadmap framing. Zero behavior, signature, or test-assertion changes.

## What Changed

**CHIP-0058 -> future-protocol wording:**
- `silent_payments/mod.rs` (module doc): "a CHIP-0058 transport client" -> "a future transport protocol" (preserved the cross-line wrap cleanly: line 4 ends "a future transport", line 5 begins "protocol,"); "Forward compatibility with CHIP-0058" -> "Forward compatibility with a future transport protocol".
- `silent_payments/types.rs`: "CHIP-0058 transport client, the Phase-6 simulator helper" -> "a future transport protocol, the Phase-6 simulator helper"; "future CHIP-0058 transport client constructs `TweakData`" -> "future transport client constructs `TweakData`".
- `chia-sdk-test/src/silent_payments/mod.rs`: "without going through a CHIP-0058 transport client (out of scope for v1)." -> "without going through a future transport protocol (not currently supported)." (both fixes on one line).
- `CLAUDE.md`: "(once a CHIP-0058 tweak-data source exists)" -> "(once a future tweak-data protocol exists)"; "Forward compatibility with CHIP-0058 ... Any CHIP-0058 transport client" -> "Forward compatibility with a future transport protocol ... Any future transport client".

**v1 version-framing removal:**
- `silent_payments/mod.rs`: heading "# Multi-input send constraint (v1)" -> "# Multi-input send constraint"; "v1 ships this documented constraint" -> "this implementation enforces this documented constraint".
- `send_keys.rs`: "Multi-party flows are out of scope for v1." -> "Multi-party flows are not currently supported."
- `driver_error.rs`: "multi-party silent-payment flows are unsupported in v1." -> "multi-party silent-payment flows are not currently supported."
- `actions/silent_payment_send.rs`: "destinations are XCH-only in v1." -> "destinations are currently XCH-only."
- `action_system/spends.rs`: dropped trailing "v1" from the deferred-constraint comment; continuation line already began with "constraint", yielding "...vs. document the constraint is an open privacy/design question..." with no orphaned word.

## Verification

- `grep -rniE 'chip[-_ ]?0058'` over SP shipping source: **0 hits**.
- `grep -rnE '\bv1\b'` over SP shipping source: **0 hits**.
- `CLAUDE.md` CHIP-0058 mentions reworded (workflow `@v1` pins and maturin version untouched): **clean**.
- `cargo build --release -p chia-sdk-driver --all-features`: **exit 0**.
- `cargo build --release -p chia-sdk-driver` (no features): **exit 0**.
- `cargo fmt --all -- --check`: **clean**.
- `git diff --stat`: 8 files, 14 insertions / 14 deletions (line-balanced prose change).

## Scope Guardrails Honored

- No edits to `.github/workflows/*` (`@v1`/`@v2` action pins, `maturin v1.8.2`).
- No edits to `ClawbackV2` / "clawback v2".
- No edits to "Reward Distributor v1" in `launch_drivers.rs`.
- No crate version-number changes.
- No ROADMAP.md change; no remotes touched.
- Zero new deps, `#[allow]`, or unsafe. All `[`intra-doc`]` links preserved verbatim.

## Deviations from Plan

None - plan executed exactly as written. (Note: the plan's quoted "Resulting two lines" for mod.rs site (1) had a minor transcription artifact; the executed edit applied the stated intent — change "a CHIP-0058 transport client" to "a future transport protocol" while preserving surrounding prose and line-wrap.)

## Commit

- `69de07e3`: docs(260603-c93): reword CHIP-0058 -> future-protocol, drop v1 framing in SP docs

## Self-Check: PASSED

- All 8 modified files present in commit `69de07e3` (verified via `git diff --stat`).
- Commit `69de07e3` exists on `main`.
- Both build modes and fmt verified green post-edit.
