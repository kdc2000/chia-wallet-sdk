---
quick_id: 260603-c93
type: execute
wave: 1
depends_on: []
autonomous: true
files_modified:
  - crates/chia-sdk-driver/src/silent_payments/mod.rs
  - crates/chia-sdk-driver/src/silent_payments/types.rs
  - crates/chia-sdk-test/src/silent_payments/mod.rs
  - crates/chia-sdk-driver/src/silent_payments/send_keys.rs
  - crates/chia-sdk-driver/src/driver_error.rs
  - crates/chia-sdk-driver/src/actions/silent_payment_send.rs
  - crates/chia-sdk-driver/src/action_system/spends.rs
  - CLAUDE.md
---

<objective>
Editorial reword of two code-review findings across the silent-payments surface:
1. Replace speculative "CHIP-0058" references with generic future-protocol wording.
2. Remove "v1" SDK-version framing (the contributor is not the chia-wallet-sdk maintainer, so version-roadmap promises are inappropriate).

Purpose: Land clean review feedback before the orchestrator rebuilds the review PR branch. The wording is speculative/presumptuous and must be neutralized without altering any technical meaning.

Output: Eight files edited in comment/rustdoc/module-doc prose ONLY. Zero behavior, signature, or test-assertion changes.

SCOPE GUARDRAIL — DO NOT TOUCH:
- GitHub Actions `@v1`/`@v2` action pins and `maturin v1.8.2` in `.github/workflows/*`
- `ClawbackV2` / "clawback v2"
- "Reward Distributor v1" in `launch_drivers.rs`
- Crate version numbers (`0.33.0`, etc.)
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
Comment/doc prose ONLY — zero code/signature/test changes. No new deps, no new `#[allow]`, no unsafe.

Workspace clippy is `deny clippy::all` and rustdoc is `deny all` — so do NOT introduce broken intra-doc links. All edits preserve existing `[`backticked`]` intra-doc references verbatim; only surrounding plain prose changes.

Exact current wording confirmed via grep at each site (line numbers approximate — match on the quoted strings, not the numbers).
</context>

<tasks>

<task type="auto">
  <name>Task 1: Reword CHIP-0058 references and remove v1 framing (8 files, prose only)</name>
  <files>crates/chia-sdk-driver/src/silent_payments/mod.rs, crates/chia-sdk-driver/src/silent_payments/types.rs, crates/chia-sdk-test/src/silent_payments/mod.rs, crates/chia-sdk-driver/src/silent_payments/send_keys.rs, crates/chia-sdk-driver/src/driver_error.rs, crates/chia-sdk-driver/src/actions/silent_payment_send.rs, crates/chia-sdk-driver/src/action_system/spends.rs, CLAUDE.md</files>
  <action>
Apply the following EXACT prose edits. Use the Edit tool with the quoted current text as the search target. Change ONLY the words specified; preserve all surrounding whitespace, `//!` / `///` / `//` comment markers, `[`backticked`]` intra-doc links, and line wrapping intent. These are doc/comment lines — no Rust code, signatures, or test assertions are touched.

=== CHIP-0058 → future-protocol wording ===

(1) crates/chia-sdk-driver/src/silent_payments/mod.rs
  - Line ~4: `//! A wallet that receives [`TweakData`] from any source (a CHIP-0058 transport`
    → change "a CHIP-0058 transport" to "a future transport protocol". The following line (~5) reads `//! client, the `chia_sdk_test` simulator helper, ...` — since "transport client" was split across lines 4-5, after the reword line 4 ends with "a future transport protocol" and line 5 should drop the now-orphaned leading word "client,". Resulting two lines:
      `//! A wallet that receives [`TweakData`] from any source (a future transport`
      `//! protocol, the `chia_sdk_test` simulator helper, or a handcrafted fixture) can`
    (Preserve the rest of line 5 verbatim: `detect payments addressed to its scan/spend key pair via [`scan_from_tweaks`].` on line 6 is unchanged.) Keep the TweakData-is-transport-agnostic point intact.
  - Line ~14: `//! Forward compatibility with CHIP-0058: [`TweakData`] has no transport fields`
    → `//! Forward compatibility with a future transport protocol: [`TweakData`] has no transport fields`
    (Keep the TweakData-is-transport-agnostic point. The trailing `[`TweakData`]` link is preserved.)

(2) crates/chia-sdk-driver/src/silent_payments/types.rs
  - Line ~7: `//!   CHIP-0058 transport client, the Phase-6 simulator helper, etc.) but`
    → `//!   a future transport protocol, the Phase-6 simulator helper, etc.) but`
  - Line ~30: `/// future CHIP-0058 transport client constructs `TweakData` from its wire`
    → `/// future transport client constructs `TweakData` from its wire`
    (Preserve the `[`TweakData`]`/`` `TweakData` `` backtick token verbatim.)

(3) crates/chia-sdk-test/src/silent_payments/mod.rs
  - Line ~5 (BOTH fixes on one line): `//! without going through a CHIP-0058 transport client (out of scope for v1).`
    → `//! without going through a future transport protocol (not currently supported).`

(4) CLAUDE.md
  - Line ~121: `(once a CHIP-0058 tweak-data source exists)`
    → `(once a future tweak-data protocol exists)` — preserve the rest of the sentence verbatim.
  - Line ~130: `**Forward compatibility with CHIP-0058**: The receive-side primitive accepts a transport-agnostic `TweakData` input. Any CHIP-0058 transport client built later must be able to construct `TweakData` from its wire messages without breaking the existing SDK API.`
    → reword the two CHIP-0058 mentions preserving meaning:
      `**Forward compatibility with a future transport protocol**: The receive-side primitive accepts a transport-agnostic `TweakData` input. Any future transport client built later must be able to construct `TweakData` from its wire messages without breaking the existing SDK API.`

=== v1 version-framing removal ===

(5) crates/chia-sdk-driver/src/silent_payments/mod.rs
  - Line ~19: `//! # Multi-input send constraint (v1)`
    → `//! # Multi-input send constraint`
  - Line ~31: `//! recipient. v1 ships this documented constraint; sending to a silent-payment`
    → `//! recipient. this implementation enforces this documented constraint; sending to a silent-payment`
    (Keep the rest of the sentence verbatim across the following lines: `destination across mixed asset types or with extra cycle members is out of` / `scope.`)

(6) crates/chia-sdk-driver/src/silent_payments/send_keys.rs
  - Line ~165: `/// Multi-party flows are out of scope for v1.`
    → `/// Multi-party flows are not currently supported.`

(7) crates/chia-sdk-driver/src/driver_error.rs
  - Line ~140: `/// required — multi-party silent-payment flows are unsupported in v1.`
    → `/// required — multi-party silent-payment flows are not currently supported.`

(8) crates/chia-sdk-driver/src/actions/silent_payment_send.rs
  - Line ~634: `/// destinations are XCH-only in v1.`
    → `/// destinations are currently XCH-only.`

(9) crates/chia-sdk-driver/src/action_system/spends.rs
  - Line ~629: `// AssertConcurrent cycle to exactly the SP XCH-input set vs. document the v1`
    → `// AssertConcurrent cycle to exactly the SP XCH-input set vs. document the`
    (Drop only the trailing word "v1". The continuation on the next line `// constraint is an open privacy/design question...` already begins with "constraint", yielding "...vs. document the constraint is an open privacy/design question...". Confirm the next line starts with "constraint"; if it starts with "v1 constraint" instead, drop "v1" from there. Net result: the phrase reads "...vs. document the constraint is an open privacy/design question..." with no "v1".)

After all edits, do NOT run git push or touch remotes. Do NOT update ROADMAP.md.
  </action>
  <verify>
    <automated>
# Both greps MUST return 0 hits over the SP shipping source (chip-0058 + standalone v1 word).
# Run from repo root /home/kdc/chia-wallet-sdk.
SRC="crates/chia-sdk-driver/src/silent_payments crates/chia-sdk-test/src/silent_payments crates/chia-sdk-driver/src/action_system/spends.rs crates/chia-sdk-driver/src/actions/silent_payment_send.rs crates/chia-sdk-driver/src/driver_error.rs"
C58=$(grep -rniE 'chip[-_ ]?0058' $SRC | wc -l)
V1=$(grep -rnE '\bv1\b' $SRC | wc -l)
echo "chip-0058 hits: $C58 (expect 0)"; echo "v1 hits: $V1 (expect 0)"
test "$C58" -eq 0 &amp;&amp; test "$V1" -eq 0 || { echo "FAIL: residual hits"; exit 1; }
# Also confirm CLAUDE.md chip-0058 reworded (CLAUDE.md is doc, not SP source — separate check).
grep -niE 'chip[-_ ]?0058' CLAUDE.md | grep -v 'workflows' &amp;&amp; { echo "FAIL: CLAUDE.md still has CHIP-0058"; exit 1; } || echo "CLAUDE.md clean"
# Builds (both feature modes) + fmt must pass.
cargo build --release -p chia-sdk-driver --all-features
cargo build --release -p chia-sdk-driver
cargo fmt --all -- --check
    </automated>
  </verify>
  <done>
- `grep -rniE 'chip[-_ ]?0058'` over SP shipping source returns 0 hits.
- `grep -rnE '\bv1\b'` over SP shipping source returns 0 hits.
- CLAUDE.md lines 121 and 130 no longer reference CHIP-0058 (workflow `@v1` pins and maturin version untouched).
- `cargo build --release -p chia-sdk-driver --all-features` exits 0.
- `cargo build --release -p chia-sdk-driver` (no features) exits 0.
- `cargo fmt --all -- --check` exits 0 (clean).
- Zero behavior/signature/test-assertion changes; zero new deps, `#[allow]`, or unsafe. All `[`intra-doc`]` links preserved (no new rustdoc-deny breakage).
  </done>
</task>

</tasks>

<verification>
- chip-0058 grep: 0 hits over SP shipping source.
- standalone-v1 grep: 0 hits over SP shipping source.
- CLAUDE.md CHIP-0058 mentions reworded (workflow/maturin v-strings untouched).
- `--all-features` and no-features `chia-sdk-driver` release builds both exit 0.
- `cargo fmt --all -- --check` clean.
- Scope guardrail honored: no edits to `.github/workflows/*`, `ClawbackV2`, "Reward Distributor v1", or crate version numbers.
</verification>

<success_criteria>
Eight files reworded in prose only; both grep gates return 0 over SP shipping source; CLAUDE.md CHIP-0058 mentions reworded; both build modes and fmt pass; no remotes touched; ROADMAP.md untouched.
</success_criteria>

<output>
After completion, create `.planning/quick/260603-c93-reword-review-findings-chip-0058-future-/260603-c93-SUMMARY.md`
</output>
