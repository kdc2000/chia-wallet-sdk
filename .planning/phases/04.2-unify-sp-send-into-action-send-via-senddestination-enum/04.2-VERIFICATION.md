---
phase: 04.2-unify-sp-send-into-action-send-via-senddestination-enum
verified: 2026-05-17T23:47:29Z
status: passed
score: 13/13 success criteria verified
re_verification: null
gaps: null
human_verification: null
---

# Phase 04.2: Unify SP send into Action::send via SendDestination enum — Verification Report

**Phase Goal:** Fold `SilentPaymentSend` into `Action::send` via a new `SendDestination` enum so wallets have a single send-action surface for both regular and silent-payment destinations. SP becomes a *destination type*, not a separate action variant. Matches Sage's existing model where every wallet operation goes through one `Action::send(Id, p2_puzzle_hash, amount, memos)` call shape — under this refactor, Sage's existing 12 `Action::send` call sites continue to compile unchanged via `impl From<Bytes32> for SendDestination`.

**Verified:** 2026-05-17T23:47:29Z
**Status:** passed
**Re-verification:** No — initial verification

## Goal Achievement

### Observable Truths (13 Success Criteria)

| #   | Truth                                                                                                                                                                       | Status     | Evidence                                                                                                                                                                                                       |
| --- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 1   | `Action::SilentPaymentSend` variant + constructor + dispatch arms removed from `action_system/action.rs`                                                                    | VERIFIED   | `action.rs` line 20-32: `Action` enum has 11 variants, no `SilentPaymentSend`. No `Action::silent_payment_send` constructor (grep returned 0 hits in action.rs). Dispatch arms in `SpendAction for Action` impl have no SP variant. |
| 2   | `SendDestination` enum exists with `PuzzleHash(Bytes32)` always-on + `SilentPayment(...)` chip-0057-gated + `impl From<Bytes32>`                                            | VERIFIED   | `crates/chia-sdk-driver/src/action_system/send_destination.rs` (51 lines): `pub enum SendDestination { PuzzleHash(Bytes32), #[cfg(feature = "chip-0057")] SilentPayment(Box<SilentPaymentAddress>) }` + `impl From<Bytes32> for SendDestination` at lines 46-50. Boxed to satisfy `clippy::large_enum_variant`. |
| 3   | `Action::send` signature is `(id, destination: impl Into<SendDestination>, amount, memos)`; `SendAction.destination: SendDestination`                                       | VERIFIED   | `action.rs` lines 47-54: `pub fn send(id: Id, destination: impl Into<SendDestination>, amount: u64, memos: Memos)` calls `Self::Send(SendAction::new(id, destination.into(), ...))`. `actions/send.rs` line 18: `pub destination: SendDestination`. SendAction loses `Copy` (verified: only `#[derive(Debug, Clone)]`). |
| 4   | `Spends::with_silent_payment_keys` builder + chip-0057 fields present; `silent_payment_counters` and `silent_payments_pending` preserved                                    | VERIFIED   | `action_system/spends.rs` lines 31-37: 4 chip-0057-gated `pub(crate)` fields (`silent_payment_counters`, `silent_payments_pending`, `silent_payment_synthetic_pks`, `silent_payment_synthetic_sks`). Lines 113-121: `pub fn with_silent_payment_keys(&mut self, ...) -> &mut Self` chainable builder writes `Some(map)` into both Option fields. Mirror sites for Unfinished→Finished migration verified at lines 498-504. |
| 5   | `Spends::finish_with_silent_payment_keys` deleted; logic absorbed into chip-0057 branch of `Spends::finish_with_keys`                                                       | VERIFIED   | Method gone (no hits in non-build sources). `finish_with_keys` at `spends.rs:524-567` has chip-0057 SP branch at lines 534-537 calling private free fn `sp_finish_branch` (lines 586-680+) that absorbs the 9-step derivation pipeline with proper gate ordering (Pitfall 7). |
| 6   | `crates/chia-sdk-driver/src/actions/silent_payment_send.rs` deleted; apply-time logic in `actions/send.rs::SendAction::spend` chip-0057 branch                              | VERIFIED   | File does not exist (`ls` returns "No such file or directory"). `actions.rs` no longer declares `silent_payment_send` module. `actions/send.rs:46-56` has chip-0057 SP arm at top of `SendAction::spend`. Helpers `spend_silent_payment` (lines 135-179) and `memo_hint_guard` (lines 192+) relocated. |
| 7   | `silent_payments/send_keys.rs` shrunk to derivation helper / type holder                                                                                                    | VERIFIED   | File survives at 218 lines (down from 365). Contains `SilentPaymentPending` `pub(crate)` struct + module rustdoc referencing the new `sp_finish_branch` helper + 3 reshaped tests (`multi_party_hard_errors`, `multi_input_requires_assert_concurrent_relation`, `single_input_accepts_relation_none`). |
| 8   | `DriverError::SilentPaymentKeysNotRegistered` exists (chip-0057-gated)                                                                                                       | VERIFIED   | `driver_error.rs:160-164`: variant gated on `#[cfg(feature = "chip-0057")]` with locked error text "silent payment send requires Spends::with_silent_payment_keys to be called before finish (no SP secret keys registered)". Fires from gate 2 of `sp_finish_branch` (spends.rs:606-608). |
| 9   | `DriverError::SilentPaymentRequiresXch` exists (chip-0057-gated)                                                                                                            | VERIFIED   | `driver_error.rs:166-170`: variant gated on `#[cfg(feature = "chip-0057")]` with locked error text "silent payment destination requires Id::Xch (CAT/NFT/option silent payments are deferred to v2)". Fires from `SendAction::spend` chip-0057 arm (`actions/send.rs:51-53`). |
| 10  | Phase 4.1 work preserved: `SilentPaymentRequiresInputBinding`, `Relation::AssertConcurrent` gate, cycle pinning test in `action_system/spends.rs::tests`, `Relation` rustdoc | VERIFIED   | `driver_error.rs:154-158`: `SilentPaymentRequiresInputBinding` still chip-0057-gated. `spends.rs:601`: gate fires first in `sp_finish_branch`. `spends.rs:916/921/926`: 3 cycle pinning tests (`assert_concurrent_relation_emits_cycle_for_n_coins_{2,3,4}`) untouched and PASS. `action_system/relation.rs:4,9,26`: 3 CHIP-0057 rustdoc mentions. |
| 11  | All existing SP tests pass under new construction pattern                                                                                                                    | VERIFIED   | Test run results: 10 silent_payment_tests in `actions/send.rs` PASS (action_state_machine, round_trip_matches_derive_one_time_puzzle_hash, multi_output_same_scan_pk_increments_k, multi_output_distinct_scan_pks_independent_counters, input_hash_round_trip, 3 memo guard tests, 2 NEW Wave 0 acceptance tests). 3 send_keys.rs tests PASS. 3 D-06 cycle pinning tests PASS. **16/16 SP-domain tests PASS** under new API. |
| 12  | Bindings descriptor pattern verified ahead of Phase 5 (no bindy-macro changes required)                                                                                     | VERIFIED   | `04.2-RESEARCH.md:551-572` confirms SendDestination slots into bindy's existing opaque-handle pattern (per `Id` precedent at `bindings/action_system.json:222-281`). RESEARCH §Pitfall 6 explicitly directs NOT to update `bindings/action_system.json` in 04.2 — the Rust binding facade at `chia-sdk-bindings/src/action_system.rs:294` keeps passing `Bytes32` and compiles via `From<Bytes32>`. Phase 5 carries the JSON update. |
| 13  | Workspace gates green: build, clippy chip-0057 -D warnings, fmt --check, machete; grep bans hold                                                                            | VERIFIED   | Re-ran during this verification: `cargo build --release --workspace --all-features` SUCCEEDS (1 pre-existing -W on SendDestination missing_copy_implementations, expected — boxed SilentPayment variant makes Copy impossible). `cargo clippy -p chia-sdk-driver --features chip-0057 --all-targets -- -D warnings` CLEAN. `cargo fmt --all --check` CLEAN. `cargo machete` reports no unused deps. Grep bans verified: `mod_by_group_order` 0 hits, `^use sha2::` 0 hits in `silent_payments/`, `Sha256::digest` 0 hits in `silent_payments/`, `emit_silent_payment_announcements` 0 hits. SEND-08 Privacy-warning count: 10 (≥10 target). |

**Score:** 13/13 success criteria verified

### Required Artifacts

| Artifact                                                                                | Expected                                                            | Status     | Details                                                                                                                                                       |
| --------------------------------------------------------------------------------------- | ------------------------------------------------------------------- | ---------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `crates/chia-sdk-driver/src/action_system/send_destination.rs`                          | NEW: SendDestination enum + From<Bytes32>                           | VERIFIED   | Exists (51 lines); contains required pattern; wired into `action_system.rs` module barrel (lines 8, 21).                                                       |
| `crates/chia-sdk-driver/src/action_system/action.rs`                                    | Action::send signature widened; SilentPaymentSend deleted          | VERIFIED   | 297 lines; Action::send signature uses `impl Into<SendDestination>`; no SilentPaymentSend variant; 11 chip-0057-agnostic variants; Privacy-warning rustdoc.    |
| `crates/chia-sdk-driver/src/action_system/spends.rs`                                    | 4 chip-0057 fields + with_silent_payment_keys + sp_finish_branch    | VERIFIED   | All 4 fields present, chainable builder at 113-121, chip-0057 SP branch at 534-537 before prepare(), private `sp_finish_branch` free fn at 585+.               |
| `crates/chia-sdk-driver/src/actions/send.rs`                                            | SendAction.destination field + chip-0057 SP arm + helpers + 10 tests | VERIFIED   | 1100+ lines; SendAction::destination at line 18; chip-0057 SP arm at 46-56; spend_silent_payment + memo_hint_guard helpers; 10 silent_payment_tests pass.    |
| `crates/chia-sdk-driver/src/actions/silent_payment_send.rs`                             | DELETED                                                             | VERIFIED   | File does not exist (`ls` returns ENOENT).                                                                                                                    |
| `crates/chia-sdk-driver/src/silent_payments/send_keys.rs`                               | Shrunk; finish_with_silent_payment_keys removed; 3 reshaped tests   | VERIFIED   | 218 lines (down from 365); SilentPaymentPending pub(crate) struct retained; 3 reshaped tests use new API.                                                      |
| `crates/chia-sdk-driver/src/driver_error.rs`                                            | 2 new chip-0057 error variants                                      | VERIFIED   | Lines 160-164 (SilentPaymentKeysNotRegistered) and 166-170 (SilentPaymentRequiresXch), both chip-0057-gated with locked error text.                            |
| `src/prelude.rs`                                                                        | SendDestination in always-on block; SilentPaymentSend removed       | VERIFIED   | Line 19: `SendDestination` in always-on driver re-export. `SilentPaymentSend` absent (grep 0 hits).                                                            |
| `.planning/REQUIREMENTS.md`                                                             | ACTION-API-01 entry + SEND-04 description updated + traceability     | VERIFIED   | Line 21 (SEND-04 updated), Line 22 (ACTION-API-01 new), Lines 99-100 (traceability), Line 126 (Last updated 2026-05-17).                                       |

### Key Link Verification

| From                                              | To                                                       | Via                                                          | Status | Details                                                                                                                          |
| ------------------------------------------------- | -------------------------------------------------------- | ------------------------------------------------------------ | ------ | -------------------------------------------------------------------------------------------------------------------------------- |
| `action_system/send_destination.rs`               | `chia_sdk_utils::silent_payments::SilentPaymentAddress` | `use` under chip-0057                                        | WIRED  | Line 11-12: `#[cfg(feature = "chip-0057")] use chia_sdk_utils::silent_payments::SilentPaymentAddress;`                            |
| `action_system/spends.rs:with_silent_payment_keys` | 2 chip-0057 SP key Option<IndexMap> fields              | chainable setter writing `Some(_)`                           | WIRED  | Lines 118-119: `self.silent_payment_synthetic_pks = Some(...); self.silent_payment_synthetic_sks = Some(...);`                   |
| `action_system.rs` barrel                         | `send_destination` module                                | `pub mod` + `pub use`                                        | WIRED  | Line 8: `mod send_destination;` Line 21: `pub use send_destination::*;`                                                          |
| `actions/send.rs:SendAction::spend`               | `Spends::silent_payments_pending` + `silent_payment_counters` | chip-0057 SP arm + helper `spend_silent_payment`            | WIRED  | Apply-time pushes pending entry (send.rs:167) + increments scan_pk counter (send.rs:162).                                       |
| `action_system/spends.rs:finish_with_keys`        | `sp_finish_branch` consumer of `silent_payments_pending`  | chip-0057 SP branch before prepare()                         | WIRED  | Lines 534-537 short-circuit on `is_empty`; sp_finish_branch consumes via `std::mem::take` (line 648).                           |
| `action_system/action.rs:Action::send`            | `impl Into<SendDestination>` coercion                    | `destination.into()` call                                    | WIRED  | Line 53: `Self::Send(SendAction::new(id, destination.into(), amount, memos))`.                                                  |
| `crates/chia-sdk-bindings/src/action_system.rs:294` | `sdk::Action::send` (Bytes32 puzzle_hash passthrough)  | `From<Bytes32>` source-compat                                | WIRED  | Verified: bindings facade passes raw `puzzle_hash: Bytes32` and compiles unchanged via `From<Bytes32>` impl in send_destination.rs. |
| Prelude SendDestination re-export                 | always-on block (not chip-0057)                          | `pub use chia_sdk_driver::SendDestination` in always-on block | WIRED  | Line 19 confirms in always-on driver block.                                                                                     |

### Data-Flow Trace (Level 4) — Not Applicable

Phase 04.2 is a pure-Rust refactor with no UI / external service / dynamic data flow. The data flow is the API surface itself; it has been verified by running the test suite (16 SP-domain tests PASS, all using the new construction pattern end-to-end including apply→finish→derived one-time puzzle hash round-trip).

### Behavioral Spot-Checks

| Behavior                                                              | Command                                                                                                                  | Result                                                                                                                                                 | Status |
| --------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------ | ------------------------------------------------------------------------------------------------------------------------------------------------------ | ------ |
| Workspace builds clean at all features                                | `cargo build --release --workspace --all-features`                                                                       | Compiles successfully. 1 pre-existing `-W missing-copy-implementations` on SendDestination (Box-variant makes Copy impossible — documented exemption). | PASS   |
| Driver chip-0057 clippy `-D warnings`                                 | `cargo clippy -p chia-sdk-driver --features chip-0057 --all-targets -- -D warnings`                                      | Clean. No errors, no warnings.                                                                                                                         | PASS   |
| Workspace formatter check                                             | `cargo fmt --all --check`                                                                                                | Clean exit code 0.                                                                                                                                     | PASS   |
| Unused dependencies                                                   | `cargo machete`                                                                                                          | "didn't find any unused dependencies".                                                                                                                 | PASS   |
| 10 silent_payment_tests in actions/send.rs                            | `cargo test --release -p chia-sdk-driver --features chip-0057 --lib actions::send::silent_payment_tests`                | 10 passed; 0 failed.                                                                                                                                   | PASS   |
| 3 reshaped tests in silent_payments/send_keys.rs                      | `cargo test --release -p chia-sdk-driver --features chip-0057 --lib silent_payments::send_keys::tests`                   | 3 passed; 0 failed.                                                                                                                                    | PASS   |
| 3 D-06 cycle pinning tests in action_system/spends.rs                 | `cargo test --release -p chia-sdk-driver --features chip-0057 --lib action_system::spends::tests::assert_concurrent_relation_emits_cycle` | 3 passed; 0 failed.                                                                                                                                    | PASS   |
| Grep ban: `mod_by_group_order` absent                                 | `grep -rn mod_by_group_order crates/ src/`                                                                              | 0 hits.                                                                                                                                                | PASS   |
| Grep ban: `^use sha2::` absent under silent_payments/                | `grep -rn '^use sha2::' crates/chia-sdk-driver/src/silent_payments/`                                                    | 0 hits.                                                                                                                                                | PASS   |
| Grep ban: `Sha256::digest` absent under silent_payments/             | `grep -rn 'Sha256::digest' crates/chia-sdk-driver/src/silent_payments/`                                                 | 0 hits.                                                                                                                                                | PASS   |
| FINGERPRINT-01: `emit_silent_payment_announcements` absent           | `grep -rn emit_silent_payment_announcements crates/ src/`                                                               | 0 hits.                                                                                                                                                | PASS   |
| FINGERPRINT-01: Pass 2b marker in send_keys.rs                       | `grep -ni 'Pass 2b' crates/chia-sdk-driver/src/silent_payments/send_keys.rs`                                            | 1 hit (line 10 — rustdoc reference).                                                                                                                   | PASS   |
| SEND-08 Privacy-warning count ≥10                                    | `grep -rn 'Privacy warning:' crates/chia-sdk-driver/src/`                                                               | 10 hits.                                                                                                                                               | PASS   |
| OLD API absent: SilentPaymentSend in code                            | `grep -rn SilentPaymentSend crates/.../src/ src/ napi/ pyo3/ wasm/` (excluding stale rustdoc + relocated test comments) | 0 functional references; only 2 stale rustdoc strings (one_time.rs:56, relation.rs:27) reference the old name in narrative comments. Non-blocking.    | PASS   |

### Requirements Coverage

| Requirement   | Source Plan         | Description                                                                                                       | Status    | Evidence                                                                                                                                                                                                                                                                       |
| ------------- | ------------------- | ----------------------------------------------------------------------------------------------------------------- | --------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| SEND-04       | 04.2-02, 04.2-03    | Silent-payment send composes with Spends action system via `Action::send` + `Spends::finish_with_keys` chip-0057 branch + `with_silent_payment_keys` registration | SATISFIED | REQUIREMENTS.md:21 description updated to reference new construction shape (historical Phase 4 form preserved as parenthetical). Re-validated by 16/16 SP-domain tests passing under the unified API. Traceability row: "Phase 4 / Phase 4.2" (REQUIREMENTS.md:99).            |
| ACTION-API-01 | 04.2-01, 04.2-02, 04.2-03 | Unified send-action public surface: single `Action::send(id, impl Into<SendDestination>, amount, memos)` constructor; SP code internal; `From<Bytes32>` source-compat; runtime gates `SilentPaymentRequiresXch` + `SilentPaymentKeysNotRegistered`         | SATISFIED | REQUIREMENTS.md:22 — new entry with full description. Traceability row at line 100 ("Phase 4.2"). Acceptance tests `silent_payment_destination_requires_xch_id` + `silent_payment_keys_not_registered_errors_at_finish` PASS. Bindings facade keeps passing Bytes32 unchanged. |

Requirement IDs from all 3 plan frontmatters (`SEND-04`, `ACTION-API-01`) are accounted for. No orphaned requirements.

### Anti-Patterns Found

| File                                                                  | Line   | Pattern                                                                                       | Severity | Impact                                                                                                                          |
| --------------------------------------------------------------------- | ------ | --------------------------------------------------------------------------------------------- | -------- | ------------------------------------------------------------------------------------------------------------------------------- |
| `crates/chia-sdk-driver/src/silent_payments/one_time.rs`              | 56     | Stale rustdoc reference to `SilentPaymentSend::memos` (deleted type)                          | Info     | Documentation drift; cargo doc still renders. Logged for follow-up; does not break build, clippy, or tests.                     |
| `crates/chia-sdk-driver/src/action_system/relation.rs`                | 27     | Stale rustdoc reference to "Wallets emitting `SilentPaymentSend` actions" (deleted variant)   | Info     | Documentation drift; same as above.                                                                                             |
| `crates/chia-sdk-driver/src/silent_payments/aggregate.rs`             | 11, 19 | Stale rustdoc reference to `Spends::finish_with_silent_payment_keys` (deleted method)         | Info     | Documentation drift; logged for follow-up. Does not break gates.                                                                |
| `crates/chia-sdk-driver/src/silent_payments/one_time.rs`              | 20     | Stale rustdoc reference to `Spends::finish_with_silent_payment_keys` (deleted method)         | Info     | Same as above.                                                                                                                  |
| `crates/chia-sdk-driver/src/silent_payments/input_hash.rs`            | 35     | Stale rustdoc reference to `Spends::finish_with_silent_payment_keys` (deleted method)         | Info     | Same as above.                                                                                                                  |
| `crates/chia-sdk-driver/src/action_system/relation.rs`                | 8, 28  | Stale rustdoc references to `silent_payments/send_keys.rs::finish_with_silent_payment_keys`   | Info     | Same as above.                                                                                                                  |
| `crates/chia-sdk-utils/src/silent_payments/mod.rs`                    | 40     | Pre-existing rustdoc broken intra-doc link (NOT introduced by Phase 04.2)                     | Info     | Documented in `deferred-items.md`. Verified pre-existing on `main`. Out of Phase 04.2 scope. Does not block CI gates.           |

**None of the above are blockers.** They are documentation drift items where rustdoc comments still reference the old symbol names. Build, clippy `-D warnings`, fmt, machete, and the full SP test suite all pass. These can be cleaned up in a future docs pass; they do not affect goal achievement.

### Human Verification Required

No human verification needed. Phase 04.2 is a pure-Rust API refactor whose behavior is fully exercised by the 16 SP-domain tests (10 in `actions/send.rs::silent_payment_tests`, 3 in `silent_payments/send_keys.rs::tests`, 3 D-06 cycle pinning tests in `action_system/spends.rs::tests`). All gates programmatically verified.

### Gaps Summary

**No gaps found.** All 13 success criteria pass under direct codebase verification:

- The OLD API (`Action::SilentPaymentSend` variant + `Action::silent_payment_send` constructor + `actions/silent_payment_send.rs` file + `Spends::finish_with_silent_payment_keys` method) is comprehensively GONE.
- The NEW API (`SendDestination` enum + `Action::send` widened with `impl Into<SendDestination>` + `Spends::with_silent_payment_keys` + chip-0057 SP branch of `Spends::finish_with_keys` via `sp_finish_branch` private free fn) is in place and wired end-to-end.
- Source-compat via `From<Bytes32> for SendDestination` is verified at the bindings facade caller (`chia-sdk-bindings/src/action_system.rs:294`) — it still passes raw `Bytes32` and compiles unchanged.
- All Phase 4.1 D-06 work preserved: `SilentPaymentRequiresInputBinding`, `Relation::AssertConcurrent` requirement, 3 cycle-pinning tests, `Relation` rustdoc with ≥1 CHIP-0057 mention.
- All workspace gates green at this verification timestamp: build, clippy chip-0057 `-D warnings`, fmt `--check`, machete, FINGERPRINT-01 grep bans, Phase 1 grep bans, SEND-08 Privacy-warning count ≥10.
- Both requirement IDs declared in plan frontmatters (`SEND-04` re-validated, `ACTION-API-01` newly closed) are SATISFIED in `.planning/REQUIREMENTS.md` with traceability rows at Phase 4.2.
- Stale rustdoc references in 6 sites referencing the now-deleted symbol names are flagged as Info-severity anti-patterns; they do not affect any CI gate or runtime behavior and can be cleaned up in a future docs sweep.

Phase 04.2 goal achieved. Phase 5 (Bindings) is fully unblocked — the post-04.2 API surface (`Action::send` + `SendDestination` + `Spends::with_silent_payment_keys` + `Spends::finish_with_keys`) is the final shape Phase 5's binding descriptors will expose.

---

_Verified: 2026-05-17T23:47:29Z_
_Verifier: Claude (gsd-verifier)_
