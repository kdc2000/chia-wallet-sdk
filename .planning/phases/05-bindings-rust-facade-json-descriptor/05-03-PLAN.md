---
phase: 05-bindings-rust-facade-json-descriptor
plan: 03
type: execute
wave: 2
depends_on:
  - 05-02
files_modified:
  - napi/index.d.ts
  - napi/index.js
  - pyo3/python/chia_wallet_sdk
  - wasm/pkg
  - bindings.json
autonomous: true
requirements:
  - BIND-01
  - BIND-02

must_haves:
  truths:
    - "napi build (via pnpm build) succeeds and generates index.d.ts exposing all 5 SC1 SP types + the 4 static functions"
    - "pyo3 build (via maturin develop) succeeds, confirming pyo3-target binding crate compiles against the new chip-0057 descriptor"
    - "wasm-pack build --target nodejs succeeds, confirming wasm-target binding crate compiles against the new descriptor"
    - "If Vec<PublicKey> marshaling fails on wasm/napi (Open Question Q1), fallback type-group entries in bindings.json land inline"
    - "Generated napi/index.d.ts contains SendDestination class with all 6 methods"
  artifacts:
    - path: "napi/index.d.ts"
      provides: "Generated TypeScript declarations exposing SP surface for the AVA round-trip test in Plan 05-04"
      contains: "SilentPaymentAddress"
    - path: "napi/index.js"
      provides: "Generated JS wrapper module loaded by AVA tests"
    - path: "bindings.json"
      provides: "Root type_groups + per-target maps; possibly modified inline if Vec<PublicKey> needs explicit entries"
  key_links:
    - from: "bindings/silent_payments.json"
      to: "napi/index.d.ts"
      via: "bindy_napi!('bindings.json') macro expansion during pnpm build"
      pattern: "scanFromTweaks"
    - from: "bindings/silent_payments.json"
      to: "pyo3 .so / .pyi"
      via: "bindy_pyo3!() macro expansion during maturin develop"
      pattern: "scan_from_tweaks"
    - from: "bindings/silent_payments.json"
      to: "wasm/pkg/*.d.ts"
      via: "bindy_wasm!() macro expansion during wasm-pack build"
      pattern: "scanFromTweaks"
---

<objective>
Wave 2 — verify the three target binding crates build cleanly against the Wave-1 descriptor + facade.

After this plan:
- `cd napi && pnpm install && pnpm build` succeeds; the generated `napi/index.d.ts` exposes `SilentPaymentAddress`, `SilentPaymentKeys`, `TweakData`, `DetectedSpCoin`, `LabelRegistry`, `ScalarField`, `SilentPayments`, `SilentPaymentNetwork`, `OutputMeta`, and `SendDestination`. The 4 static methods (`scanFromTweaks`, `deriveOneTimePuzzleHash`, `computeInputHash`, `aggregateSenderSks`) are callable as `SilentPayments.scanFromTweaks(...)` etc.
- `cd pyo3 && maturin develop` succeeds (Python module compiles and installs to the venv).
- `cd wasm && wasm-pack build --target nodejs` succeeds (wasm package emits to `wasm/pkg/`).
- If `Vec<PublicKey>` triggers wasm marshaling failure (RESEARCH §"Open Questions" Q1), the fallback fix lands inline in `bindings.json` `wasm` + `wasm_stubs` sections (precedent: `Vec<Bytes32>` entries at bindings.json:33-34).
- The three target builds become reproducible — Plan 05-04 invokes them as preconditions to the AVA test.

Purpose: Cross-target validation. Each target binding crate consumes the same `bindings/*.json` descriptors through its own bindy macro invocation (`bindy_napi!()`, `bindy_pyo3!()`, `bindy_wasm!()`). Each codepath in bindy-macro is independent — a descriptor that compiles for napi may still fail for wasm (different marshaling rules) or pyo3 (different async runtime). This plan exercises all three.

Output: A reproducibly-buildable cross-target binding surface for chip-0057 silent payments. Plan 05-04 writes the AVA round-trip test against the napi-generated TypeScript surface.

**Toolchain preconditions** — verify present BEFORE starting tasks; if any are missing, install:
- `pnpm --version` → expect 9.x (pinned at 9.11.0)
- `maturin --version` → install via `pipx install maturin` or `pip install maturin` if missing
- `wasm-pack --version` → install via `cargo install wasm-pack` if missing
- `rustup target list --installed | grep wasm32-unknown-unknown` → install via `rustup target add wasm32-unknown-unknown` if missing
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/05-bindings-rust-facade-json-descriptor/05-CONTEXT.md
@.planning/phases/05-bindings-rust-facade-json-descriptor/05-RESEARCH.md
@.planning/phases/05-bindings-rust-facade-json-descriptor/05-PHASE-NOTES.md
@.planning/phases/05-bindings-rust-facade-json-descriptor/05-02-SUMMARY.md
@CLAUDE.md
@bindings.json
@napi/package.json
@napi/src/lib.rs
@wasm/package.json

<interfaces>
<!-- Build commands and expected artifacts. -->

### napi build
- Command: `cd napi && pnpm install && pnpm build`
- `pnpm build` runs `napi build --platform --release` per `napi/package.json:17`
- Artifact: `napi/index.d.ts` (TS declarations) + `napi/index.js` (CommonJS wrapper) + `napi/chia-wallet-sdk.*.node` (per-platform native binary)
- Generated `.d.ts` shape for a static method (per bindy-macro/src/lib.rs:302-303 + 311-324):
  ```typescript
  export class SilentPayments {
    static scanFromTweaks(scanSk: PublicKey, ...): Array<DetectedSpCoin>;
    // ...
  }
  ```

### pyo3 build
- Command: `cd pyo3 && maturin develop`
- maturin compiles the pyo3 binding crate + installs the resulting wheel into the active Python env
- Artifact: `pyo3/python/chia_wallet_sdk/*.so` + `chia_wallet_sdk/*.pyi`
- Generated `.pyi` shape for a static method:
  ```python
  class SilentPayments:
      @staticmethod
      def scan_from_tweaks(scan_sk: SecretKey, ...) -> list[DetectedSpCoin]: ...
  ```

### wasm build
- Command: `cd wasm && wasm-pack build --target nodejs`
- wasm-pack invokes `cargo build --target wasm32-unknown-unknown --release` + post-processes
- Artifact: `wasm/pkg/*.wasm` + `wasm/pkg/*.d.ts` + `wasm/pkg/*.js`
- Generated `.d.ts` shape for a static method (typescript_custom_section, per bindy-macro/src/lib.rs:725-737):
  ```typescript
  export class SilentPayments {
    static scanFromTweaks(scan_sk: PublicKey, ...): DetectedSpCoin[];
  }
  ```

### bindings.json type_groups fallback (Open Question Q1)
If wasm fails on `Vec<PublicKey>`, append to `bindings.json` per precedent at lines 33-34 + 46:
```json
  "wasm": {
    ...
    "Vec<PublicKey>": "js_sys::Array",
    ...
  },
  "wasm_stubs": {
    ...
    "Vec<PublicKey>": "PublicKey[]",
    ...
  }
```
This is the same shape `Vec<Bytes32>` and `Vec<Bytes>` already use. Add only if the build fails on this — otherwise omit (don't add speculative entries; `cargo machete` and CI cleanliness prefer minimal config).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: napi build — generate index.d.ts and verify all SC1 symbols exposed</name>
  <files>napi/index.d.ts, napi/index.js, .planning/phases/05-bindings-rust-facade-json-descriptor/05-VALIDATION.md</files>
  <read_first>
    - napi/package.json — confirm `build` script and AVA config
    - napi/src/lib.rs — confirm `bindy_macro::bindy_napi!("bindings.json")` invocation pattern (no hand-edits needed; the macro reads bindings/ on its own)
    - bindings/silent_payments.json — the descriptor about to be code-generated
    - bindings/action_system.json — the modified SendDestination + Action.send descriptor
  </read_first>
  <action>
**Step 1: Install pnpm deps (idempotent; no-op if up to date)**

```bash
cd /home/kdc/chia-wallet-sdk/napi && pnpm install
```

If this fails with "pnpm not found", install pnpm globally: `npm install -g pnpm@9.11.0` and retry.

**Step 2: Build napi**

```bash
cd /home/kdc/chia-wallet-sdk/napi && pnpm build
```

Expected: produces `napi/index.d.ts` + `napi/index.js` + a per-platform `*.node` binary. Build time ~60-120 seconds.

If the build fails with errors like:
- `cannot find type 'SilentPayments'` → the bindy_macro didn't pick up the new JSON; verify `bindings/silent_payments.json` is well-formed via `jq .` AND that the macro invocation in `napi/src/lib.rs:8` reads from the root `bindings.json` (which then auto-discovers `bindings/*.json` via bindy-macro/src/lib.rs:95-104).
- `Vec<PublicKey>` marshaling — add a `Vec<PublicKey>` entry to `bindings.json`'s `napi` section if missing. Precedent: `Vec<Bytes32>` doesn't appear in the `napi` map (it defaults to `Array<Uint8Array>`), so `Vec<PublicKey>` should also work by default. If it doesn't, append: `"Vec<PublicKey>": "napi::bindgen_prelude::Array<...>"` — but try without first.
- `cannot find type 'SilentPaymentAddress'` in `bindings/action_system.json`'s SendDestination factory — both descriptors must be loaded by the macro (auto-discovery from `bindings/`); the issue is more likely a name typo. Re-verify the JSON entry name exactly matches `SilentPaymentAddress` (case-sensitive).

**Step 3: Verify generated `napi/index.d.ts` exposes all expected symbols**

```bash
cd /home/kdc/chia-wallet-sdk
grep -E 'export (class|enum) (SilentPaymentAddress|SilentPaymentKeys|TweakData|DetectedSpCoin|LabelRegistry|ScalarField|SilentPayments|SilentPaymentNetwork|OutputMeta|SendDestination)' napi/index.d.ts
```

Expected: 10 matches (one per type). If fewer, examine `napi/index.d.ts` to see which types are missing and why (likely a typo or mismatched arg count in `silent_payments.json` causing the macro to skip the class).

```bash
grep -E '(scanFromTweaks|deriveOneTimePuzzleHash|computeInputHash|aggregateSenderSks)' napi/index.d.ts
```

Expected: at least 4 matches (one per static method; bindy converts snake_case to camelCase for napi/wasm targets).

```bash
grep -E 'destination: SendDestination' napi/index.d.ts
```

Expected: 1+ matches confirming `Action.send` signature is the SendDestination shape (NOT the old `puzzle_hash: Uint8Array` shape).

If `destination: SendDestination` is absent but `puzzle_hash: Uint8Array` is present, the Wave-1 descriptor change in Plan 05-02 Task 3 didn't take — re-verify `jq '.Action.methods.send.args.destination' bindings/action_system.json`.

**Step 4: Update VALIDATION.md** — set the napi row in the per-task verification map to `✅ green`. Open `.planning/phases/05-bindings-rust-facade-json-descriptor/05-VALIDATION.md`, find the row `| napi build produces index.d.ts with required symbols |`, change `⬜ pending` to `✅ green` in the final column.
  </action>
  <verify>
    <automated>cd napi && pnpm install && pnpm build && grep -qE 'export class SilentPaymentAddress' index.d.ts && grep -qE 'export class SilentPayments[^A-Za-z]' index.d.ts && grep -qE 'export class SendDestination' index.d.ts && grep -qE 'scanFromTweaks' index.d.ts && grep -qE 'deriveOneTimePuzzleHash' index.d.ts && grep -qE 'computeInputHash' index.d.ts && grep -qE 'aggregateSenderSks' index.d.ts && grep -qE 'destination:\s*SendDestination' index.d.ts</automated>
  </verify>
  <acceptance_criteria>
    - `napi/index.d.ts` file exists and is newer than the last `bindings/silent_payments.json` modification time — verify with: `[ napi/index.d.ts -nt bindings/silent_payments.json ]`
    - All 10 expected types exported — verify with: `[ "$(grep -cE 'export (class|enum) (SilentPaymentAddress|SilentPaymentKeys|TweakData|DetectedSpCoin|LabelRegistry|ScalarField|SilentPayments|SilentPaymentNetwork|OutputMeta|SendDestination)\b' napi/index.d.ts)" -ge 10 ]`
    - All 4 SilentPayments static methods exposed — verify with: `for m in scanFromTweaks deriveOneTimePuzzleHash computeInputHash aggregateSenderSks; do grep -q "$m" napi/index.d.ts || { echo "missing: $m"; exit 1; }; done`
    - Action.send signature uses SendDestination (NOT puzzle_hash: Uint8Array) — verify with: `grep -qE 'destination:\s*SendDestination' napi/index.d.ts`
    - VALIDATION.md napi row marked ✅ green — verify with: `grep -E 'napi build produces.*✅ green' .planning/phases/05-bindings-rust-facade-json-descriptor/05-VALIDATION.md`
  </acceptance_criteria>
  <done>
    napi crate builds cleanly with the new chip-0057 descriptor and the generated TypeScript declarations expose every SC1 + SC3 symbol with correct shapes.
  </done>
</task>

<task type="auto">
  <name>Task 2: pyo3 build via maturin develop</name>
  <files>pyo3/python/chia_wallet_sdk, .planning/phases/05-bindings-rust-facade-json-descriptor/05-VALIDATION.md</files>
  <read_first>
    - pyo3/Cargo.toml — confirm the package name (used by `cargo build -p <name>` if direct cargo build needed)
    - pyo3/src/lib.rs — confirm `bindy_macro::bindy_pyo3!()` invocation
    - CLAUDE.md "Commands" section — line documenting `maturin develop && pytest` as the pyo3 test recipe
  </read_first>
  <action>
**Step 1: Verify maturin is available**

```bash
maturin --version || pipx install maturin || pip install --user maturin
which maturin
```

**Step 2: Build pyo3 binding crate via maturin**

```bash
cd /home/kdc/chia-wallet-sdk/pyo3 && maturin develop 2>&1 | tail -40
```

Expected: maturin compiles the binding crate, builds a wheel, and installs the resulting Python module into the active environment. Build time ~120-180 seconds (similar to napi).

If maturin fails with `error: no virtual environment found`: create one first via `python3 -m venv .venv && source .venv/bin/activate && pip install maturin && maturin develop`. Document the venv path in PHASE-NOTES.md so subsequent re-runs use the same env.

If maturin fails with bindy macro errors mentioning a SP type:
- The pyo3 codegen path (bindy-macro/src/lib.rs:1210-1392) for static methods uses `#[staticmethod]`; the IntEnum path for `SilentPaymentNetwork` uses `#[pyo3::pyclass(eq, eq_int)]`. Check the error text and consult the relevant macro source range.
- Most pyo3-specific issues are around `IntoPy` / `FromPyObject` for `Option<T>` of opaque types — if you see this, the workaround is to expose the `T` directly and return a sentinel value (e.g., `forward(m) -> Option<PublicKey>` → may need a wrapper). Try the build first; only fall back if it fails.

If maturin fails with errors about `Vec<PublicKey>` marshaling:
- pyo3 typically handles `Vec<T>` for any class type natively. If it doesn't, add to `bindings.json`'s `pyo3` section: `"Vec<PublicKey>": "Vec<PublicKey>"`. Likely not needed.

**Step 3: Verify build completed**

```bash
cd /home/kdc/chia-wallet-sdk
find pyo3 -name '*.so' -newer bindings/silent_payments.json -mmin -10 2>/dev/null
```

Expected: at least one `.so` file produced. If none, the build did not complete successfully despite reaching the end — check `maturin develop` exit code.

```bash
python3 -c "import chia_wallet_sdk; print(chia_wallet_sdk.SilentPaymentNetwork.Mainnet); print(chia_wallet_sdk.SilentPayments)"
```

Expected: prints `SilentPaymentNetwork.Mainnet` and `<class 'chia_wallet_sdk.SilentPayments'>` without ImportError. If `ImportError: cannot import name 'SilentPayments'`, the macro skipped the class — return to napi-debug step 2.

**Step 4: Update VALIDATION.md** — set the pyo3 row in the per-task verification map to `✅ green`. Change `⬜ pending` to `✅ green` for the `pyo3 maturin develop succeeds` row.
  </action>
  <verify>
    <automated>cd pyo3 && maturin develop 2>&1 | tail -5 && python3 -c "import chia_wallet_sdk; assert hasattr(chia_wallet_sdk, 'SilentPaymentAddress'); assert hasattr(chia_wallet_sdk, 'SilentPaymentKeys'); assert hasattr(chia_wallet_sdk, 'SilentPayments'); assert hasattr(chia_wallet_sdk, 'TweakData'); assert hasattr(chia_wallet_sdk, 'DetectedSpCoin'); assert hasattr(chia_wallet_sdk, 'LabelRegistry'); assert hasattr(chia_wallet_sdk, 'ScalarField'); assert hasattr(chia_wallet_sdk, 'SilentPaymentNetwork'); assert hasattr(chia_wallet_sdk, 'OutputMeta'); assert hasattr(chia_wallet_sdk, 'SendDestination'); print('all 10 types present')"</automated>
  </verify>
  <acceptance_criteria>
    - `maturin develop` exits 0 (verified by the automated command above)
    - Python smoke test confirms 10 SP type imports — verify via the python3 -c check above producing "all 10 types present"
    - At least one .so file in pyo3/ tree newer than bindings/silent_payments.json — verify with: `find pyo3 -name '*.so' -newer bindings/silent_payments.json | head -1 | grep -q '.so'`
    - VALIDATION.md pyo3 row marked ✅ green — verify with: `grep -E 'pyo3 maturin develop.*✅ green' .planning/phases/05-bindings-rust-facade-json-descriptor/05-VALIDATION.md`
  </acceptance_criteria>
  <done>
    pyo3 crate compiles cleanly with the new chip-0057 descriptor and a Python interpreter can import the installed module and access all 10 SP types.
  </done>
</task>

<task type="auto">
  <name>Task 3: wasm-pack build (nodejs target) — verify wasm-bindgen emits correct TS stubs</name>
  <files>wasm/pkg, bindings.json, .planning/phases/05-bindings-rust-facade-json-descriptor/05-PHASE-NOTES.md, .planning/phases/05-bindings-rust-facade-json-descriptor/05-VALIDATION.md</files>
  <read_first>
    - wasm/package.json — confirm `test` script invokes `wasm-pack build --target nodejs && ava`
    - wasm/src/lib.rs — confirm `bindy_macro::bindy_wasm!()` invocation
    - bindings.json lines 30-49 — wasm + wasm_stubs sections; the fallback Vec<PublicKey> entries land here if needed
    - .planning/phases/05-bindings-rust-facade-json-descriptor/05-RESEARCH.md §"Open Questions" Q1 — the predicted Vec<PublicKey> risk
  </read_first>
  <action>
**Step 1: Verify wasm32 target + wasm-pack available**

```bash
rustup target list --installed | grep wasm32-unknown-unknown || rustup target add wasm32-unknown-unknown
wasm-pack --version || cargo install wasm-pack
```

**Step 2: Run wasm-pack build**

```bash
cd /home/kdc/chia-wallet-sdk/wasm && pnpm install && wasm-pack build --target nodejs 2>&1 | tail -50
```

Expected: produces `wasm/pkg/*.wasm` + `wasm/pkg/*.d.ts` + `wasm/pkg/*.js`. Build time ~90-180 seconds.

**Common failures + remediations**:

(a) `error[E0277]: the trait bound 'js_sys::Array: From<Vec<PublicKey>>' is not satisfied` — the predicted Vec<PublicKey> issue. Apply the inline fallback per RESEARCH §"Open Questions" Q1:

Open `bindings.json` and insert these entries:

In the `"wasm": { ... }` block (around line 30), add this entry alphabetically:
```json
    "Vec<PublicKey>": "js_sys::Array",
```

In the `"wasm_stubs": { ... }` block (around line 38), add this entry alphabetically:
```json
    "Vec<PublicKey>": "PublicKey[]",
```

(Precedent: existing entries for `Vec<Bytes32>` on lines 33-34 and 46-47.)

Re-run `wasm-pack build --target nodejs`. If still failing, escalate to the wrapper-type fallback: define a `TweakPoints(Vec<PublicKey>)` newtype in both the JSON descriptor and the silent_payments.rs facade, and use `TweakPoints` as the `tweak_points` field type instead of `Vec<PublicKey>`. This is more invasive but guaranteed to work. Document the fallback in PHASE-NOTES.md.

(b) `error: TypeScript output for SilentPaymentNetwork enum has wrong discriminant` — wasm IntEnums use numeric discriminants per bindy-macro/src/lib.rs:981-1026. If the generated `.d.ts` shows `Mainnet = "Mainnet"` (string) instead of `Mainnet = 0` (number), the bindy-macro wasm path produced the wrong shape. Unlikely — the macro handles `Enum { values: [...] }` correctly per RESEARCH Pattern 4. If it does happen, inspect the macro output and adjust.

(c) `cannot find type 'SecretKey' in wasm_bindgen scope` — chia-bls types are usually `remote: true` in bindings/bls.json; verify they're loaded by the macro (they are — auto-discovery picks them up).

**Step 3: Verify generated wasm pkg has TS stubs**

```bash
cd /home/kdc/chia-wallet-sdk
ls wasm/pkg/
test -f wasm/pkg/*.d.ts
grep -l "SilentPaymentAddress" wasm/pkg/*.d.ts
grep -l "scanFromTweaks" wasm/pkg/*.d.ts
```

Expected: at least one `.d.ts` mentions both `SilentPaymentAddress` and `scanFromTweaks` (the file naming depends on `wasm/Cargo.toml`'s package name; typical pattern is `chia_wallet_sdk_wasm.d.ts` or similar).

**Step 4: Update VALIDATION.md** — set the wasm row to `✅ green`.

**Step 5: If a Vec<PublicKey> fallback was applied, record it in PHASE-NOTES.md** with a "Wave 2 Vec<PublicKey> marshaling fix" section listing the inline `bindings.json` changes and the rationale. Otherwise, add a one-line note: "Wave 2 Vec<PublicKey> marshaling: PASSED without fallback (auto-detected by bindy-macro)."
  </action>
  <verify>
    <automated>cd wasm && pnpm install && wasm-pack build --target nodejs 2>&1 | tail -5 && grep -lq 'SilentPaymentAddress' pkg/*.d.ts && grep -lq 'scanFromTweaks' pkg/*.d.ts</automated>
  </verify>
  <acceptance_criteria>
    - `wasm-pack build --target nodejs` exits 0 (verified by the automated command)
    - `wasm/pkg/*.wasm` file exists — verify with: `ls wasm/pkg/*.wasm`
    - `wasm/pkg/*.d.ts` contains SilentPaymentAddress + scanFromTweaks — verify with the grep checks above
    - VALIDATION.md wasm row marked ✅ green — verify with: `grep -E 'wasm-pack build.*✅ green' .planning/phases/05-bindings-rust-facade-json-descriptor/05-VALIDATION.md`
    - PHASE-NOTES.md records the Vec<PublicKey> marshaling status (PASSED or fallback-applied) — verify with: `grep -qE 'Wave 2 Vec<PublicKey>' .planning/phases/05-bindings-rust-facade-json-descriptor/05-PHASE-NOTES.md`
  </acceptance_criteria>
  <done>
    wasm crate builds cleanly via wasm-pack with the new chip-0057 descriptor; the nodejs-target wasm pkg exposes all SP types with correct shapes; any Vec<PublicKey> marshaling issues are resolved inline and documented.
  </done>
</task>

</tasks>

<verification>
After all 3 target builds succeed:

1. `cd napi && pnpm build` — exit 0 + `napi/index.d.ts` contains all 10 SC1+SC3 symbols
2. `cd pyo3 && maturin develop` — exit 0 + Python import smoke test passes
3. `cd wasm && wasm-pack build --target nodejs` — exit 0 + `pkg/*.d.ts` contains SilentPaymentAddress + scanFromTweaks
4. `bindings.json` either unchanged OR contains Vec<PublicKey> entries in `wasm`/`wasm_stubs` sections (documented in PHASE-NOTES.md)
5. VALIDATION.md per-task map: napi/pyo3/wasm rows all ✅ green
</verification>

<success_criteria>
- BIND-01 (descriptor compiles to napi + pyo3 + wasm) — fully landed across all three targets
- BIND-02 (send-side primitives reachable from all 3 targets) — same
- Phase 5 SC1 (per-target build verification: napi build, maturin develop, wasm-pack build all succeed) — fully landed

Plan complete when:
- All 3 target build commands exit 0
- napi-generated TS declarations contain all 10 SP-related types + 4 static methods + the SendDestination signature on Action.send
- pyo3-installed module exposes all 10 SP-related types via Python import
- wasm pkg contains TS declarations mentioning SilentPaymentAddress + scanFromTweaks
- Vec<PublicKey> marshaling status documented in PHASE-NOTES.md
- VALIDATION.md per-task map updated to show ✅ green for napi/pyo3/wasm rows
</success_criteria>

<output>
After completion, create `.planning/phases/05-bindings-rust-facade-json-descriptor/05-03-SUMMARY.md` per the standard SUMMARY template.
</output>
