---
phase: 09.3-non-breaking-sp-send-restore-action-send-contract-make-sp-send-additive-revert-action-api-01-unification
plan: 03
type: execute
wave: 3
depends_on: ["09.3-01", "09.3-02"]
files_modified:
  - .planning/REQUIREMENTS.md
autonomous: false
requirements:
  - ACTION-API-01
  - SEND-04

must_haves:
  truths:
    - "Sage main compiles against this branch with ZERO source edits (modulo the upstream FeeAction.reserved confound)"
    - "The full CI lint matrix is green across all chip-0057 permutations (on and off, per-crate)"
    - "ACTION-API-01 and SEND-04 in REQUIREMENTS.md describe SP send as a distinct flag-gated action, not a SendDestination folded into send"
  artifacts:
    - path: ".planning/REQUIREMENTS.md"
      provides: "Reframed ACTION-API-01 + SEND-04 wording"
      contains: "Action::silent_payment_send"
  key_links:
    - from: "this branch (restored Action::send contract)"
      to: "Sage main (sage-wallet, sage, sage-rpc, sage-api)"
      via: "[patch.crates-io] + cargo check"
      pattern: "patch.crates-io"
---

<objective>
Close the phase: reframe ACTION-API-01 + SEND-04 in REQUIREMENTS.md (SP send is a distinct `chip-0057`-gated action, not a `SendDestination` folded into `send`), run the full CI lint matrix across all `chip-0057` permutations, and run the AC3 Sage zero-edit recompile harness — the empirical proof the source-break is gone.

Purpose: AC3 (Sage compiles with zero source edits) and AC5 (fmt/clippy/machete across all permutations) are the phase's hardest gates. AC3 requires an external repo (Sage) not vendored here, so it is a documented manual/scripted checkpoint. AC5 is the full workspace lint sweep that the per-crate Wave-1/2 gates approximate but do not cover end-to-end.

Output: REQUIREMENTS.md reframed; full lint matrix green; Sage `cargo check -p sage-wallet -p sage -p sage-rpc -p sage-api` produces ZERO SP-caused errors (neutralizing the upstream `FeeAction.reserved` E0063 confound).

This plan gates on Plans 01 (Rust core) + 02 (bindings) — both must be green first.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/09.3-non-breaking-sp-send-restore-action-send-contract-make-sp-send-additive-revert-action-api-01-unification/09.3-CONTEXT.md
@.planning/phases/09.3-non-breaking-sp-send-restore-action-send-contract-make-sp-send-additive-revert-action-api-01-unification/09.3-RESEARCH.md
@.planning/phases/09.3-non-breaking-sp-send-restore-action-send-contract-make-sp-send-additive-revert-action-api-01-unification/09.3-VALIDATION.md

<interfaces>
<!-- The two REQUIREMENTS.md entries to reframe (current wording). -->

REQUIREMENTS.md SEND-04 (line 21): currently says the parenthetical "Phase 04.2 unified the surface into the generic `Action::send` + `Spends::finish_with_keys` per the `SendDestination` enum refactor". Reframe to: Phase 4 shipped a dedicated `Action::silent_payment_send`; Phase 4.2 briefly unified into `Action::send` via `SendDestination`; Phase 9.3 restored the dedicated `chip-0057`-gated `Action::silent_payment_send` action and reverted `Action::send` to the concrete `Bytes32` contract (non-breaking).

REQUIREMENTS.md ACTION-API-01 (line 22): currently "Unified send-action public surface: a single `Action::send(id: Id, destination: impl Into<SendDestination>, ...)` ... no public `silent_payment_send` factory remains ... `From<Bytes32> for SendDestination` keeps all 28 callers ...". Reframe to: SP send is a distinct `chip-0057`-gated `Action::SilentPaymentSend` variant + `Action::silent_payment_send(recipient, amount, memos)` constructor; the generic `Action::send(id: Id, puzzle_hash: Bytes32, amount: u64, memos: Memos)` is concrete and never source-breaks consumers. Runtime enforcement is `DriverError::SilentPaymentKeysNotRegistered` (SP applied without `with_silent_payment_keys` before finish); the action is structurally XCH-only so the former `SilentPaymentRequiresXch` is removed.

Traceability table (lines 138, 137): the ACTION-API-01 / SEND-04 rows reference "Phase 4.2 unified". Update the Notes column to reflect the 9.3 reversion.

Sage recompile recipe (from CONTEXT.md §Empirical evidence + RESEARCH §8):
- clone `xch-dev/sage` `main`
- add to Sage's root Cargo.toml: `[patch.crates-io] chia-wallet-sdk = { path = "/home/kdc/chia-wallet-sdk" }`
- `cargo check -p sage-wallet -p sage -p sage-rpc -p sage-api`
- Sage uses features `rustls`/`offer-compression`/`peer-simulator`, NO `chip-0057`.
- NEUTRALIZE the upstream `FeeAction.reserved` E0063 at `sage/src/endpoints/action_system.rs:138` (upstream PR #349 drift, NOT this phase). If it is the ONLY remaining error, AC3 passes.
- The four pre-fix breaks were: E0283 ×4 at make_offer.rs:92,101,110,119 (`.into()` ambiguity) + E0560 ×1 at action_system.rs:64 (`SendAction { puzzle_hash }` literal). All five must now be GONE.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Reframe ACTION-API-01 + SEND-04 in REQUIREMENTS.md</name>
  <read_first>
    - .planning/REQUIREMENTS.md (SEND-04 line 21, ACTION-API-01 line 22, Traceability rows for SEND-04 line 137 + ACTION-API-01 line 138)
    - .planning/phases/09.3-.../09.3-CONTEXT.md §Scope ("reframe ACTION-API-01 ... and SEND-04 wording")
    - .planning/phases/09.3-.../09.3-RESEARCH.md §phase_requirements (the exact reframed wording the research recommends)
  </read_first>
  <action>
    Pure documentation edit — no source, no behavior. Reframe two requirement entries and their Traceability rows so the source of truth matches the shipped 9.3 reality.

    EDIT A — SEND-04 (line 21 parenthetical): replace the "Phase 04.2 unified the surface into the generic `Action::send`" note with:
    "(Phase 4 shipped a dedicated `Action::silent_payment_send` + `Spends::finish_with_silent_payment_keys`; Phase 4.2 briefly unified the surface into `Action::send` via a `SendDestination` enum; Phase 9.3 reverted that unification — SP send is again a distinct `chip-0057`-gated `Action::silent_payment_send` action and `Action::send(id, puzzle_hash: Bytes32, amount, memos)` is concrete and non-breaking. The apply+finish closure semantics — record `SilentPaymentPending` at apply, derive at finish via `Spends::finish_with_keys` after `with_silent_payment_keys` — are unchanged.)"

    EDIT B — ACTION-API-01 (line 22): replace the body with:
    "**ACTION-API-01** — Silent-payment send is a distinct `chip-0057`-gated `Action::SilentPaymentSend(SilentPaymentSendAction)` variant constructed via `Action::silent_payment_send(recipient: SilentPaymentAddress, amount: u64, memos: Memos)`. The generic `Action::send(id: Id, puzzle_hash: Bytes32, amount: u64, memos: Memos)` is concrete (no `impl Into`), so all existing `Action::send(id, ph, ...)` and `Action::send(id, x.into(), ...)` callers compile unchanged — no source break. The dedicated action is structurally XCH-only (no `Id` param). Runtime enforcement: `DriverError::SilentPaymentKeysNotRegistered` fires if an SP send is applied without a prior `Spends::with_silent_payment_keys` call before finish. (Phase 4.2's `SendDestination` enum + `impl Into<SendDestination>` source-broke consumers — Sage E0283/E0560 — and was reverted in Phase 9.3; `SendDestination` and the now-unreachable `DriverError::SilentPaymentRequiresXch` are removed.)"

    EDIT C — Traceability table:
    - SEND-04 row (line 137): change Notes to "Phase 4 dedicated action; 4.2 unified via SendDestination; 9.3 reverted to dedicated chip-0057 Action::silent_payment_send + concrete Action::send".
    - ACTION-API-01 row (line 138): change the Phase column to "Phase 4.2 / Phase 9.3" and Notes to "9.3 reframed: SP send is a distinct chip-0057-gated Action::silent_payment_send; Action::send is concrete Bytes32 (non-breaking)".

    EDIT D — append a dated note to the "Last updated" footer (line 182) recording the 9.3 reframe (one sentence).
  </action>
  <verify>
    <automated>grep -c "Action::silent_payment_send" .planning/REQUIREMENTS.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "Action::silent_payment_send" .planning/REQUIREMENTS.md` returns >= 2 (SEND-04 + ACTION-API-01 both reference the restored ctor).
    - The only remaining mentions of `SendDestination` in the requirement BODIES are historical/reverted-context phrasing (ACTION-API-01's parenthetical noting it was reverted); the live API is no longer described as using it.
    - `grep -q "SilentPaymentKeysNotRegistered" .planning/REQUIREMENTS.md` (runtime enforcement still documented).
    - `SilentPaymentRequiresXch` appears only in the "removed" context (ACTION-API-01 notes its removal), not as a live enforcement claim.
  </acceptance_criteria>
  <done>ACTION-API-01 + SEND-04 (body + Traceability rows + footer) describe SP send as a distinct `chip-0057`-gated `Action::silent_payment_send` action with a concrete non-breaking `Action::send`; the `SendDestination` framing is marked as reverted.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Full CI lint matrix across all chip-0057 permutations (AC5)</name>
  <read_first>
    - CLAUDE.md §Commands (the exact CI build/test/lint lines; per-crate -F chip-0057 builds)
    - .planning/phases/09.3-.../09.3-VALIDATION.md (AC5 row: fmt/clippy/machete all permutations)
    - .planning/phases/09.3-.../09.3-RESEARCH.md §"Common Pitfalls" (Pitfall 2 chip-off; Pitfall 3 missing_copy)
  </read_first>
  <action>
    Run the full workspace + per-crate lint matrix. No source edits unless a gate fails (fix inline in the file that introduced the regression — from Plan 01 or 02 — then re-run; do NOT add `#[allow]`).

    Run and confirm each exits clean:
    1. `cargo fmt --all -- --files-with-diff --check` (formatting).
    2. `cargo build --release --all-features` (full workspace, chip-0057 ON via --all-features).
    3. `cargo build --release --workspace` (no features — chip-0057 OFF; the gated SP variant/arms/ctor must vanish cleanly).
    4. Per-crate -F chip-0057 builds for the SP-touching crates:
       - `cargo build --release -p chia-sdk-driver -F chip-0057`
       - `cargo build --release -p chia-sdk-bindings -F napi` and `-F wasm` and `-F pyo3` (each builds the binding glue with chip-0057-default deps).
    5. `cargo clippy --workspace --all-features --all-targets` (CI's clippy gate — must be warning-free).
    6. `cargo clippy -p chia-sdk-driver --features chip-0057 --all-targets -- -D warnings` AND `cargo clippy -p chia-sdk-driver --all-targets -- -D warnings` (chip-on AND chip-off, pedantic-as-error).
    7. `cargo test --release --workspace --all-features --exclude chia-wallet-sdk-napi --exclude chia-sdk-derive --exclude chia-wallet-sdk-py --exclude chia-wallet-sdk-wasm --exclude chia-sdk-bindings --exclude bindy --exclude bindy-macro` (the CLAUDE.md CI test line — full Rust suite green, SP oracle tests included).
    8. `cargo machete` (zero new ignored entries; deleting SendDestination must not orphan a dep edge).
  </action>
  <verify>
    <automated>cargo fmt --all -- --files-with-diff --check && cargo build --release --all-features && cargo build --release --workspace && cargo clippy --workspace --all-features --all-targets && cargo test --release --workspace --all-features --exclude chia-wallet-sdk-napi --exclude chia-sdk-derive --exclude chia-wallet-sdk-py --exclude chia-wallet-sdk-wasm --exclude chia-sdk-bindings --exclude bindy --exclude bindy-macro</automated>
  </verify>
  <acceptance_criteria>
    - `cargo fmt --all -- --files-with-diff --check` exits 0.
    - `cargo build --release --all-features` exits 0.
    - `cargo build --release --workspace` (no features) exits 0.
    - `cargo build --release -p chia-sdk-driver -F chip-0057` exits 0; `cargo build --release -p chia-sdk-bindings -F napi` / `-F wasm` / `-F pyo3` each exit 0.
    - `cargo clippy --workspace --all-features --all-targets` is warning-free.
    - `cargo clippy -p chia-sdk-driver --features chip-0057 --all-targets -- -D warnings` AND the no-features variant both exit 0.
    - Full Rust CI test line exits 0.
    - `cargo machete` exits 0 with zero new `[package.metadata.cargo-machete] ignored` entries.
  </acceptance_criteria>
  <done>The full CI lint + build + test matrix is green across chip-0057 ON and OFF, per-crate; machete clean; fmt clean. AC5 satisfied.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: AC3 — Sage zero-edit recompile harness</name>
  <files>(external) /tmp/sage-ac3 — cloned Sage repo, not part of this workspace</files>
  <what-built>
    Plans 01 + 02 restored the pre-4.2 `Action::send(id, Bytes32, amount, memos)` contract (+ `SendAction { puzzle_hash } + Copy`) and made SP send a separate `chip-0057`-gated action, with the `SendDestination` `impl Into` removed. This is the exact contract Sage built clean against before Phase 4.2. This checkpoint verifies — against the live external Sage repo — that the source break the phase exists to fix is actually gone.
  </what-built>
  <action>
    Drive the harness (the steps are automatable; only the PASS/FAIL judgment — neutralizing the upstream confound — requires human confirmation):
    1. `git clone https://github.com/xch-dev/sage /tmp/sage-ac3 && cd /tmp/sage-ac3 && git checkout main`
    2. Add to `/tmp/sage-ac3/Cargo.toml` (root): `[patch.crates-io]` then `chia-wallet-sdk = { path = "/home/kdc/chia-wallet-sdk" }`.
    3. `cargo check -p sage-wallet -p sage -p sage-rpc -p sage-api` (Sage features rustls/offer-compression/peer-simulator, NO chip-0057).
    4. Capture the full error output and present it for the human PASS/FAIL judgment below.
  </action>
  <how-to-verify>
    Inspect the `cargo check` output:
    - The five Phase-4.2-caused breaks MUST be GONE: E0283 ×4 at `sage-wallet/src/wallet/offer/make_offer.rs:92,101,110,119` (the `.into()` ambiguity) and E0560 ×1 at `sage/src/endpoints/action_system.rs:64` (`SendAction { puzzle_hash }` literal).
    - NEUTRALIZE the upstream confound: if the ONLY remaining error is E0063 missing `FeeAction.reserved` at `sage/src/endpoints/action_system.rs:138` (upstream PR #349, NOT this phase), AC3 PASSES.
    - If any OTHER error appears, or any of the five SP breaks remain, AC3 FAILS — report the exact error(s) so the planner can route a gap-closure fix.

    Expected PASS state: zero SP-caused errors; at most the single upstream `FeeAction.reserved` E0063 remains.
  </how-to-verify>
  <verify>
    <automated>MANUAL — AC3 requires the external xch-dev/sage repo (not vendored here). The check is `cargo check -p sage-wallet -p sage -p sage-rpc -p sage-api` against the `[patch.crates-io]` working-tree pin; PASS = zero SP-caused errors (the upstream FeeAction.reserved E0063 neutralized). Documented manual verification per VALIDATION.md Manual-Only Verifications.</automated>
  </verify>
  <resume-signal>Type "approved" if Sage compiles with zero SP-caused errors (modulo the FeeAction.reserved confound), or paste the remaining error output if any SP break survives.</resume-signal>
  <done>Sage `cargo check -p sage-wallet -p sage -p sage-rpc -p sage-api` against the `[patch]` pin produces zero SP-caused errors (the five Phase-4.2 breaks gone; only the upstream FeeAction.reserved E0063 may remain). AC3 confirmed by the user.</done>
</task>

</tasks>

<verification>
- AC3 (Sage zero-edit): the five Phase-4.2 breaks (E0283 ×4 + E0560 ×1) are gone; only the upstream `FeeAction.reserved` E0063 may remain (neutralized).
- AC5 (lints): fmt + clippy (workspace all-features + per-crate chip-on/off -D warnings) + machete all clean; full Rust CI test line green.
- ACTION-API-01 + SEND-04 reframed to describe the dedicated `chip-0057`-gated `Action::silent_payment_send` + concrete `Action::send`.
</verification>

<success_criteria>
- Sage `cargo check -p sage-wallet -p sage -p sage-rpc -p sage-api` against `[patch]` produces zero SP-caused errors (FeeAction.reserved neutralized).
- Full CI lint/build/test matrix green across all chip-0057 permutations; machete clean; fmt clean.
- REQUIREMENTS.md ACTION-API-01 + SEND-04 reframed; `SendDestination` framing marked reverted.
</success_criteria>

<output>
After completion, create `.planning/phases/09.3-non-breaking-sp-send-restore-action-send-contract-make-sp-send-additive-revert-action-api-01-unification/09.3-03-SUMMARY.md`
</output>
