---
phase: 09.3
slug: non-breaking-sp-send-restore-action-send-contract-make-sp-send-additive-revert-action-api-01-unification
status: draft
nyquist_compliant: false
wave_0_complete: false
created: 2026-06-03
---

# Phase 09.3 — Validation Strategy

> Per-phase validation contract for feedback sampling during execution.
> Derived from 09.3-RESEARCH.md § Validation Architecture.

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | Rust `cargo test` + `rstest 0.22`; napi/wasm AVA 7.0.0; pyo3 pytest |
| **Config file** | `Cargo.toml` (workspace); `napi/package.json`, `wasm/package.json`; `pyo3/tests/` |
| **Quick run command** | `cargo test --release -p chia-sdk-driver --all-features -- silent_payment` |
| **Full suite command** | `cargo test --release --workspace --all-features --exclude chia-wallet-sdk-napi --exclude chia-sdk-derive --exclude chia-wallet-sdk-py --exclude chia-wallet-sdk-wasm --exclude chia-sdk-bindings --exclude bindy --exclude bindy-macro` |
| **Estimated runtime** | ~60–120s (Rust); binding suites extra (build + run) |

Binding suites: `cd napi && pnpm test`; `cd wasm && pnpm test`; `cd pyo3 && maturin develop && pytest`.

---

## Sampling Rate

- **After every task commit:** `cargo test --release -p chia-sdk-driver --all-features -- silent_payment` + scoped `cargo clippy -p chia-sdk-driver --all-features` (and the **chip-0057-off** permutation when touching cfg gates).
- **After every plan wave:** full workspace test line + 4-target binding builds (Wave 2+).
- **Before `/gsd:verify-work`:** full CI matrix green + **Sage zero-edit recompile** + **4-language SP E2E**.
- **Max feedback latency:** ~120 seconds (Rust quick run).

---

## Per-Task Verification Map

> Task IDs assigned by the planner; criterion→test mapping below is the contract each task must satisfy.

| Criterion | Behavior | Test Type | Automated Command / Oracle | File Exists | Status |
|-----------|----------|-----------|----------------------------|-------------|--------|
| AC1 (no E0283) | `Action::send(id, x.into(), …)` compiles | compile/unit | 16 in-repo `Action::send` callers + restored `test_action_send_*` (send.rs:126,158,199,253,290,338) | ✅ | ⬜ pending |
| AC2 (no E0560) | `SendAction{puzzle_hash}` + `Copy` restored | compile/unit | struct-literal users + send.rs unit tests | ✅ | ⬜ pending |
| AC3 (Sage zero-edit) | Sage 4 crates compile against `[patch]` | external compile | clone `xch-dev/sage` main + `[patch.crates-io]` + `cargo check -p sage-wallet -p sage -p sage-rpc -p sage-api` (neutralize `FeeAction.reserved`) | ❌ Wave 3 harness | ⬜ pending |
| AC4 (4-lang SP E2E) | send→scan→detect | integration/e2e | `tests/silent_payments_e2e.rs` (3) + napi/pyo3/wasm SP E2E specs | ✅ (need API migration) | ⬜ pending |
| AC4 (oracle byte-pin) | one-time PH unchanged | unit | `round_trip_matches_derive_one_time_puzzle_hash`, `input_hash_round_trip` | ✅ (move with file) | ⬜ pending |
| AC5 (lints) | fmt/clippy/machete all permutations | gate | `cargo fmt --all --check`; `cargo clippy --workspace --all-features --all-targets`; per-crate `-F chip-0057` + no-features; `cargo machete` | ✅ | ⬜ pending |

*Status: ⬜ pending · ✅ green · ❌ red · ⚠️ flaky*

---

## Wave 0 Requirements

- [x] No new test *infrastructure* needed — all oracles exist. This phase **migrates** existing tests' construction shape (`Action.send(SendDestination…)` → `Action.silentPaymentSend(…)`); it does not add test files.
- [ ] **Sage recompile harness** (Wave 3, manual/scripted, not a unit test): clone `xch-dev/sage` `main`, apply `[patch.crates-io] chia-wallet-sdk = { path = ... }`, `cargo check`. Neutralize the upstream `FeeAction.reserved` (#349) confound when judging zero-edit. No in-repo fixture.

---

## Manual-Only Verifications

| Behavior | Criterion | Why Manual | Test Instructions |
|----------|-----------|------------|-------------------|
| Sage compiles unchanged | AC3 | Requires an external repo (Sage) not vendored in this workspace | Clone xch-dev/sage main; add `[patch.crates-io] chia-wallet-sdk = { path = "/home/kdc/chia-wallet-sdk" }`; `cargo check -p sage-wallet -p sage -p sage-rpc -p sage-api`; expect 0 SP-caused errors (ignore `FeeAction.reserved`). |

---

## Validation Sign-Off

- [ ] All tasks have automated verify or Wave 0 dependency (AC3 is documented manual)
- [ ] Sampling continuity: no 3 consecutive tasks without automated verify
- [ ] Wave 0 covers all MISSING references (Sage harness only)
- [ ] No watch-mode flags
- [ ] Feedback latency < 120s
- [ ] `nyquist_compliant: true` set in frontmatter (flips on VERIFICATION passed)

**Approval:** pending
