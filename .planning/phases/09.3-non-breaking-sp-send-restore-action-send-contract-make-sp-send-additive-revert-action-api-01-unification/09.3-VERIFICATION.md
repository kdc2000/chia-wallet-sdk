---
status: passed
phase: 09.3-non-breaking-sp-send-restore-action-send-contract-make-sp-send-additive-revert-action-api-01-unification
verified: 2026-06-03
plans: [09.3-01, 09.3-02, 09.3-03]
requirements: [ACTION-API-01, SEND-04]
---

# Phase 09.3 Verification — Non-breaking SP send (Option A)

**Status: passed.** The phase goal — *existing SDK consumers compile UNCHANGED (chip-0057 on or off); SP send is a distinct chip-0057-gated action, not a `SendDestination` folded into `Action::send`* — is achieved and **empirically proven against the real downstream consumer (Sage)**.

## must_haves (goal-backward)

| # | Must-have | Verdict | Evidence |
|---|-----------|---------|----------|
| AC1 | `Action::send(id, ph, …)` and `Action::send(id, x.into(), …)` compile unchanged (no E0283) | ✓ PASS | Sage `make_offer.rs` E0283 ×4 GONE; `Action::send` is concrete `pub fn send(id: Id, puzzle_hash: Bytes32, amount: u64, memos: Memos)` (action.rs:40) |
| AC2 | `SendAction.puzzle_hash` field + `Copy` restored (no E0560) | ✓ PASS | `SendAction { id, puzzle_hash: Bytes32, amount, memos }` + `#[derive(Debug, Clone, Copy)]` (send.rs:9); Sage `action_system.rs` E0560 GONE |
| AC3 | Sage `main` compiles against this branch with ZERO source edits | ✓ PASS | `cargo check -p sage-wallet -p sage -p sage-rpc -p sage-api` over `[patch.crates-io]`: E0283=0, E0560=0; only the upstream `FeeAction.reserved` (#349) E0063 remains (neutralized per plan) |
| AC4 | 4-language SP E2E green + one-time-PH oracle byte-identical | ✓ PASS | Rust: 2343 driver tests + 4 e2e (incl. `test_simulator_e2e_labeled/_multi_input`); napi/pyo3/wasm SP E2E green (Wave 2); oracle pins `round_trip_matches_derive_one_time_puzzle_hash` + `input_hash_round_trip` green |
| AC5 | fmt/clippy/machete clean across all chip-0057 permutations | ✓ PASS | full matrix: build chip-on/off + per-crate, `clippy --workspace --all-features`, driver `-D warnings` chip-on/off, full CI test line, `cargo machete` — all exit 0; `cargo fmt --check` clean |

## Requirements

- **ACTION-API-01** — satisfied (reframed): SP send is `Action::SilentPaymentSend` via `Action::silent_payment_send`; `Action::send` concrete `Bytes32`. REQUIREMENTS.md body + traceability updated.
- **SEND-04** — satisfied (reframed): apply+finish closure semantics unchanged; production caller is `Action::silent_payment_send(recipient, amount, memos)`.

## Preserved invariants

- GUARD-01/02/03 (Phase 9.2) untouched — `with_silent_payment_keys` newtypes, `sp_finish_branch` runtime guard, raw-key bindings all intact.
- `Spends.prepare(deltas, Option<Relation>)` (BRIDGE-04) retained.
- `SendDestination` deleted from every surface (Rust driver, prelude, bindings facade/JSON, napi `index.d.ts`, wasm `.d.ts`, pyo3 `.pyi`) — 0 references.

## Out-of-scope items (not regressions of this phase)

- Upstream `FeeAction.reserved` (PR #349) — the fork tracks unreleased upstream; surfaces in Sage independent of SP. Distinct PR-prep concern (isolate/rebase SP commits onto current upstream).
- Two pre-existing binding-test failures (GUARD-03 negative-test regex; multi-input `tweak_data_from_block_spends` grouping) — baseline-proven (commit `cfaaf275`) to predate Phase 09.3; logged in deferred-items.md.
