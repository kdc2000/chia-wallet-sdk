---
phase: 09.3-non-breaking-sp-send (milestone v1.0 complete — PR-prep)
task: PR-prep
total_tasks: n/a
status: paused
last_updated: 2026-06-04T21:22:08.657Z
---

<current_state>
Milestone v1.0 (CHIP-0057 silent payments) is **code-complete, verified, and audited**. All 13 phases are done, including the final Phase 09.3 (which reverted the breaking `Action::send` unification — SP send is now a separate `chip-0057`-gated `Action::silent_payment_send`).

We are now in **post-milestone PR preparation**, not a phase task. The deliverable is the branch **`chip-0057-silent-payments`**: a clean, scrubbed, 5-commit series rebased onto the real upstream (`xch-dev/chia-wallet-sdk` `main`), already **force-pushed to the user's fork** (`origin` = `kdc2000/chia-wallet-sdk`, tip `6e14ad49`). **No PR has been opened.** `main` is clean.
</current_state>

<completed_work>

- Milestone v1.0 fully built + audited (52/52 requirements; `.planning/v1.0-MILESTONE-AUDIT.md`).
- Phase 09.3: non-breaking SP send — `Action::send` restored to concrete `Bytes32`, SP send is a gated `Action::silent_payment_send`. **Sage compiles zero-edit** (verified).
- Quick task 260603-ilc: cleared the 2 deferred binding-test failures (commit `133cc80b`).
- Built code-only branch `chip-0057-silent-payments` (dropped 167 `.planning` commits + `CLAUDE.md` + `ISSUE-*` residue).
- Rebased onto `xch-dev/main` (zero conflicts). Validated under **Rust 1.95**: build (chip on/off), clippy `-D warnings`, full Rust tests, napi(54)/wasm(10)/pyo3(4), and a Sage zero-edit recompile.
- Squashed into a clean **5-commit layered series** (crypto → addresses → driver → bindings → integration).
- Scrubbed 92 GSD nomenclature refs (`BIND-03`, `GUARD-01`, `Phase 09.2`, etc.) from comments/test-names; re-verified all suites green. Force-pushed to fork.
</completed_work>

<remaining_work>

- **Open the PR** against `xch-dev/chia-wallet-sdk` — the ONLY step that touches upstream. Drafted title/body exist (in the prior chat). Before opening, add a short **"CHIP-0057 status / what consumes TweakData today"** paragraph to pre-empt the maintainer's likely timing question.
- Delete local backup branches (`…-137-backup`, `…-scrubbed-backup`) once satisfied.
</remaining_work>

<decisions_made>

- **Upstream = `xch-dev/chia-wallet-sdk`** (= Rigidity, same org as Sage), NOT `Chia-Network/chia-wallet-sdk` (stale/diverged, 102 commits of fork noise).
- **SP send kept as a separate gated action**, leaving `Action::send` concrete — because the Phase-4.2 `impl Into<SendDestination>` unification source-broke Sage.
- **Squash to 5 layered commits** to hide internal add-then-revert churn (`SendDestination`).
- **Scrubbed internal nomenclature** so the PR reads clean to an external maintainer.
- **Remotes `upstream`/`xchdev` set to push=`no-push`**; never push upstream or open a PR without an explicit user "go".
</decisions_made>

<blockers>
- None technical. Opening the PR is a deliberate user decision (outward-facing to upstream).
</blockers>

<context>
The branch is finished and pushable: 5 commits on `xch-dev/main`, 67 files, +8,314/−33, zero residue at file *and* comment level, authored `Kevin Chmilar`, all green under Rust 1.95. The fork branch matches local.

Two consumer-upgrade notes for the eventual PR/Sage bump — both **non-SP**, and deliberately **left out of the PR body** (they'd confuse a maintainer): `cargo update -p time` (from upstream's `chia-ssl 0.42.1`), and `FeeAction.reserved` (a Sage-vs-upstream-baseline lag, not introduced by this work).

Likely maintainer reactions to prepare for: size (offer stacked PRs — already in draft body), CHIP-0057 maturity, and "receive primitive without a transport client yet" timing.
</context>

<next_action>
If resuming PR work: add the CHIP-0057-status paragraph to the drafted PR body, then — only on an explicit user go — `gh pr create --repo xch-dev/chia-wallet-sdk --base main --head kdc2000:chip-0057-silent-payments`. Otherwise nothing is pending and `main` is clean; this is a natural stopping point.
</next_action>
