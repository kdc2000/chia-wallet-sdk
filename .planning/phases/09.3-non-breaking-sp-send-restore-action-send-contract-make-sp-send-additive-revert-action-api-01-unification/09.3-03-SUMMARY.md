---
phase: 09.3-non-breaking-sp-send-restore-action-send-contract-make-sp-send-additive-revert-action-api-01-unification
plan: 03
subsystem: verification
tags: [chip-0057, silent-payments, action-api-01, send-04, ci-matrix, sage, ac3, checkpoint]

# Dependency graph
requires:
  - phase: 09.3-01
    provides: "Restored concrete Action::send contract + gated Action::silent_payment_send (the Rust core the Sage recompile validates)"
  - phase: 09.3-02
    provides: "Bindings reverted to match + SendDestination purged from all FFI surfaces"
provides:
  - "REQUIREMENTS.md ACTION-API-01 + SEND-04 reframed: SP send is a distinct chip-0057-gated Action::silent_payment_send; Action::send is concrete Bytes32 (non-breaking); SendDestination framing marked reverted"
  - "Full CI lint/build/test matrix green across chip-0057 ON and OFF (per-crate), fmt clean, machete clean"
  - "AC3 empirical proof: Sage main compiles against this branch with ZERO SP-caused errors (only the upstream FeeAction.reserved #349 confound remains)"
affects: []

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "External-consumer compat gate: clone xch-dev/sage + [patch.crates-io] path-pin + cargo check, neutralizing the upstream FeeAction.reserved drift — the empirical proof the source-break is gone"

key-files:
  created: []
  modified:
    - ".planning/REQUIREMENTS.md - ACTION-API-01 + SEND-04 bodies + Traceability rows + footer reframed (SP send = distinct gated action; Action::send concrete non-breaking)"
    - "crates/chia-sdk-driver/src/action_system/action.rs - cargo fmt (import order)"
    - "crates/chia-sdk-bindings/src/action_system.rs - cargo fmt (send signature line-wrap)"
    - "crates/chia-sdk-driver/src/silent_payments/send_keys.rs - cargo fmt (call line-wrap)"

key-decisions:
  - "AC3 judged PASS (user-confirmed checkpoint): the 5 Phase-4.2-caused breaks (E0283 x4 at make_offer.rs .into() sites + E0560 at action_system.rs SendAction{puzzle_hash} literal) are ALL gone. The single remaining error — E0063 missing FeeAction.reserved at sage/endpoints/action_system.rs:138 — is upstream PR #349 drift (the fork tracks unreleased upstream), explicitly out of scope per the plan's neutralization rule."

requirements-completed: [ACTION-API-01, SEND-04]

# Metrics
---

# Phase 09.3: Non-breaking SP send (Plan 03 — Verify + reframe) Summary

**Reframed ACTION-API-01 + SEND-04 to describe SP send as a distinct `chip-0057`-gated `Action::silent_payment_send` action (concrete non-breaking `Action::send`), ran the full CI lint/build/test matrix green across both `chip-0057` permutations, and empirically proved AC3 — Sage `main` compiles against this branch with ZERO SP-caused errors (only the upstream `FeeAction.reserved` confound remains).**

## Accomplishments

- **Task 1 (REQUIREMENTS.md reframe):** ACTION-API-01 + SEND-04 bodies, Traceability rows, and footer now describe SP send as a distinct flag-gated `Action::silent_payment_send` with a concrete, non-breaking `Action::send`; the `SendDestination` unification is marked reverted. `Action::silent_payment_send` referenced 5×; `SilentPaymentRequiresXch` appears only in removed-context.
- **Task 2 (CI matrix, AC5):** `cargo fmt --check` (after a cosmetic fmt fix to 3 phase-touched files), `cargo build --release --all-features`, `--workspace` (no features), per-crate `-F chip-0057` (driver) + `-F napi/-F wasm/-F pyo3` (bindings), `cargo clippy --workspace --all-features --all-targets`, driver clippy `-D warnings` chip-on AND chip-off, the full CI test line, and `cargo machete` — all exit 0. (The only clippy warnings are the pre-existing `chia-sdk-daemon` match nits that predate this milestone.)
- **Task 3 (AC3, checkpoint — user-approved PASS):** cloned `xch-dev/sage` `main`, `[patch.crates-io] chia-wallet-sdk = { path }`, `cargo check -p sage-wallet -p sage -p sage-rpc -p sage-api` → **E0283: 0, E0560: 0** (all 5 SP breaks gone), only **E0063 FeeAction.reserved ×1** (upstream #349, neutralized).

## Verification evidence

| AC | Gate | Result |
|----|------|--------|
| AC1 | `Action::send(id, x.into(), …)` compiles | ✓ (Sage make_offer.rs E0283 ×4 gone; driver builds) |
| AC2 | `SendAction.puzzle_hash` + `Copy` | ✓ (Sage action_system.rs E0560 gone) |
| AC3 | Sage zero-edit compile | ✓ (only upstream FeeAction.reserved remains) |
| AC4 | 4-lang SP E2E + oracle byte-pin | ✓ (Rust 2343 + 4 e2e; napi/pyo3/wasm SP E2E green — Wave 1/2) |
| AC5 | fmt/clippy/machete all permutations | ✓ (matrix green) |

## Notes

Executed inline by the orchestrator (the Wave-1 subagent hit a session limit; Waves 2–3 closeout completed without re-spawning to avoid further limits). Two pre-existing binding-test failures (GUARD-03 negative-test regex; multi-input `tweak_data_from_block_spends` grouping) were baseline-proven by Wave 2 to predate Phase 09.3 and are logged in deferred-items.md — out of scope here.
