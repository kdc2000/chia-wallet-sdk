---
phase: 09.3-non-breaking-sp-send-restore-action-send-contract-make-sp-send-additive-revert-action-api-01-unification
plan: 02
subsystem: bindings
tags: [chip-0057, silent-payments, action-api-01, send-04, bindings, napi, pyo3, wasm, revert, non-breaking]

# Dependency graph
requires:
  - phase: 09.3-01 (plan)
    provides: "sdk::Action::silent_payment_send(recipient, amount, memos) + restored concrete Action::send(id, puzzle_hash: Bytes32, amount, memos); SendDestination deleted from chia-sdk-driver"
provides:
  - "Binding Action.send takes a raw puzzle hash again (Bytes32 -> Uint8Array/bytes per type-group); no SendDestination wrapper across napi/pyo3/wasm"
  - "Additive Action.silentPaymentSend(recipient, amount, memos) facade + bindings/action_system.json descriptor delegating to sdk::Action::silent_payment_send"
  - "SendDestination opaque-handle class purged from facade, lib.rs re-export, JSON descriptor, and ALL regenerated binding surfaces (napi index.d.ts/index.js, wasm pkg .d.ts, pyo3 .pyi)"
  - "All 7 binding test files migrated: SP sites -> Action.silentPaymentSend/silent_payment_send; 10 non-SP SendDestination.puzzleHash wraps in napi action_system.spec.ts reverted to raw puzzle hash (TS mirror of the Sage E0283/E0560 break closed)"
affects:
  - "09.3-03 (verify) gates on this being the binding-side basis for the zero-edit recompile; consumes the regenerated surfaces"

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "Generated-artifact regeneration: napi index.d.ts/index.js via `napi build`, pyo3 .pyi via `cargo run -p pyo3-stub-generator` (the .pyi is NOT regenerated by plain `maturin develop` — it is a checked-in stub written from bindings.json by the stub-generator binary), wasm .d.ts via `wasm-pack build` (gitignored, not committed)"
    - "Additive gated-action binding mirrors the Rust core: Action.silentPaymentSend is a separate factory rather than a destination folded into send — consumer-invisible, no break to existing Action.send callers in any of the three target languages"

key-files:
  created: []
  modified:
    - "crates/chia-sdk-bindings/src/action_system.rs - Action::send reverted to puzzle_hash: Bytes32; added Action::silent_payment_send facade; deleted SendDestination struct+impl; updated stale Relation doc reference"
    - "crates/chia-sdk-bindings/src/lib.rs - deleted DriverSendDestination re-export + its doc block"
    - "bindings/action_system.json - send args destination:SendDestination -> puzzle_hash:Bytes32; added silent_payment_send factory; removed SendDestination class block"
    - "napi/index.d.ts, napi/index.js - regenerated (SendDestination gone, silentPaymentSend added, send takes Uint8Array)"
    - "pyo3/chia_wallet_sdk.pyi - regenerated via pyo3-stub-generator (SendDestination gone, silent_payment_send added, send takes bytes)"
    - "napi/__test__/silent_payments_e2e.spec.ts - 2 SP sends migrated; Id+SendDestination imports dropped"
    - "napi/__test__/silent_payments_multi_input.spec.ts - SP send migrated; Id+SendDestination imports dropped"
    - "napi/__test__/silent_payments.spec.ts - dead SC3 SendDestination smoke deleted; Action.silentPaymentSend smoke added; Id+SendDestination imports dropped"
    - "napi/__test__/action_system.spec.ts - 10 non-SP SendDestination.puzzleHash(x) wraps reverted to raw x; SendDestination import dropped"
    - "pyo3/tests/test_silent_payments.py - 3 SP sends migrated to Action.silent_payment_send; Id+SendDestination imports dropped"
    - "wasm/__test__/silent_payments.spec.ts - SP send migrated; Id+SendDestination imports dropped"
    - "wasm/__test__/silent_payments_multi_input.spec.ts - SP send migrated; Id+SendDestination imports dropped"
    - ".planning/.../deferred-items.md - 2 pre-existing binding-test failures logged (GUARD-03 regex; multi-input grouping)"

key-decisions:
  - "Skipped `requirements mark-complete` for ACTION-API-01/SEND-04 — both already `[x]` in REQUIREMENTS.md from Phase 4.2; the prose reframe (SP as a distinct flag-gated action vs a SendDestination folded into send) is explicitly plan 09.3-03's job per the phase plan."
  - "Updated the Relation facade doc comment's stale `SendDestination::SilentPayment` reference to `Action::silent_payment_send` (Rule 1 - stale doc referencing a deleted type) — the Relation facade itself (BRIDGE-04) was NOT otherwise touched."
  - "Two binding-test failures (GUARD-03 error-message regex + multi-input tweak_data_from_block_spends 3-vs-1 grouping) are PRE-EXISTING — proven by building the pre-09.3 baseline commit cfaaf275 in a throwaway worktree and reproducing both failures identically. Logged to deferred-items.md, NOT fixed (SCOPE BOUNDARY)."

requirements-completed: []  # ACTION-API-01 + SEND-04 already [x] from Phase 4.2; prose reframe is plan 09.3-03

# Metrics
duration: 36
tasks: 2
files: 14
completed: 2026-06-03
---

# Phase 09.3 Plan 02: Bindings — non-breaking SP send Summary

**Reverted the binding `Action.send` to take a raw puzzle hash, added an additive `Action.silentPaymentSend(recipient, amount, memos)` facade + descriptor delegating to `sdk::Action::silent_payment_send`, and purged the `SendDestination` opaque-handle class from the facade, `lib.rs` re-export, JSON descriptor, and every regenerated binding surface — closing the binding-level analog of the Sage E0283/E0560 break by reverting the 10 non-SP `SendDestination.puzzleHash(...)` wraps in `action_system.spec.ts`.**

## Accomplishments

- **Task 1 (Rust facade + JSON):** `Action::send` takes `puzzle_hash: Bytes32` again (no `SendDestination`); new `Action::silent_payment_send` facade converts `crate::SilentPaymentAddress -> chia_sdk_utils::silent_payments::SilentPaymentAddress` and delegates to `sdk::Action::silent_payment_send`. `SendDestination` struct+impl deleted from the facade; `DriverSendDestination` re-export deleted from `lib.rs`. `bindings/action_system.json`: `send` arg `destination:SendDestination` -> `puzzle_hash:Bytes32`, added `silent_payment_send` factory, removed the `SendDestination` class block. GUARD-03 `with_silent_payment_keys` (count unchanged at 2) and BRIDGE-04 `Spends.prepare` untouched. `cargo build -p chia-sdk-bindings --all-features` + `clippy -D warnings` clean; JSON valid.
- **Task 2 (rebuild + migrate):** Rebuilt all three targets (`napi build`, `wasm-pack build` via `pnpm test`, `maturin develop` + `cargo run -p pyo3-stub-generator` to regenerate the checked-in `.pyi`). Migrated 7 test files: SP sends -> `Action.silentPaymentSend` / `Action.silent_payment_send`; deleted the dead SC3 `SendDestination` smoke and added a `silentPaymentSend` smoke; reverted the 10 non-SP `SendDestination.puzzleHash(x)` wraps in `napi/__test__/action_system.spec.ts` to raw `x`; dropped now-unused `Id`/`SendDestination` imports.

## Verification

- `grep` checks (Task 1): SendDestination = 0 in action_system.rs / lib.rs / action_system.json; `pub fn silent_payment_send` + `sdk::Action::silent_payment_send` present; `"puzzle_hash": "Bytes32"` + `"silent_payment_send"` in JSON; `with_silent_payment_keys` count = 2 (GUARD-03 intact).
- `cargo build -p chia-sdk-bindings --all-features` -> exit 0; `cargo clippy -p chia-sdk-bindings --all-features -- -D warnings` -> exit 0.
- SendDestination purged everywhere (0): bindings src, JSON, napi __test__/index.d.ts/index.js, wasm __test__/pkg .d.ts, pyo3 tests/.pyi.
- Regenerated `napi/index.d.ts`: `static send(id, puzzleHash: Uint8Array, ...)` + `static silentPaymentSend(recipient: SilentPaymentAddress, ...)`; `SendDestination` class removed. `pyo3 .pyi`: `def send(id, puzzleHash: bytes, ...)` + `def silent_payment_send(recipient: SilentPaymentAddress, ...)`. `wasm pkg .d.ts`: `silentPaymentSend` present, `SendDestination` = 0.
- **SP-send E2E (the detection oracle) green in all three targets:** napi `silent_payments_e2e › BIND-03` PASS, wasm `silent_payments › BIND-03 wasm` PASS, pyo3 `test_unlabeled_e2e` PASS. SC2 address round-trips + SC3 `silentPaymentSend` smoke pass.
- **Binding-level analog of AC3 (zero-edit):** `napi/__test__/action_system.spec.ts` passes with `Action.send(id, puzzleHash, amount)` (raw puzzle hash) — non-SP callers no longer need a `SendDestination` wrapper.
- Rust cross-check: `cargo test -p chia-sdk-driver --all-features test_simulator_e2e_multi_input` PASS (Rust multi-input SP send + scan works via `tweak_data_from_simulator_block`).

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 1 - Bug] Stale Relation facade doc referencing deleted SendDestination**
- **Found during:** Task 1
- **Issue:** the `Relation` facade doc comment in `action_system.rs` referenced `SendDestination::SilentPayment`, a now-deleted type.
- **Fix:** reworded to reference `Action::silent_payment_send`; the `Relation` facade code (BRIDGE-04) was not otherwise touched.
- **Files modified:** crates/chia-sdk-bindings/src/action_system.rs
- **Commit:** 84ca133e

**2. [Rule 3 - Blocking] pyo3 .pyi stub not regenerated by `maturin develop`**
- **Found during:** Task 2
- **Issue:** after `maturin develop` the compiled module exposed `Action.silent_payment_send` at runtime, but the checked-in `pyo3/chia_wallet_sdk.pyi` still declared `SendDestination` and lacked `silent_payment_send`. The `.pyi` is a checked-in stub written from `bindings.json` by the `pyo3-stub-generator` binary, not regenerated by `maturin develop`.
- **Fix:** ran `cargo run -p pyo3-stub-generator` (writes `pyo3/chia_wallet_sdk.pyi`). Confirmed SendDestination=0, silent_payment_send=1, `send` takes `bytes`.
- **Files modified:** pyo3/chia_wallet_sdk.pyi
- **Commit:** 87144ec7

## Deferred Issues (pre-existing, out of scope)

Two binding-test failures were discovered. Both were proven PRE-EXISTING by building the pre-09.3 baseline commit `cfaaf275` in a throwaway worktree and reproducing them identically. Logged to `deferred-items.md`; NOT fixed (SCOPE BOUNDARY — only auto-fix issues directly caused by this task's changes). The migration preserved the exact same runtime behavior — the core single-input SP-send E2E passes in all three targets.

1. **GUARD-03 negative-test error-regex mismatch** (napi/wasm/pyo3): the matcher expects `/not the synthetic key|KeyNotSynthetic/i` but the driver renders `"silent payment key not synthetic"` (`DriverError::SilentPaymentKeyNotSynthetic`). Test-side regex written against a never-shipped string (Phase 09.2-03). One-line fix per target when picked up.
2. **Multi-input `tweak_data_from_block_spends` grouping** (napi/wasm/pyo3): yields 3 tweak_points, test asserts 1. The failing path is the Phase-09-06 BRIDGE-05 block-spends grouping helper, NOT the SP-send action — the Rust-level `test_simulator_e2e_multi_input` (using `tweak_data_from_simulator_block`) PASSES, so the SP send + `AssertConcurrent` cycle binding + scan all work. Needs investigation in the block-spends tweak-data helper.

## Notes

- The plan named "7 binding test files" in the objective but listed/modified 7 sources across napi (4) + pyo3 (1) + wasm (2); all migrated. Generated artifacts (napi index.d.ts/index.js, pyo3 .pyi) committed; wasm pkg is gitignored.
- No new dependencies; no `#[cfg]` gates inside the facade (chip-0057 unconditional on chia-sdk-bindings per D-01); zero new `#[allow]` attributes.

## Self-Check: PASSED
