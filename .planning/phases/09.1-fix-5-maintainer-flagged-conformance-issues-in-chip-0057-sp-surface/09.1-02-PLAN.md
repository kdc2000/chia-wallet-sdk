---
phase: 09.1-fix-5-maintainer-flagged-conformance-issues-in-chip-0057-sp-surface
plan: 02
type: execute
wave: 2
depends_on: ["01"]
files_modified:
  - crates/chia-sdk-driver/src/silent_payments/protocol.rs
  - crates/chia-sdk-bindings/src/silent_payments.rs
autonomous: true
requirements: [ISSUE-1]

must_haves:
  truths:
    - "An empty `coin_ids` list passed to the binding facade's `compute_input_hash` returns an `Err` (a typed DriverError) instead of panicking across the FFI boundary."
    - "The `# Panics` rustdoc on `compute_input_hash` in protocol.rs is updated/removed to match the new no-panic-at-the-boundary contract."
    - "A test proves empty input yields `Err` (not a panic) at the binding boundary."
    - "After ISSUE-1 is verified, the full global regression bar (workspace test line, fmt, machete, all-features clippy) is green with zero new workspace deps."
  artifacts:
    - path: "crates/chia-sdk-bindings/src/silent_payments.rs"
      provides: "Empty-input guard on the FFI-reachable compute_input_hash facade returning Err"
      contains: "is_empty"
    - path: "crates/chia-sdk-driver/src/silent_payments/protocol.rs"
      provides: "compute_input_hash with rustdoc matching the no-FFI-panic contract"
      contains: "compute_input_hash"
  key_links:
    - from: "crates/chia-sdk-bindings/src/silent_payments.rs:425 (compute_input_hash facade)"
      to: "DriverError::SilentPaymentNoXchInputs"
      via: "is_empty() guard returning Err before calling the driver fn"
      pattern: "coin_ids.is_empty"
    - from: "binding facade compute_input_hash"
      to: "chia_sdk_driver::compute_input_hash"
      via: "guarded slice pass-through (driver fn still receives only non-empty slices)"
      pattern: "chia_sdk_driver::compute_input_hash"
---

<objective>
Eliminate the FFI panic on empty input (ISSUE-1): `compute_input_hash` at `crates/chia-sdk-driver/src/silent_payments/protocol.rs:183` `assert!`s on an empty `coin_ids` slice, and that slice flows straight through the `Result`-returning binding facade at `crates/chia-sdk-bindings/src/silent_payments.rs:425` unguarded — so an empty list from Python/JS/WASM panics across the FFI boundary instead of returning a typed error. This violates the repo's "no panicking in library code."

Then, with ISSUE-1's acceptance verified first, run the global regression bar (the full CI test line, fmt, machete, all-features clippy) to confirm the combined 09.1 changeset (this plan + Plan 01) is upstream-merge clean.

Purpose: make the FFI boundary return a typed `Err` for empty input rather than aborting the host process; gate the whole phase on the regression bar.
Output: a guarded binding facade + corrected rustdoc + a boundary test proving `Err` (no panic); a green global regression run.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@CLAUDE.md

<interfaces>
<!-- The driver fn today (crates/chia-sdk-driver/src/silent_payments/protocol.rs:181-197). It asserts on empty.
     The two NON-binding production call sites — action_system/spends.rs:645 (xch_input_ids) and
     silent_payments/block_tweak_data.rs:189 (per-group coin_ids) — both derive their slices from input sets that
     upstream gates already guarantee non-empty, so they need NO change. The ONLY unguarded FFI-reachable path is the
     binding facade below. This makes "guard in the facade" (option b) strictly lower-churn than changing the driver
     fn's signature (option a), which would touch all 5 call sites + ~8 tests. -->
```rust
// protocol.rs
#[must_use]
pub fn compute_input_hash(coin_ids: &[Bytes32], aggregated_sender_pk: &PublicKey) -> ScalarField {
    assert!(!coin_ids.is_empty(), "compute_input_hash requires at least one coin id");
    let coin_id_min = coin_ids.iter().min().expect("non-empty checked above");
    // ... tagged_hash(CHIA_SP_INPUTS, coin_id_min || serialize(A_sum)) ...
}
```

<!-- The binding facade today (crates/chia-sdk-bindings/src/silent_payments.rs:425-430). Returns Result; the `.into()`
     converts ScalarField; DriverError propagates through the bindings `?`/error machinery (DriverError has a
     binding-side conversion — derive_one_time_puzzle_hash and the other SP facade methods already return
     Result<_, bindy::Error> over chia_sdk_driver errors). -->
```rust
pub fn compute_input_hash(
    coin_ids: Vec<Bytes32>,
    aggregated_sender_pk: PublicKey,
) -> Result<ScalarField> {
    Ok(chia_sdk_driver::compute_input_hash(&coin_ids, &aggregated_sender_pk).into())
}
```

<!-- The semantically-correct existing variant for the empty case (crates/chia-sdk-driver/src/driver_error.rs:145-146):
     no new variant needed. -->
```rust
#[error("silent payment requires at least one wallet-controlled XCH input")]
SilentPaymentNoXchInputs,
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Guard the FFI-reachable compute_input_hash facade against empty input + update protocol.rs # Panics rustdoc (ISSUE-1)</name>
  <files>crates/chia-sdk-bindings/src/silent_payments.rs, crates/chia-sdk-driver/src/silent_payments/protocol.rs</files>
  <read_first>
    - crates/chia-sdk-bindings/src/silent_payments.rs:425-430 — the `compute_input_hash` facade method (returns `Result<ScalarField>`).
    - The sibling facade methods around it (`derive_one_time_puzzle_hash` at :407, `aggregate_sender_sks` at :432) to confirm how `DriverError` / errors are propagated into the binding `Result` (whether via `?` on a `chia_sdk_driver` error or `bindy::Error::SilentPayment`). Match the existing error-return idiom exactly.
    - crates/chia-sdk-driver/src/silent_payments/protocol.rs:171-197 — the `# Panics` rustdoc paragraph (lines 171-173) and the `assert!(!coin_ids.is_empty(), ...)` (lines 183-186).
    - crates/chia-sdk-driver/src/driver_error.rs:135-146 — confirm `DriverError::SilentPaymentNoXchInputs` exists with message "silent payment requires at least one wallet-controlled XCH input" and that it is the cleanest fit for the empty-input case.
    - The two non-binding call sites (action_system/spends.rs:645, silent_payments/block_tweak_data.rs:189) — confirm both pass non-empty slices and therefore need NO change (option b, facade-guard, is chosen precisely because they are already safe).
  </read_first>
  <action>
    Implement option (b): keep the driver `compute_input_hash` pure-fn contract, but add an explicit emptiness guard in the binding facade so the FFI boundary returns `Err` instead of panicking.

    In `crates/chia-sdk-bindings/src/silent_payments.rs`, change the `compute_input_hash` facade (lines 425-430) to guard `coin_ids` before delegating:
    ```rust
    pub fn compute_input_hash(
        coin_ids: Vec<Bytes32>,
        aggregated_sender_pk: PublicKey,
    ) -> Result<ScalarField> {
        if coin_ids.is_empty() {
            return Err(chia_sdk_driver::DriverError::SilentPaymentNoXchInputs.into());
        }
        Ok(chia_sdk_driver::compute_input_hash(&coin_ids, &aggregated_sender_pk).into())
    }
    ```
    Use whatever the existing error-into-binding-Result idiom is in this file (the `.into()` on `DriverError` must reach the facade's `Result` error type; if the sibling methods use `?` over `chia_sdk_driver` errors or a `bindy::Error::SilentPayment` conversion, match that path so the early-return error converts the same way the happy-path `?` would). Do NOT change the driver fn's signature; do NOT touch `action_system/spends.rs` or `block_tweak_data.rs`.

    Then update the `# Panics` rustdoc on the driver `compute_input_hash` in `crates/chia-sdk-driver/src/silent_payments/protocol.rs` (lines 171-173). The `assert!` remains a private-contract guard for in-crate callers (which always pass non-empty), but the FFI boundary no longer reaches it. Rewrite the `# Panics` paragraph to state the contract precisely, e.g.:
    ```
    /// # Panics
    /// Panics if `coin_ids` is empty. This is an internal invariant: every
    /// in-crate caller (`Spends::finish_with_keys`, `tweak_data_from_block_spends`)
    /// passes a non-empty XCH-input set, and the bindings facade rejects empty
    /// input with `DriverError::SilentPaymentNoXchInputs` before reaching this
    /// function — so the panic is not reachable across the FFI boundary.
    ```
    Do NOT introduce any planning-artifact comment text (CLEANUP-01 grep ban applies to both files). Do NOT add any `#[allow]`. Do NOT add new workspace deps.
  </action>
  <verify>
    <automated>cargo build -p chia-sdk-bindings --all-features 2>&1 | tail -5 && cargo clippy -p chia-sdk-bindings --all-features --all-targets -- -D warnings 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'coin_ids.is_empty()' crates/chia-sdk-bindings/src/silent_payments.rs` returns 1 (the new guard).
    - The guard returns `DriverError::SilentPaymentNoXchInputs` (grep the facade method body shows `SilentPaymentNoXchInputs`).
    - `cargo build -p chia-sdk-bindings --all-features` exits 0.
    - `cargo clippy -p chia-sdk-bindings --all-features --all-targets -- -D warnings` exits 0.
    - The `# Panics` rustdoc at protocol.rs no longer claims an unconditional/FFI-reachable panic (grep around the `# Panics` block shows the "not reachable across the FFI boundary" / facade-rejects wording).
    - The driver `compute_input_hash` signature is UNCHANGED (`grep -c 'pub fn compute_input_hash(coin_ids: &\[Bytes32\], aggregated_sender_pk: &PublicKey) -> ScalarField' crates/chia-sdk-driver/src/silent_payments/protocol.rs` returns 1).
  </acceptance_criteria>
  <done>The FFI-reachable `compute_input_hash` facade returns `Err(DriverError::SilentPaymentNoXchInputs)` for empty input (no panic crosses the boundary); the driver fn is unchanged and its `# Panics` rustdoc accurately describes the internal-only invariant. chia-sdk-bindings builds and clippy-clean under `--all-features`.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Boundary test — empty input yields Err (not panic) + run the global regression bar</name>
  <files>crates/chia-sdk-bindings/src/silent_payments.rs</files>
  <behavior>
    - Test 1 (the load-bearing proof for ISSUE-1): calling the binding facade `SilentPayments::compute_input_hash` (or the equivalent in-crate entry the facade exposes) with an EMPTY `Vec<Bytes32>` returns `Err` — specifically a `DriverError::SilentPaymentNoXchInputs`-derived error — and does NOT panic. Assert on the `Err` (use `assert!(result.is_err())` plus, if the binding error type implements `PartialEq`/`Debug`, a tighter match on the variant; otherwise match the error display string `"silent payment requires at least one wallet-controlled XCH input"`).
    - Test 2 (regression guard for the happy path): a single-element `Vec<Bytes32>` returns `Ok(_)` (the facade still delegates correctly for non-empty input).
  </behavior>
  <read_first>
    - crates/chia-sdk-bindings/src/silent_payments.rs — confirm where a `#[cfg(test)] mod tests {}` would live (add one if absent) and how to construct a `PublicKey` + `Bytes32` for the test (mirror any existing test fixtures in the bindings crate, e.g. `PublicKey::default()` / a fixed 32-byte `Bytes32`).
    - The chosen error type's traits — to decide between `assert!(result.is_err())` + display-string match vs a `matches!`/`assert_eq!` on the variant.
    - CLAUDE.md "Build & Test Commands (as used by CI)" — the exact full workspace test line to run for the global regression bar (the multi-crate `cargo test --release --workspace --all-features --exclude ...` invocation).
  </read_first>
  <action>
    RED→GREEN: add a `#[cfg(test)] mod tests` (or extend the existing one) in `crates/chia-sdk-bindings/src/silent_payments.rs` with the two tests described in `<behavior>`. The empty-input test is the proof that ISSUE-1 is closed: it MUST exercise the same facade method the FFI bindings call (so the guard is on the path under test), assert `Err` (not a panic), and — if feasible — pin the error to `SilentPaymentNoXchInputs` by variant or by its display string `"silent payment requires at least one wallet-controlled XCH input"`. The single-element test confirms the happy path still returns `Ok`.

    Then run the GLOBAL REGRESSION BAR (this is the sequencing gate — ISSUE-1 acceptance above is verified first, then the whole-phase bar runs LAST):
      1. `cargo fmt --all -- --files-with-diff --check`
      2. `cargo clippy --workspace --all-features --all-targets` (CI-style, no `-D warnings`; must exit 0) AND the scoped strict gates already covered above.
      3. `cargo machete` (zero NEW ignored entries).
      4. The full CI workspace test line from CLAUDE.md:
         `cargo test --release --workspace --all-features --exclude chia-wallet-sdk-napi --exclude chia-sdk-derive --exclude chia-wallet-sdk-py --exclude chia-wallet-sdk-wasm --exclude chia-sdk-bindings --exclude bindy --exclude bindy-macro`
         PLUS, because the regular CI test line EXCLUDES chia-sdk-bindings (where the new tests live), explicitly run: `cargo test -p chia-sdk-bindings --all-features silent_payments` so the two new boundary tests are actually executed.
    Do NOT add new workspace deps; do NOT add `#[allow]`.
  </action>
  <verify>
    <automated>cargo test -p chia-sdk-bindings --all-features silent_payments 2>&1 | tail -15 && cargo fmt --all -- --files-with-diff --check 2>&1 | tail -3 && cargo machete 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `cargo test -p chia-sdk-bindings --all-features silent_payments` passes, including a test that calls `compute_input_hash` with an empty `Vec<Bytes32>` and asserts `Err` (no panic — the test process does NOT abort).
    - The empty-input test pins `SilentPaymentNoXchInputs` either by error variant or by the display string `"silent payment requires at least one wallet-controlled XCH input"`.
    - `cargo fmt --all -- --files-with-diff --check` exits 0.
    - `cargo machete` exits 0 with no new `[package.metadata.cargo-machete] ignored` entries anywhere.
    - `cargo clippy --workspace --all-features --all-targets` exits 0.
    - The full CI workspace test line (excludes-list per CLAUDE.md) passes.
    - ZERO new workspace dependencies; ZERO new `#[allow]` attributes in either modified file.
  </acceptance_criteria>
  <done>Two boundary tests prove empty input → `Err(SilentPaymentNoXchInputs)` (no panic) and non-empty input → `Ok`; the global regression bar (fmt, all-features clippy, machete, full workspace test line + the bindings-crate test run) is green; zero new deps and zero new `#[allow]`.</done>
</task>

</tasks>

<verification>
- Empty-input to the binding `compute_input_hash` returns `Err(DriverError::SilentPaymentNoXchInputs)` with no panic crossing the FFI boundary; a test proves it.
- `compute_input_hash`'s `# Panics` rustdoc matches the new internal-only-invariant contract; the driver fn signature is unchanged.
- `cargo build -p chia-sdk-bindings --all-features` + `cargo clippy -p chia-sdk-bindings --all-features --all-targets -- -D warnings` exit 0.
- Global regression bar green: `cargo fmt --all --check`, `cargo clippy --workspace --all-features --all-targets`, `cargo machete` (zero new ignored), the full CI workspace test line, and `cargo test -p chia-sdk-bindings --all-features silent_payments`.
- ZERO new workspace deps; ZERO new `#[allow]` attributes.
</verification>

<success_criteria>
- ISSUE-1 closed and verified under the permutation that currently misses it: a test at the binding boundary proves empty input → `Err` (not a panic); `cargo test -p chia-sdk-driver --features chip-0057` and the bindings test both green.
- The combined 09.1 changeset (Plan 01 + Plan 02) passes the full global regression bar with zero new workspace deps.
</success_criteria>

<output>
After completion, create `.planning/phases/09.1-fix-5-maintainer-flagged-conformance-issues-in-chip-0057-sp-surface/09.1-02-SUMMARY.md`
</output>
