---
phase: 1
plan: 01
title: "Workspace chip-0057 feature flag scaffolding"
type: execute
wave: 1
depends_on: []
files_modified:
  - "Cargo.toml"
  - "crates/chia-sdk-types/Cargo.toml"
  - "crates/chia-sdk-driver/Cargo.toml"
  - "crates/chia-sdk-utils/Cargo.toml"
autonomous: true
requirements: [WS-01]
requirements_addressed: [WS-01]

must_haves:
  truths:
    - "Root workspace exposes a chip-0057 feature that cascades to types/driver/utils."
    - "`cargo build -p chia-sdk-types -F chip-0057` succeeds (no module code yet — empty feature is a no-op)."
    - "`cargo build -p chia-sdk-driver -F chip-0057` succeeds (feature exists, cascades into chia-sdk-types/chip-0057)."
    - "`cargo build -p chia-sdk-utils -F chip-0057` succeeds (utils gains its first `[features]` block in this plan)."
    - "`cargo build --workspace --all-features` succeeds (cumulative cascade activates correctly)."
    - "`cargo build --workspace` (no features) still succeeds (no regression)."
  artifacts:
    - path: "Cargo.toml"
      provides: "Root workspace `[features]` entry `chip-0057 = [chia-sdk-driver/chip-0057, chia-sdk-types/chip-0057, chia-sdk-utils/chip-0057]`"
      contains: "chip-0057 = ["
    - path: "crates/chia-sdk-types/Cargo.toml"
      provides: "`chip-0057 = []` feature entry"
      contains: "chip-0057 = []"
    - path: "crates/chia-sdk-driver/Cargo.toml"
      provides: "`chip-0057 = [chia-sdk-types/chip-0057]` cascading feature entry"
      contains: "chip-0057 = [\"chia-sdk-types/chip-0057\"]"
    - path: "crates/chia-sdk-utils/Cargo.toml"
      provides: "NEW `[features]` block with `chip-0057 = []`"
      contains: "[features]"
  key_links:
    - from: "Cargo.toml"
      to: "crates/chia-sdk-types/Cargo.toml"
      via: "feature dependency string `chia-sdk-types/chip-0057`"
      pattern: "chia-sdk-types/chip-0057"
    - from: "Cargo.toml"
      to: "crates/chia-sdk-driver/Cargo.toml"
      via: "feature dependency string `chia-sdk-driver/chip-0057`"
      pattern: "chia-sdk-driver/chip-0057"
    - from: "Cargo.toml"
      to: "crates/chia-sdk-utils/Cargo.toml"
      via: "feature dependency string `chia-sdk-utils/chip-0057`"
      pattern: "chia-sdk-utils/chip-0057"
    - from: "crates/chia-sdk-driver/Cargo.toml"
      to: "crates/chia-sdk-types/Cargo.toml"
      via: "feature cascade `chip-0057 = [chia-sdk-types/chip-0057]`"
      pattern: "chia-sdk-types/chip-0057"
---

<objective>
Add the `chip-0057` workspace feature and cascade it to `chia-sdk-types`, `chia-sdk-driver`, and `chia-sdk-utils`. This scaffolds the gate that every Phase 1+ module will compile behind. Mirrors the `chip-0037` precedent commit `bbc7f57f` literally.

Purpose: Establish the feature flag before any module code is written, so Plans 02/03/04 can land `#[cfg(feature = "chip-0057")]` gated modules in parallel and have them compile. `chia-sdk-utils` has no `[features]` block currently — this plan adds one in anticipation of Phase 2's address work (per Q8 deferral in STATE.md, no `dep:chia-sdk-types` edge is added in Phase 1).

Output: Four edited Cargo.toml files. No Rust source changes, no module files. The feature is an empty no-op until Plans 02/03/04 wire modules under it.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-crypto-primitives-workspace-integration/01-RESEARCH.md
@.planning/phases/01-crypto-primitives-workspace-integration/01-VALIDATION.md
@CLAUDE.md
@Cargo.toml
@crates/chia-sdk-types/Cargo.toml
@crates/chia-sdk-driver/Cargo.toml
@crates/chia-sdk-utils/Cargo.toml

<interfaces>
<!-- Existing patterns to mirror exactly. From the four Cargo.toml files above. -->

Root Cargo.toml `[features]` block (currently lines 72-79) — chip-0037 precedent:
```toml
[features]
chip-0035 = ["chia-sdk-driver/chip-0035", "chia-sdk-types/chip-0035"]
chip-0037 = ["chia-sdk-driver/chip-0037", "chia-sdk-types/chip-0037"]
```

chia-sdk-types `[features]` block (currently lines 17-20) — empty-list pattern:
```toml
[features]
chip-0035 = []
chip-0037 = []
action-layer = []
```

chia-sdk-driver `[features]` block (currently lines 20-24) — cascade-only pattern:
```toml
[features]
chip-0035 = ["chia-sdk-types/chip-0035"]
chip-0037 = ["chia-sdk-types/chip-0037", "dep:sha3"]
action-layer = ["chia-sdk-types/action-layer"]
```

chia-sdk-utils Cargo.toml — verified by reading file: has NO `[features]` block today. Currently goes `[lints]` (line 14-15) → `[dependencies]` (line 17). The new `[features]` block must be inserted between them.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add chip-0057 to root and per-crate Cargo.toml files</name>
  <read_first>
    - Cargo.toml (root) — confirm exact location of `[features]` block (lines 72-79) and the chip-0037 line to model after
    - crates/chia-sdk-types/Cargo.toml — confirm `[features]` block at lines 17-20
    - crates/chia-sdk-driver/Cargo.toml — confirm `[features]` block at lines 20-24
    - crates/chia-sdk-utils/Cargo.toml — confirm there is NO `[features]` block (verify by reading end-to-end)
    - .planning/phases/01-crypto-primitives-workspace-integration/01-RESEARCH.md Example 6 (lines 547-572) — the literal edits
  </read_first>
  <files>Cargo.toml, crates/chia-sdk-types/Cargo.toml, crates/chia-sdk-driver/Cargo.toml, crates/chia-sdk-utils/Cargo.toml</files>
  <action>
Make four Cargo.toml edits. Each edit is small and exact — match formatting style of the surrounding lines.

1. **Root `Cargo.toml`** — Insert AFTER the existing `chip-0037 = [...]` line (currently line 74) in the `[features]` block:

```toml
chip-0057 = ["chia-sdk-driver/chip-0057", "chia-sdk-types/chip-0057", "chia-sdk-utils/chip-0057"]
```

Match the single-line shape of the existing `chip-0035` / `chip-0037` lines (NOT a multi-line array — those two precedents are inline).

2. **`crates/chia-sdk-types/Cargo.toml`** — Append AFTER `action-layer = []` line (currently line 20) in the `[features]` block:

```toml
chip-0057 = []
```

Empty list mirrors `chip-0035 = []` / `chip-0037 = []` directly above. All deps `silent_payments/` needs (`num-bigint`, `chia-sha2`, `hex-literal`, `chia-bls`) are already unconditional in `[dependencies]` — no `optional = true` flips required.

3. **`crates/chia-sdk-driver/Cargo.toml`** — Insert AFTER the existing `chip-0037 = [...]` line (currently line 22) in the `[features]` block:

```toml
chip-0057 = ["chia-sdk-types/chip-0057"]
```

Cascade-only — no `dep:` activations needed (Phase 1 adds no chip-0057 code to chia-sdk-driver; the cascade just lets Phase 3/4 see `chia_sdk_types::silent_payments::*`).

4. **`crates/chia-sdk-utils/Cargo.toml`** — Insert a NEW `[features]` block. There is currently no `[features]` block in this file. Place it BETWEEN the existing `[lints]` block (lines 14-15) and the existing `[dependencies]` block (line 17). The result should look like:

```toml
[lints]
workspace = true

[features]
chip-0057 = []

[dependencies]
thiserror = { workspace = true }
...
```

Phase 1 adds no `chip-0057`-gated code to `chia-sdk-utils`. The empty feature exists so Phase 2 (`SilentPaymentKeys`, `SilentPaymentAddress`) has a feature to gate code under without touching Cargo.toml again. Per STATE.md Q8 deferral: do NOT add `chia-sdk-types = { workspace = true, optional = true }` or change `chip-0057 = [...]` to include `dep:chia-sdk-types` — that's Phase 2's pre-flight audit.

**Do NOT** add any new entries to `[workspace.dependencies]`. **Do NOT** add any new `[package.metadata.cargo-machete] ignored` entries.

After making all four edits, run `cargo fmt --all -- --files-with-diff --check` to confirm TOML formatting is unchanged (cargo fmt does not touch Cargo.toml but ensures nothing else slipped). Then run the gate command in `<verify>`.
  </action>
  <verify>
    <automated>cargo build --release -p chia-sdk-types -F chip-0057 && cargo build --release -p chia-sdk-driver -F chip-0057 && cargo build --release -p chia-sdk-utils -F chip-0057 && cargo build --release --workspace --all-features && cargo build --release --workspace</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q 'chip-0057 = \["chia-sdk-driver/chip-0057"' Cargo.toml` exits 0
    - `grep -q 'chia-sdk-types/chip-0057' Cargo.toml` exits 0
    - `grep -q 'chia-sdk-utils/chip-0057' Cargo.toml` exits 0
    - `grep -q '^chip-0057 = \[\]$' crates/chia-sdk-types/Cargo.toml` exits 0
    - `grep -q 'chip-0057 = \["chia-sdk-types/chip-0057"\]' crates/chia-sdk-driver/Cargo.toml` exits 0
    - `grep -q '^\[features\]$' crates/chia-sdk-utils/Cargo.toml` exits 0
    - `grep -q '^chip-0057 = \[\]$' crates/chia-sdk-utils/Cargo.toml` exits 0
    - `cargo build --release -p chia-sdk-types -F chip-0057` exits 0
    - `cargo build --release -p chia-sdk-driver -F chip-0057` exits 0
    - `cargo build --release -p chia-sdk-utils -F chip-0057` exits 0
    - `cargo build --release --workspace --all-features` exits 0
    - `cargo build --release --workspace` exits 0 (no-features still builds cleanly)
    - `git diff --stat -- '**/Cargo.toml'` shows exactly 4 files changed with small additions (no removals, no formatting churn elsewhere)
    - `git diff -- '**/Cargo.toml' | grep -q '^\+.*package.metadata.cargo-machete' && false || true` (zero new `package.metadata.cargo-machete` lines introduced — final cargo-machete coverage is at Plan 05)
  </acceptance_criteria>
  <done>
    The `chip-0057` feature exists in the root workspace and cascades correctly to types, driver, and utils. All five build permutations from `01-RESEARCH.md` lines 38, 405-409 pass. No Rust source files were modified. No new workspace dependencies were added.
  </done>
</task>

</tasks>

<verification>
**Plan-level gate commands (must all exit 0):**

```bash
cargo build --release -p chia-sdk-types -F chip-0057
cargo build --release -p chia-sdk-driver -F chip-0057
cargo build --release -p chia-sdk-utils -F chip-0057
cargo build --release --workspace --all-features
cargo build --release --workspace
cargo fmt --all -- --files-with-diff --check
```

`cargo machete` is run at plan 05 (cumulative gate) — at this plan stage no Rust code is added so machete cannot regress.

**Must-haves verified:**
- M1 (cargo build -p chia-sdk-types -F chip-0057): yes
- M2 (cargo build --workspace --all-features): yes
- M3 (cargo build --workspace no features): yes
- M9 (chip-0057 declared on types, driver, AND utils): yes
</verification>

<success_criteria>
- All four Cargo.toml files updated with single-line additions matching the existing chip-0035/chip-0037 pattern.
- All six gate commands in `<verification>` pass.
- No new `[workspace.dependencies]` entries.
- No new `[package.metadata.cargo-machete] ignored` entries anywhere.
- Plans 02, 03, 04 can now reference `#[cfg(feature = "chip-0057")]` and have it activate correctly.
</success_criteria>

<output>
After completion, create `.planning/phases/01-crypto-primitives-workspace-integration/01-01-SUMMARY.md` per the standard SUMMARY template.
</output>
