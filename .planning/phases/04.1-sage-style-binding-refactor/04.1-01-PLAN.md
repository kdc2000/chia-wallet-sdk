---
phase: 04.1
plan: 01
title: "Drop opcode 60/61 announcement emission + update multi-input tests to Relation::AssertConcurrent"
type: execute
wave: 1
status: pending
depends_on: []
files_modified:
  - "crates/chia-sdk-driver/src/silent_payments/send_keys.rs"
  - "crates/chia-sdk-driver/src/actions/silent_payment_send.rs"
autonomous: true
requirements: [SEND-06, FINGERPRINT-01]

must_haves:
  truths:
    - "`crates/chia-sdk-driver/src/silent_payments/send_keys.rs` no longer defines `emit_silent_payment_announcements`. The method body (the `/// Emit cross-input announcement binding (opcode 60 / opcode 61) for` rustdoc starting at line 45 plus the `pub(crate) fn` definition running 87-116) is deleted entirely. (per ROADMAP §04.1 SC1; per FINGERPRINT-01)"
    - "`crates/chia-sdk-driver/src/silent_payments/send_keys.rs::finish_with_silent_payment_keys` no longer calls `self.emit_silent_payment_announcements(...)`. The Step 8.5 call site at lines 245-250 (the 5-line `// Step 8.5: ...` comment block plus the `self.emit_silent_payment_announcements(ctx, &xch_input_ids);` statement) is deleted. (per ROADMAP §04.1 SC1; per FINGERPRINT-01)"
    - "`crates/chia-sdk-driver/src/silent_payments/send_keys.rs` module docstring (lines 1-9) is updated: the clause `and (Plan 04-04) emits the opcode 60/61 announcement binding for >=2-input scenarios.` is replaced with `and relies on the SDK's Relation::AssertConcurrent cycle binding (Phase 04.1) for multi-input atomicity.` (per RESEARCH §1d)"
    - "`crates/chia-sdk-driver/src/silent_payments/send_keys.rs` imports cleaned: `chia_protocol::Bytes` (line 12 currently includes it), `chia_sdk_types::Conditions` + `chia_sdk_types::conditions::announcement_id` (lines 14-17 currently), and `crate::SpendKind` (line 22 currently) are no longer present. `Bytes32` and `Coin` continue to be imported from `chia_protocol` (used elsewhere in the file). (per RESEARCH §1c)"
    - "`crates/chia-sdk-driver/src/actions/silent_payment_send.rs::tests::single_input_no_announcement` (lines 439-498) is deleted. (per ROADMAP §04.1 SC2; per RESEARCH §2 row 439)"
    - "`crates/chia-sdk-driver/src/actions/silent_payment_send.rs::tests::cross_index_announcement_binding` (lines 500-577) is deleted. (per ROADMAP §04.1 SC2; per RESEARCH §2 row 500)"
    - "`crates/chia-sdk-driver/src/actions/silent_payment_send.rs::tests::input_hash_round_trip` (lines 578-664) is updated: the `Relation::None` argument to `finish_with_silent_payment_keys` at line 615 is changed to `Relation::AssertConcurrent` (the test uses 2 XCH inputs and now reflects production-correct usage). The test body, imports, recipient setup, and the receiver-side `compute_input_hash` math closure assertion are retained unchanged. (per ROADMAP §04.1 SC6; per RESEARCH §8 + Pitfall B)"
    - "`crates/chia-sdk-driver/src/silent_payments/send_keys.rs::tests::multi_party_hard_errors` (line 277-318) is updated: the `Relation::None` argument at line 307 is changed to `Relation::AssertConcurrent`. The test continues to expect `Err(DriverError::SilentPaymentMultiPartyUnsupported)` — the SK-coverage check in Step 3 still fires first under the new relation. (per RESEARCH §Pitfall A + Pitfall B option b)"
    - "Post-edit grep gates hold (per VALIDATION.md): `! grep -rn 'emit_silent_payment_announcements' crates/chia-sdk-driver/src/` returns empty AND `! grep -E 'create_coin_announcement|assert_coin_announcement|announcement_id|CREATE_COIN_ANNOUNCEMENT|ASSERT_COIN_ANNOUNCEMENT' crates/chia-sdk-driver/src/silent_payments/send_keys.rs` returns empty. (closes FINGERPRINT-01 deletion half)"
    - "`cargo build --release -p chia-sdk-driver --features chip-0057` succeeds. `cargo clippy -p chia-sdk-driver --features chip-0057 --all-targets -- -D warnings` is clean. (per VALIDATION.md Plan 04.1-01 build & lint gate)"
    - "Six surviving Phase 4 action tests still pass: `action_state_machine`, `round_trip_matches_derive_one_time_puzzle_hash`, `multi_output_same_scan_pk_increments_k`, `multi_output_distinct_scan_pks_independent_counters`, `input_hash_round_trip` (now with `Relation::AssertConcurrent`), and the 3 `memo_hint_guard_*` tests = 8 total. Plus `silent_payments::send_keys::tests::multi_party_hard_errors` still passes with `Relation::AssertConcurrent`. (per VALIDATION.md Plan 04.1-01 test rows)"
  artifacts:
    - path: "crates/chia-sdk-driver/src/silent_payments/send_keys.rs"
      provides: "`finish_with_silent_payment_keys` with the announcement emission removed (8 steps, not 9); cleaned imports; updated module docstring; `multi_party_hard_errors` test updated to `Relation::AssertConcurrent`"
      contains: "fn finish_with_silent_payment_keys"
      forbids: "emit_silent_payment_announcements"
    - path: "crates/chia-sdk-driver/src/actions/silent_payment_send.rs"
      provides: "`input_hash_round_trip` test updated to use `Relation::AssertConcurrent`; the two announcement-specific tests removed. File shrinks from 778 → ~620 lines."
      contains: "fn input_hash_round_trip"
      forbids: "fn single_input_no_announcement|fn cross_index_announcement_binding"
  key_links:
    - from: "crates/chia-sdk-driver/src/silent_payments/send_keys.rs::finish_with_silent_payment_keys"
      to: "crates/chia-sdk-driver/src/action_system/spends.rs::emit_relation"
      via: "Step 9's `self.finish_with_keys(...)` transitively calls `prepare()` → `emit_relation(relation)`. With opcode 60/61 emission gone, the cycle binding produced by `Relation::AssertConcurrent` (opcode 64 closed cycle, spends.rs:338-364) is the sole input-binding mechanism for multi-input SP sends."
      pattern: "self\\.finish_with_keys"
    - from: "Multi-input test paths in `actions::silent_payment_send::input_hash_round_trip` and `silent_payments::send_keys::multi_party_hard_errors`"
      to: "`Relation::AssertConcurrent` consumed by `finish_with_silent_payment_keys`"
      via: "Tests now reflect production-correct usage. Plan 04.1-02's runtime gate will enforce this; updating these tests pre-emptively means 04.1-02's gate addition is a no-op for them (per RESEARCH §Pitfall B)."
      pattern: "Relation::AssertConcurrent"
---

<objective>
Wave 1 of Phase 4.1: delete the SDK-emitted opcode 60/61 empty-message announcement binding from the silent-payment send path and update the affected multi-input tests to use `Relation::AssertConcurrent` (the production-correct value).

After this plan, multi-input SP transactions land at the same on-chain shape as ordinary Sage multi-input wallet transactions: the only cross-input binding is the existing `Relation::AssertConcurrent` opcode-64 cycle that every SDK-built multi-input spend already emits. The SP-specific fingerprint identified in this milestone's design review is removed.

This plan covers the deletion half of FINGERPRINT-01 (the helper + emission disappear; grep gates pass) and the re-validation half of SEND-06 (the existing multi-input tests still close round-trip math under the cycle binding alone). Plan 04.1-02 adds the runtime gate, the `Relation` rustdoc, the pinning test, and the new `DriverError` variant.

**Critical Pitfall B handling:** RESEARCH §Pitfall B identifies two multi-input tests using `Relation::None` that need updating: `input_hash_round_trip` (in `actions/silent_payment_send.rs`) and `multi_party_hard_errors` (in `silent_payments/send_keys.rs`). Without the update, `input_hash_round_trip` would continue to pass mechanically but no longer reflect production usage. After updating, both tests exercise the production-correct path. `round_trip_matches_derive_one_time_puzzle_hash` is single-input and stays on `Relation::None` (verified by RESEARCH §8). The `multi_output_*` tests don't call `finish_with_silent_payment_keys` and have no Relation argument to update.

Purpose: With the announcement emission gone and the tests updated, Plan 04.1-02 lands the runtime gate that hard-errors any multi-input SP send that passes `Relation::None`.

Output: 2 modified files; net ~150 lines removed.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/04.1-sage-style-binding-refactor/04.1-RESEARCH.md
@.planning/phases/04.1-sage-style-binding-refactor/04.1-VALIDATION.md
@.planning/phases/04-send-side-action/04-04-PLAN.md
@.planning/phases/04-send-side-action/04-04-SUMMARY.md
@CLAUDE.md
@crates/chia-sdk-driver/src/silent_payments/send_keys.rs
@crates/chia-sdk-driver/src/actions/silent_payment_send.rs
@crates/chia-sdk-driver/src/action_system/relation.rs
@crates/chia-sdk-driver/src/action_system/spends.rs

<interfaces>
Confirmed file state at start of Plan 04.1-01:

`crates/chia-sdk-driver/src/silent_payments/send_keys.rs` (319 lines total):
- Lines 1-9: module docstring (contains the clause to update on lines 8-9)
- Lines 11-24: import block (Bytes, Conditions, announcement_id, SpendKind to be removed)
- Lines 44-116: `impl Spends { pub(crate) fn emit_silent_payment_announcements(...) }` — entire method to delete (the rustdoc starts at line 45, body at line 87, closing brace at line 116)
- Lines 150-256: `pub fn finish_with_silent_payment_keys` — Step 8.5 call site at lines 245-250 inside it
- Lines 259-319: `#[cfg(test)] mod tests` with `multi_party_hard_errors` (line 307 has `Relation::None,`)

`crates/chia-sdk-driver/src/actions/silent_payment_send.rs` (778 lines total):
- Line 254: `fn round_trip_matches_derive_one_time_puzzle_hash` (single-input; line 296 `Relation::None` — KEEP unchanged)
- Line 339: `fn multi_output_same_scan_pk_increments_k` (no finish call; no Relation arg — KEEP unchanged)
- Line 393: `fn multi_output_distinct_scan_pks_independent_counters` (no finish call; no Relation arg — KEEP unchanged)
- Lines 439-498: `fn single_input_no_announcement` — DELETE
- Lines 500-577: `fn cross_index_announcement_binding` — DELETE
- Lines 578-664: `fn input_hash_round_trip` (2 XCH inputs; line 615 `Relation::None,` — UPDATE to `Relation::AssertConcurrent,`)
- Lines 665-777: 3 memo-hint guard tests — KEEP unchanged

`Relation` enum (`action_system/relation.rs`, 6 lines, no rustdoc currently):

```rust
#[derive(Debug, Clone, Copy, PartialEq, Eq, Hash)]
pub enum Relation {
    None,
    AssertConcurrent,
}
```

`Spends::emit_relation` (`action_system/spends.rs:338-364`) emits the opcode-64 cycle when `Relation::AssertConcurrent`. After Plan 04.1-01, this is the ONLY cross-input binding for multi-input SP sends:

```rust
fn emit_relation(&mut self, relation: Relation) {
    match relation {
        Relation::None => {}
        Relation::AssertConcurrent => {
            let coin_ids: Vec<Bytes32> = self
                .iter_conditions_spends()
                .map(|(coin, _)| coin.coin_id())
                .collect();
            if coin_ids.len() <= 1 { return; }
            self.iter_conditions_spends()
                .enumerate()
                .for_each(|(i, (_, spend))| {
                    spend.add_conditions(Conditions::new().assert_concurrent_spend(
                        if i == 0 { coin_ids[coin_ids.len() - 1] } else { coin_ids[i - 1] },
                    ));
                });
        }
    }
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Delete emit_silent_payment_announcements + Step 8.5 + unused imports; update module docstring + multi_party_hard_errors test in send_keys.rs</name>
  <files>
    - "crates/chia-sdk-driver/src/silent_payments/send_keys.rs"
  </files>
  <read_first>
    - "crates/chia-sdk-driver/src/silent_payments/send_keys.rs (current state: 319 lines; module docstring 1-9, imports 11-24, method to delete 44-116, call site 245-250, multi_party_hard_errors Relation::None at line 307)"
    - ".planning/phases/04.1-sage-style-binding-refactor/04.1-RESEARCH.md §1 (line-number-precise deletion targets), §Pitfall A (multi_party_hard_errors test stays; only Relation argument updates), §Pitfall B option b (multi_party_hard_errors with AssertConcurrent still hits SK-coverage check first → same expected error)"
    - ".planning/phases/04.1-sage-style-binding-refactor/04.1-VALIDATION.md (Plan 04.1-01 grep gates + tests to re-run)"
  </read_first>
  <action>
Apply five sequential `Edit` operations to `crates/chia-sdk-driver/src/silent_payments/send_keys.rs`. Apply in order; verify with the bash command at the end of `<verify>`.

**Edit 1 — Update the module docstring (lines 5-9)**

The current text reads:

```
//! - [`Spends::finish_with_silent_payment_keys`] is the finish-time entry
//!   point that aggregates the sender SKs, computes the `input_hash`, derives
//!   each output's one-time puzzle hash, emits the matching `CreateCoin`
//!   conditions, and (Plan 04-04) emits the opcode 60/61 announcement
//!   binding for >=2-input scenarios.
```

Replace the final clause. The new docstring (canonical per RESEARCH §1d):

```
//! - [`Spends::finish_with_silent_payment_keys`] is the finish-time entry
//!   point that aggregates the sender SKs, computes the `input_hash`, derives
//!   each output's one-time puzzle hash, emits the matching `CreateCoin`
//!   conditions, and relies on the SDK's `Relation::AssertConcurrent` cycle
//!   binding (Phase 04.1) for multi-input atomicity.
```

**Edit 2 — Clean unused imports (lines 11-24)**

Replace the entire import block. Current block:

```rust
use chia_bls::{PublicKey, SecretKey};
use chia_protocol::{Bytes, Bytes32, Coin};
use chia_puzzle_types::Memos;
use chia_sdk_types::{
    Conditions,
    conditions::{CreateCoin, announcement_id},
};
use clvmr::NodePtr;
use indexmap::IndexMap;

use crate::{
    Asset, Deltas, DriverError, Outputs, Relation, SpendContext, SpendKind, Spends,
    silent_payments::{aggregate_sender_sks, compute_input_hash, derive_one_time_puzzle_hash},
};
```

Replace with (per RESEARCH §1c — remove `Bytes`, `Conditions`, `announcement_id`, `SpendKind`):

```rust
use chia_bls::{PublicKey, SecretKey};
use chia_protocol::{Bytes32, Coin};
use chia_puzzle_types::Memos;
use chia_sdk_types::conditions::CreateCoin;
use clvmr::NodePtr;
use indexmap::IndexMap;

use crate::{
    Asset, Deltas, DriverError, Outputs, Relation, SpendContext, Spends,
    silent_payments::{aggregate_sender_sks, compute_input_hash, derive_one_time_puzzle_hash},
};
```

Retained import audit: `PublicKey`/`SecretKey` (used in `finish_with_silent_payment_keys` signature + tests), `Bytes32` (used in `xch_input_ids: Vec<Bytes32>`), `Coin` (used in `outputs.xch.push(Coin::new(...))`), `Memos` + `NodePtr` (used in `SilentPaymentPending` struct), `CreateCoin` (used in `CreateCoin::new(...)` at line 220), `IndexMap` (used in finish signature), `Asset`/`Deltas`/`DriverError`/`Outputs`/`Relation`/`SpendContext`/`Spends` (all used in finish + tests), `aggregate_sender_sks`/`compute_input_hash`/`derive_one_time_puzzle_hash` (used in finish body).

**Edit 3 — Delete `emit_silent_payment_announcements` method (its rustdoc + body running from line 45 through line 116)**

Locate the rustdoc starting with `    /// Emit cross-input announcement binding (opcode 60 / opcode 61) for` and the closing brace `}` of the `emit_silent_payment_announcements` method. The method sits inside the same `impl Spends { ... }` block that contains `finish_with_silent_payment_keys` — only delete the method itself, NOT the surrounding `impl Spends {` opening or `finish_with_silent_payment_keys`.

Delete from the line containing `    /// Emit cross-input announcement binding (opcode 60 / opcode 61) for` through and including the closing brace `}` of `emit_silent_payment_announcements` (and one blank line immediately after, if present). After the deletion, the `impl Spends {` block should contain ONLY `finish_with_silent_payment_keys` — the file flow should go from the `SilentPaymentPending` struct closing brace → `impl Spends {` opening → directly to the `/// Finish the spend with synthetic-key maps,` rustdoc.

**Edit 4 — Delete the Step 8.5 call site (lines 245-250)**

Inside `finish_with_silent_payment_keys`, find:

```rust
        // Step 8.5: emit cross-input announcement binding (opcode 60/61) for
        // >=2-input scenarios. Allows the recipient's Pass 2b scanner to
        // reconstruct the cross-input group and compute the matching
        // input_hash. Single-input sends short-circuit inside the helper.
        // The helper returns `()` (clippy::unnecessary_wraps) — no `?`.
        self.emit_silent_payment_announcements(ctx, &xch_input_ids);
```

Delete those 6 lines. After deletion, the body flows directly from the Step 8 per-pending loop's closing brace to the `// Step 9: delegate to the standard finish path.` comment.

**Edit 5 — Update `multi_party_hard_errors` test (currently line 307): change `Relation::None,` to `Relation::AssertConcurrent,`**

In the `mod tests` block, locate the `multi_party_hard_errors` test (starts ~line 277). Find the call to `finish_with_silent_payment_keys` whose third argument is `Relation::None,`:

```rust
        let result = spends.finish_with_silent_payment_keys(
            &mut ctx,
            &deltas,
            Relation::None,
            &indexmap! { alice.puzzle_hash => alice.pk },
            &indexmap! { alice.puzzle_hash => alice.sk.clone() },
        );
```

Change `Relation::None,` to `Relation::AssertConcurrent,`. Per RESEARCH §Pitfall B option b, the test continues to expect `Err(DriverError::SilentPaymentMultiPartyUnsupported)` — the SK-coverage check in Step 3 fires before any other check regardless of the relation argument. The test's intent (verify SK-coverage hard-error) is preserved; the argument update reflects production-correct usage for a 2-input batch and makes the test compatible with Plan 04.1-02's runtime gate.

**After all 5 edits**, run the verify command. If clippy fires on any newly-unused parameter or import, fix inline (no `#[allow]`). Per CLAUDE.md: prefer LSP diagnostics; the cargo invocations in `<verify>` are the gating check.
  </action>
  <verify>
    <automated>cargo build --release -p chia-sdk-driver --features chip-0057 2>&amp;1 | tail -3 &amp;&amp; ! grep -rn 'emit_silent_payment_announcements' crates/chia-sdk-driver/src/ &amp;&amp; ! grep -E 'create_coin_announcement|assert_coin_announcement|announcement_id|CREATE_COIN_ANNOUNCEMENT|ASSERT_COIN_ANNOUNCEMENT' crates/chia-sdk-driver/src/silent_payments/send_keys.rs &amp;&amp; cargo test --release -p chia-sdk-driver --features chip-0057 silent_payments::send_keys::tests::multi_party_hard_errors -- --exact 2>&amp;1 | grep -q 'test result: ok. 1 passed'</automated>
  </verify>
  <acceptance_criteria>
    - `! grep -rn 'emit_silent_payment_announcements' crates/chia-sdk-driver/src/` returns empty (method fully deleted; no call sites remain).
    - `! grep -E 'create_coin_announcement|assert_coin_announcement|announcement_id|CREATE_COIN_ANNOUNCEMENT|ASSERT_COIN_ANNOUNCEMENT' crates/chia-sdk-driver/src/silent_payments/send_keys.rs` returns empty.
    - `grep -nE '^use chia_protocol::' crates/chia-sdk-driver/src/silent_payments/send_keys.rs` returns exactly `use chia_protocol::{Bytes32, Coin};` (no `Bytes` other than `Bytes32`).
    - `! grep -nE '\bSpendKind\b' crates/chia-sdk-driver/src/silent_payments/send_keys.rs` returns empty.
    - `! grep -nE '\bConditions\b' crates/chia-sdk-driver/src/silent_payments/send_keys.rs` returns empty.
    - `grep -q "Relation::AssertConcurrent" crates/chia-sdk-driver/src/silent_payments/send_keys.rs` succeeds (the updated module docstring AND the multi_party_hard_errors test contain it).
    - `grep -c 'Step 8\.5' crates/chia-sdk-driver/src/silent_payments/send_keys.rs` returns 0.
    - `cargo build --release -p chia-sdk-driver --features chip-0057 2>&1 | grep -E '^error'` is empty.
    - `cargo build --release -p chia-sdk-driver 2>&1 | grep -E '^error'` is empty (no-features build still clean).
    - `cargo clippy -p chia-sdk-driver --features chip-0057 --all-targets -- -D warnings 2>&1 | grep -E '^error|^warning' | head -3` is empty.
    - `cargo test --release -p chia-sdk-driver --features chip-0057 silent_payments::send_keys::tests::multi_party_hard_errors -- --exact 2>&1 | grep -q 'test result: ok. 1 passed'` succeeds.
    - `! grep -E '#\[allow' crates/chia-sdk-driver/src/silent_payments/send_keys.rs` returns empty.
  </acceptance_criteria>
  <done>`emit_silent_payment_announcements` method + its rustdoc + Step 8.5 call site + four unused imports (Bytes, Conditions, announcement_id, SpendKind) are deleted from send_keys.rs. Module docstring no longer references opcode 60/61. `multi_party_hard_errors` test uses `Relation::AssertConcurrent`. File builds clean under chip-0057; clippy clean; SEND-03 test still passes.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Delete two announcement-specific tests from silent_payment_send.rs; update input_hash_round_trip Relation argument</name>
  <files>
    - "crates/chia-sdk-driver/src/actions/silent_payment_send.rs"
  </files>
  <read_first>
    - "crates/chia-sdk-driver/src/actions/silent_payment_send.rs (current state: 778 lines; tests start at line 193; single_input_no_announcement at 439-498, cross_index_announcement_binding at 500-577, input_hash_round_trip at 578-664 with Relation::None at line 615)"
    - ".planning/phases/04.1-sage-style-binding-refactor/04.1-RESEARCH.md §2 (test inventory with line-numbered KEEP/DELETE disposition), §8 (Pitfall B — input_hash_round_trip at line 615 MUST be updated to AssertConcurrent)"
    - ".planning/phases/04.1-sage-style-binding-refactor/04.1-VALIDATION.md (rows for surviving Plan 04.1-01 tests)"
  </read_first>
  <action>
Apply three sequential `Edit` operations to `crates/chia-sdk-driver/src/actions/silent_payment_send.rs`. Apply in the order given (delete higher-line-number test first so the earlier line numbers stay stable for the next edit); verify at the end.

**Edit 1 — Delete `cross_index_announcement_binding` test (currently lines 500-577 — delete this first to keep `single_input_no_announcement` line numbers stable)**

Locate the rustdoc that introduces `cross_index_announcement_binding` (line ~499). The rustdoc begins approximately with `/// SEND-06 + ROADMAP Phase 4 success criterion #4:`. Read lines 498-503 to confirm the exact rustdoc opening lines before performing the edit.

Delete from the rustdoc start through (and including) the closing brace `}` of `fn cross_index_announcement_binding`. The function body contains `use chia_sdk_types::{Condition, Conditions, conditions::announcement_id};` import lines that are LOCAL to the function — they're deleted along with the function. No external imports need cleanup from this deletion.

**Edit 2 — Delete `single_input_no_announcement` test (currently lines 439-498; line numbers stay valid for this edit since we deleted a later test first)**

Locate the rustdoc beginning with `/// SEND-06: a \`Spends\` with one XCH input + one \`SilentPaymentSend\``. Read lines 434-443 to confirm the exact rustdoc + function-header opening before editing.

Delete from the rustdoc start through (and including) the closing brace `}` of `fn single_input_no_announcement`. The function body contains `use chia_protocol::Bytes32;`, `use chia_sdk_types::Condition;`, and `use crate::SpendKind;` as LOCAL imports inside the function — they're deleted with it. No external imports need cleanup.

**After Edits 1 & 2**, file shrinks by ~140 lines. The next test in source order (`input_hash_round_trip`, originally line 578) shifts to approximately line 438.

**Edit 3 — Update `input_hash_round_trip` to use `Relation::AssertConcurrent`**

In the surviving `input_hash_round_trip` test body, locate the call to `finish_with_silent_payment_keys`:

```rust
        let outputs = spends.finish_with_silent_payment_keys(
            &mut ctx,
            &deltas,
            Relation::None,
            &indexmap! {
                alice.puzzle_hash => alice.pk,
                bob.puzzle_hash => bob.pk,
            },
            &indexmap! {
                alice.puzzle_hash => alice.sk.clone(),
                bob.puzzle_hash => bob.sk.clone(),
            },
        )?;
```

Change `Relation::None,` to `Relation::AssertConcurrent,`. No other change. The math closure that follows (independent reconstruction of `expected_ph` via `aggregate_sender_sks(&[alice.sk.clone(), bob.sk.clone()])`, `compute_input_hash(&coin_ids, &aggregated_sender_pk)`, `derive_one_time_puzzle_hash(...)` at `k=0`) and the `outputs.xch.iter().any(...)` assertion are correct under the new relation — they verify `compute_input_hash` determinism over the inputs, NOT the binding mechanism's emission shape.

**After all 3 edits**, six tests survive in the `mod tests` block:
1. `action_state_machine` (originally line 193; unchanged)
2. `round_trip_matches_derive_one_time_puzzle_hash` (originally line 254; unchanged — single-input, Relation::None stays correct)
3. `multi_output_same_scan_pk_increments_k` (originally line 339; unchanged — no finish call)
4. `multi_output_distinct_scan_pks_independent_counters` (originally line 393; unchanged — no finish call)
5. `input_hash_round_trip` (originally line 578; Relation argument updated to AssertConcurrent)
6. `memo_hint_guard_rejects_32_byte_first_memo` + `memo_hint_guard_allows_sentinel_prefixed` + `memo_hint_guard_allows_none` (originally lines 665-777; unchanged)

That's 8 tests total in the file.

After the edits, run the verify command. If clippy fires, fix inline (no `#[allow]`). Per CLAUDE.md: prefer LSP diagnostics; cargo invocations in `<verify>` are the gating check.
  </action>
  <verify>
    <automated>cargo build --release -p chia-sdk-driver --features chip-0057 2>&amp;1 | tail -3 &amp;&amp; ! grep -E 'fn single_input_no_announcement|fn cross_index_announcement_binding' crates/chia-sdk-driver/src/actions/silent_payment_send.rs &amp;&amp; cargo test --release -p chia-sdk-driver --features chip-0057 actions::silent_payment_send 2>&amp;1 | grep -E '8 passed|test result: ok' &amp;&amp; cargo clippy -p chia-sdk-driver --features chip-0057 --all-targets -- -D warnings 2>&amp;1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - `! grep -E 'fn single_input_no_announcement' crates/chia-sdk-driver/src/actions/silent_payment_send.rs` returns empty.
    - `! grep -E 'fn cross_index_announcement_binding' crates/chia-sdk-driver/src/actions/silent_payment_send.rs` returns empty.
    - `grep -E 'fn input_hash_round_trip' crates/chia-sdk-driver/src/actions/silent_payment_send.rs` succeeds (test is retained, not deleted).
    - Inside `input_hash_round_trip`: `grep -A 12 'fn input_hash_round_trip' crates/chia-sdk-driver/src/actions/silent_payment_send.rs | grep -q 'Relation::AssertConcurrent,'` succeeds AND the same window does NOT contain `Relation::None,` (confirms only the argument changed, no leftover).
    - `cargo build --release -p chia-sdk-driver --features chip-0057 2>&1 | grep -E '^error'` is empty.
    - `cargo build --release -p chia-sdk-driver 2>&1 | grep -E '^error'` is empty (no-features build clean).
    - `cargo test --release -p chia-sdk-driver --features chip-0057 actions::silent_payment_send::tests::input_hash_round_trip -- --exact 2>&1 | grep -q 'test result: ok. 1 passed'` succeeds.
    - `cargo test --release -p chia-sdk-driver --features chip-0057 actions::silent_payment_send::tests::round_trip_matches_derive_one_time_puzzle_hash -- --exact 2>&1 | grep -q 'test result: ok. 1 passed'` succeeds.
    - `cargo test --release -p chia-sdk-driver --features chip-0057 actions::silent_payment_send::tests::multi_output_same_scan_pk_increments_k -- --exact 2>&1 | grep -q 'test result: ok. 1 passed'` succeeds.
    - `cargo test --release -p chia-sdk-driver --features chip-0057 actions::silent_payment_send::tests::multi_output_distinct_scan_pks_independent_counters -- --exact 2>&1 | grep -q 'test result: ok. 1 passed'` succeeds.
    - `cargo test --release -p chia-sdk-driver --features chip-0057 actions::silent_payment_send::tests::action_state_machine -- --exact 2>&1 | grep -q 'test result: ok. 1 passed'` succeeds.
    - `cargo test --release -p chia-sdk-driver --features chip-0057 actions::silent_payment_send::tests::memo_hint_guard_ 2>&1 | grep -E 'test result: ok\. 3 passed|3 passed'` succeeds (all 3 memo-hint tests still pass).
    - `cargo test --release -p chia-sdk-driver --features chip-0057 actions::silent_payment_send 2>&1 | grep -E '8 passed|test result: ok\.' | head -1` succeeds (total 8 tests in the file).
    - `cargo clippy -p chia-sdk-driver --features chip-0057 --all-targets -- -D warnings 2>&1 | grep -E '^error|^warning' | head -3` is empty.
    - `! grep -E '#\[allow' crates/chia-sdk-driver/src/actions/silent_payment_send.rs` returns empty.
  </acceptance_criteria>
  <done>The two announcement-specific tests (`single_input_no_announcement`, `cross_index_announcement_binding`) are deleted. `input_hash_round_trip` uses `Relation::AssertConcurrent` and still passes its math closure assertion. All 8 surviving action tests pass; clippy clean; no new `#[allow]`.</done>
</task>

</tasks>

<verification>
After both tasks land:

1. `cargo build --release -p chia-sdk-driver --features chip-0057` succeeds.
2. `cargo build --release -p chia-sdk-driver` (no-features) succeeds.
3. `cargo build --release --workspace --all-features` succeeds.
4. `cargo clippy -p chia-sdk-driver --features chip-0057 --all-targets -- -D warnings` is clean.
5. `cargo test --release -p chia-sdk-driver --features chip-0057 actions::silent_payment_send` runs 8 tests, all pass.
6. `cargo test --release -p chia-sdk-driver --features chip-0057 silent_payments::send_keys::tests::multi_party_hard_errors -- --exact` passes.
7. `! grep -rn 'emit_silent_payment_announcements' crates/chia-sdk-driver/src/` returns empty.
8. `! grep -E 'create_coin_announcement|assert_coin_announcement|announcement_id|CREATE_COIN_ANNOUNCEMENT|ASSERT_COIN_ANNOUNCEMENT' crates/chia-sdk-driver/src/silent_payments/send_keys.rs` returns empty.
9. Phase 4 Privacy-warning coverage holds: `! grep -L 'Privacy warning' crates/chia-sdk-driver/src/actions/silent_payment_send.rs crates/chia-sdk-driver/src/silent_payments/{one_time,aggregate,input_hash,send_keys}.rs` returns empty.
10. No new `#[allow]` attributes in the silent_payments/ or actions/silent_payment_send.rs trees.
</verification>

<success_criteria>
- ROADMAP §04.1 SC1 closed: announcement-emission helper + Step 8.5 call site deleted.
- ROADMAP §04.1 SC2 partial: the two announcement-specific tests deleted (`single_input_no_announcement`, `cross_index_announcement_binding`). The 3 ROADMAP-language-included tests that are infrastructure-only (`multi_output_*`, `input_hash_round_trip`) survive unchanged or with only a Relation argument update.
- ROADMAP §04.1 SC6 partial: Phase 4 round-trip and multi-output tests still pass against the cycle binding alone. `input_hash_round_trip` now passes `Relation::AssertConcurrent`; the cycle binding's opcode-64 emission is what provides input-binding to the bundle. The receiver's `compute_input_hash` math closure still closes.
- FINGERPRINT-01 partial: deletion half closed (no opcode 60/61 emission; grep gates pass). The runtime gate (Plan 04.1-02) closes the enforcement half.
- SEND-06 re-validated under the new binding scheme: the 4 still-named tests (`round_trip_matches_derive_one_time_puzzle_hash`, `multi_output_same_scan_pk_increments_k`, `multi_output_distinct_scan_pks_independent_counters`, `input_hash_round_trip`) pass under the cycle binding.
</success_criteria>

<output>
After completion, create `.planning/phases/04.1-sage-style-binding-refactor/04.1-01-SUMMARY.md` documenting:

- Files modified (2 files; line deltas).
- Tests deleted (2: `single_input_no_announcement`, `cross_index_announcement_binding`).
- Tests updated (2: `input_hash_round_trip` and `multi_party_hard_errors` — Relation argument).
- Tests unchanged (6: 4 surviving action tests + memo guard ×3 + state machine).
- Grep gates that now pass (FINGERPRINT-01 deletion half).
- ROADMAP §04.1 criteria status: SC1 closed, SC2 partial, SC6 partial.
- Next-plan handoff: Plan 04.1-02 adds the runtime gate in `finish_with_silent_payment_keys` (rejects `Relation != AssertConcurrent` for multi-input SP), the `DriverError::SilentPaymentRequiresInputBinding` variant, the `Relation` enum rustdoc, the cycle pinning test, and 2 new gate tests in `send_keys.rs::tests`.
</output>
