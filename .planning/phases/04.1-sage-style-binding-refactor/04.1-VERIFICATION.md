---
phase: 04.1-sage-style-binding-refactor
verified: 2026-05-17T19:36:06Z
status: passed
score: 7/7 must-haves verified
re_verification: false
requirements_closed: [SEND-06, FINGERPRINT-01]
---

# Phase 04.1: Sage-style send-side binding refactor — Verification Report

**Phase Goal (from ROADMAP §04.1):** Multi-input SP transactions land at the same on-chain shape as ordinary Sage multi-input wallet transactions. The SDK-emitted opcode 60/61 empty-message announcement binding is removed; detection relies on the SDK's existing `Relation::AssertConcurrent` cycle binding (opcode 64 SCC) which is what every SDK-built multi-input spend already emits. SP transactions therefore disappear into the SDK's regular-traffic anonymity set, removing the SP-specific fingerprint identified in this milestone's design review.

**Verified:** 2026-05-17T19:36:06Z
**Status:** passed
**Re-verification:** No — initial verification

## Goal Achievement

### Observable Truths

| #  | Truth (Success Criterion)                                                                                                                                                                                                                                                                                                              | Status     | Evidence                                                                                                                                                                                                                                                                                                                                |
| -- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | ---------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 1  | SC1: `Spends::emit_silent_payment_announcements` + Step-8.5 call site deleted from `silent_payments/send_keys.rs`. No `CreateCoinAnnouncement`/`AssertCoinAnnouncement` emitted by SP send path.                                                                                                                                       | ✓ VERIFIED | `grep -rn 'emit_silent_payment_announcements' crates/chia-sdk-driver/src/` → empty. `grep -E 'create_coin_announcement\|assert_coin_announcement\|announcement_id\|CREATE_COIN_ANNOUNCEMENT\|ASSERT_COIN_ANNOUNCEMENT' .../send_keys.rs` → empty. `silent_payments/send_keys.rs` line count 346 (vs pre-phase 319; net +28 after re-additions). |
| 2  | SC2: Announcement-binding tests `cross_index_announcement_binding` and `single_input_no_announcement` deleted from `actions/silent_payment_send.rs::tests`; remaining tests still pass.                                                                                                                                            | ✓ VERIFIED | `grep -nE 'fn single_input_no_announcement\|fn cross_index_announcement_binding' .../silent_payment_send.rs` → empty. 8 remaining `#[test]` fns enumerated at lines 193, 254, 339, 393, 440, 527, 576, 615; all 8 PASS (`cargo test ... actions::silent_payment_send` → `8 passed`).                                                       |
| 3  | SC3: `finish_with_silent_payment_keys` returns `Err(DriverError::SilentPaymentRequiresInputBinding)` (new chip-0057-gated variant) when `relation != Relation::AssertConcurrent` AND `silent_payments_pending.len() > 0` AND ≥2 non-ephemeral XCH inputs. Single-input SP sends accept any `Relation`.                                  | ✓ VERIFIED | Variant present at `driver_error.rs:158` with `#[cfg(feature = "chip-0057")]` gate (line 154) and canonical wording at line 155. Gate code at `send_keys.rs:87-92` between Step-1 empty-pending short-circuit (line 83) and Step-2 input collection. Two gate tests `multi_input_requires_assert_concurrent_relation` + `single_input_accepts_relation_none` both PASS. |
| 4  | SC4: Pinning test on `Relation::AssertConcurrent` asserts the exact cyclic emission shape for N=2/3/4 inputs.                                                                                                                                                                                                                          | ✓ VERIFIED | Three named tests `assert_concurrent_relation_emits_cycle_for_n_coins_{2,3,4}` at `spends.rs:741,746,751` calling shared helper `assert_concurrent_cycle_for_n(n)` (lines 665-738). Helper walks `finished.xch.items`, asserts exactly 1 `Condition::AssertConcurrentSpend` per coin with `expected_predecessor` (`i == 0 → coin_ids[n-1]`, else `coin_ids[i-1]`). All 3 PASS. |
| 5  | SC5: `Relation::AssertConcurrent` variant gains public rustdoc identifying it as the CHIP-0057 Pass 2b detection guarantee.                                                                                                                                                                                                            | ✓ VERIFIED | `action_system/relation.rs` lines 1-35: enum-level rustdoc (lines 1-10) + per-variant rustdoc on `None` (13-17) + `AssertConcurrent` (19-34). 4 occurrences of `CHIP-0057`/`Pass 2b` in file. NOT `#[cfg(feature = "chip-0057")]` gated. Explicit reference to `Err(DriverError::SilentPaymentRequiresInputBinding)` on line 29 + pinning-test drift guard on lines 32-33. `cargo doc -p chia-sdk-driver --features chip-0057 --no-deps` → clean. |
| 6  | SC6: All Phase 4 round-trip and multi-output tests still pass against the cycle binding alone.                                                                                                                                                                                                                                         | ✓ VERIFIED | 4 named SC6 tests + 1 multi_party hard-error test all PASS: `round_trip_matches_derive_one_time_puzzle_hash`, `multi_output_same_scan_pk_increments_k`, `multi_output_distinct_scan_pks_independent_counters`, `input_hash_round_trip` (now `Relation::AssertConcurrent` at line 477), `multi_party_hard_errors` (now `Relation::AssertConcurrent` at line 231; still returns `SilentPaymentMultiPartyUnsupported`). 21 silent_payments + 8 actions::silent_payment_send tests PASS. |
| 7  | SC7: Workspace gates green: build `--all-features`, clippy `-D warnings` on driver +chip-0057, fmt, machete. Phase 4 grep bans hold.                                                                                                                                                                                                   | ✓ VERIFIED | `cargo build --release --workspace --all-features` → `Finished release profile in 3m 17s`. `cargo clippy -p chia-sdk-driver --features chip-0057 --all-targets -- -D warnings` → exit 0. `cargo fmt --all --check` → exit 0. `cargo machete` → "no unused dependencies". Phase 1 bans (`mod_by_group_order`, `^use sha2::`, `Sha256::digest`) all empty across SP trees. Phase 4 Privacy-warning coverage holds (5 files all retain). Full workspace test suite **2419 passed, 0 failed**. |

**Score:** 7/7 truths verified

### Required Artifacts (Level 1-3)

| Artifact                                                       | Expected                                                                                       | Exists | Substantive (>placeholder) | Wired             | Status     |
| -------------------------------------------------------------- | ---------------------------------------------------------------------------------------------- | ------ | -------------------------- | ----------------- | ---------- |
| `crates/chia-sdk-driver/src/silent_payments/send_keys.rs`      | Gate inserted; helper deleted; 2 new gate tests; imports cleaned                               | ✓      | ✓ (346 lines; 8-step finish; gate at lines 87-92) | ✓ (referenced by `actions/silent_payment_send.rs` + many downstream call sites) | ✓ VERIFIED |
| `crates/chia-sdk-driver/src/actions/silent_payment_send.rs`    | 2 announcement tests deleted; `input_hash_round_trip` updated to `Relation::AssertConcurrent`  | ✓      | ✓ (640 lines; 8 tests all passing)                | ✓ (action exposed via `Action::silent_payment_send`)                            | ✓ VERIFIED |
| `crates/chia-sdk-driver/src/driver_error.rs`                   | New `#[cfg(feature = "chip-0057")]` variant `SilentPaymentRequiresInputBinding` after `SilentPaymentMemoHintForbidden` | ✓      | ✓ (variant + canonical error message at lines 154-158) | ✓ (constructed in `send_keys.rs:91`; asserted in gate test `send_keys.rs:295`) | ✓ VERIFIED |
| `crates/chia-sdk-driver/src/action_system/relation.rs`         | Enum-level + per-variant rustdoc (NOT cfg-gated)                                               | ✓      | ✓ (35 lines vs pre-phase 6; full rustdoc per RESEARCH §6b verbatim with `coin_id` backtick fix) | ✓ (`Relation` enum already consumed pervasively across `action_system/`, `silent_payments/`)    | ✓ VERIFIED |
| `crates/chia-sdk-driver/src/action_system/spends.rs`           | New `#[cfg(test)] mod tests { ... }` block with parametric pinning test (NOT cfg-gated)        | ✓      | ✓ (754 lines; mod tests at lines 647-754; 3 named tests at lines 741, 746, 751) | ✓ (tests run as part of `action_system::spends::tests` test path; all 3 PASS) | ✓ VERIFIED |

### Key Link Verification (Level 3)

| From                                                                     | To                                                                                  | Via                                                                                                                                                                                | Status  | Details                                                                                                                                                                          |
| ------------------------------------------------------------------------ | ----------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `finish_with_silent_payment_keys` (gate body)                            | `DriverError::SilentPaymentRequiresInputBinding`                                    | `return Err(DriverError::SilentPaymentRequiresInputBinding)` at `send_keys.rs:91` when `non_ephemeral_xch_count >= 2 && !matches!(relation, Relation::AssertConcurrent)`           | ✓ WIRED | Construction site confirmed at line 91; assertion site in gate test `multi_input_requires_assert_concurrent_relation` at line 295 (matches against same variant).                |
| `finish_with_silent_payment_keys` (post-gate path)                       | `Spends::emit_relation` (the surviving cycle-binding code path)                     | Step 9 delegation `self.finish_with_keys(...)` → transitively calls `prepare()` → `emit_relation(relation)`; with opcode 60/61 emission deleted, this is the sole binding mechanism | ✓ WIRED | Pre-existing path; only new behaviour is the upstream gate; `input_hash_round_trip` integration test passes end-to-end under `Relation::AssertConcurrent`.                       |
| Pinning test `assert_concurrent_cycle_for_n(n)`                          | `Spends::emit_relation` (cycle emission code)                                       | `spends.prepare(&mut ctx, &deltas, Relation::AssertConcurrent)?` at `spends.rs:694` → `emit_relation` runs inside `prepare`; test walks `finished.xch.items` for each coin's conditions  | ✓ WIRED | Tests pass for N=2/3/4 — confirms the closed-cycle predecessor pattern (coin 0 → coin N-1; coin i → coin i-1) holds end-to-end.                                                  |
| `Relation` enum rustdoc                                                  | CHIP-0057 Pass 2b spec + `finish_with_silent_payment_keys` gate                     | Inline doc references `CHIP-0057`/`Pass 2b` (4 occurrences) + names `Err(DriverError::SilentPaymentRequiresInputBinding)` explicitly at line 29 + drift-guard reference to pinning test at lines 32-33 | ✓ WIRED | `cargo doc -p chia-sdk-driver --features chip-0057 --no-deps` → exit 0; intra-doc downgrade (per Plan 04.1-02 deviation #2) consciously avoids broken link.                       |
| Multi-input tests (`input_hash_round_trip`, `multi_party_hard_errors`)   | `Relation::AssertConcurrent` consumed by `finish_with_silent_payment_keys`          | Test bodies at `silent_payment_send.rs:477` and `send_keys.rs:231` now both pass `Relation::AssertConcurrent`; the gate is a no-op for them (production-correct usage)             | ✓ WIRED | Both tests PASS; multi_party retains its `SilentPaymentMultiPartyUnsupported` expectation (SK-coverage check still fires in Step 3).                                             |

### Data-Flow Trace (Level 4)

Level 4 is N/A for Phase 04.1 in the conventional render-dynamic-data sense — this phase produces no UI/data-rendering surfaces. Equivalent "data flows" here are the on-chain conditions emitted from `emit_relation`, which the pinning test (SC4) traces explicitly:

| Trace                                                                                 | Source                                                | Produces Real Data                                                                                                          | Status      |
| ------------------------------------------------------------------------------------- | ----------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------- | ----------- |
| `Spends::emit_relation(Relation::AssertConcurrent)` → `ConditionsSpend.conditions`    | `emit_relation` body at `spends.rs:338-364`           | ✓ Yes — pinning test walks live `Condition::AssertConcurrentSpend` instances in each coin's `ConditionsSpend.conditions_ref()` and asserts correct `coin_id` target per coin index | ✓ FLOWING   |
| `finish_with_silent_payment_keys` Step-8 per-pending loop → `outputs.xch` `Coin`s     | `send_keys.rs` Step-8 body                            | ✓ Yes — `input_hash_round_trip` test reconstructs `expected_ph` via `derive_one_time_puzzle_hash(...)` at k=0 and asserts `outputs.xch.iter().any(|c| c.puzzle_hash == expected_ph)` | ✓ FLOWING   |
| Gate path (multi-input SP without `AssertConcurrent`)                                 | `send_keys.rs:91` `return Err(...)`                   | ✓ Yes — `multi_input_requires_assert_concurrent_relation` asserts `matches!(result, Err(DriverError::SilentPaymentRequiresInputBinding))` with real `Spends` + 2 wallet inputs | ✓ FLOWING   |

### Behavioral Spot-Checks

Cargo invocations as commanded by the user's brief (workspace build + clippy + fmt + machete are non-negotiable; tests scoped to driver as permitted):

| Behavior                                                              | Command                                                                                                          | Result                                                  | Status |
| --------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------- | ------ |
| Full workspace `--all-features` build clean (SC7)                     | `cargo build --release --workspace --all-features`                                                               | `Finished release profile in 3m 17s` (no errors)        | ✓ PASS |
| Scoped clippy strict on `chia-sdk-driver -F chip-0057` (SC7)          | `cargo clippy -p chia-sdk-driver --features chip-0057 --all-targets -- -D warnings`                              | exit 0; no warnings                                     | ✓ PASS |
| Format check (SC7)                                                    | `cargo fmt --all -- --files-with-diff --check`                                                                   | exit 0 (no diff)                                        | ✓ PASS |
| Unused-dependency check (SC7)                                         | `cargo machete`                                                                                                  | "no unused dependencies in this directory"               | ✓ PASS |
| Workspace test suite green (SC7 closure)                              | `cargo test --release --workspace --all-features --exclude binding-crates`                                       | **2419 passed, 0 failed, 0 ignored**                    | ✓ PASS |
| SP-domain driver tests (silent_payments)                              | `cargo test --release -p chia-sdk-driver --features chip-0057 silent_payments`                                   | 21 passed, 0 failed (incl. both new gate tests)         | ✓ PASS |
| SP-domain driver tests (actions::silent_payment_send)                 | `cargo test --release -p chia-sdk-driver --features chip-0057 actions::silent_payment_send`                      | 8 passed, 0 failed                                      | ✓ PASS |
| Pinning tests (SC4)                                                   | `cargo test --release -p chia-sdk-driver action_system::spends::tests`                                           | 3 passed, 0 failed (N=2, N=3, N=4)                      | ✓ PASS |
| Cargo doc clean (SC5 ancillary; SUMMARY claim)                        | `cargo doc -p chia-sdk-driver --no-deps --features chip-0057`                                                    | exit 0; no broken intra-doc links                       | ✓ PASS |

### Requirements Coverage

| Requirement     | Source Plan                | Description                                                                                                                                                                                                  | Status      | Evidence                                                                                                                                                                                                                                                                                              |
| --------------- | -------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | ----------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| SEND-06         | 04.1-01-PLAN, 04.1-02-PLAN | Multi-input cross-key SP sends bind via `Relation::AssertConcurrent` cycle; sender + receiver `compute_input_hash` agree (re-validated under cycle binding)                                                  | ✓ SATISFIED | REQUIREMENTS.md line 23 entry updated; 4 named tests + pinning test green; `input_hash_round_trip` reconstructs `expected_ph` end-to-end under `Relation::AssertConcurrent`. SEND-06 description in REQUIREMENTS.md explicitly references the runtime gate.                                            |
| FINGERPRINT-01  | 04.1-01-PLAN, 04.1-02-PLAN | Multi-input SP transactions emit no SP-specific on-chain marker; runtime gate hard-errors `Relation != AssertConcurrent` for ≥2 non-ephemeral XCH inputs                                                     | ✓ SATISFIED | REQUIREMENTS.md line 26 + traceability table row 103. Deletion half: grep gates pass. Enforcement half: gate at `send_keys.rs:87-92`; two gate tests verify both branches (gate fires + gate skipped for single-input). Variant present in `driver_error.rs:154-158` under `#[cfg(feature = "chip-0057")]`. |

**Orphan check:** `grep -E "Phase 04?\.1" .planning/REQUIREMENTS.md` returns only the two registered IDs (SEND-06 + FINGERPRINT-01). No orphaned requirements were expected for this phase that don't appear in plan frontmatter.

### Anti-Patterns Found

Per the user's brief, scanned files modified by phase 04.1.

| File                                                                | Line | Pattern             | Severity | Impact                                                                                                                                                                                                       |
| ------------------------------------------------------------------- | ---- | ------------------- | -------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `crates/chia-sdk-driver/src/silent_payments/scanner.rs`             | (1)  | `#[allow(...)]`     | ℹ️ Info  | Pre-existing Plan 03-03 `#[allow(clippy::similar_names)]` on `scan_from_tweaks` — out of phase 04.1 scope; explicitly documented in 04.1-02-SUMMARY "Carried-forward Observations" + phase summary §6.       |

No new `#[allow]` introduced anywhere in `silent_payments/` or `actions/silent_payment_send.rs`. No `TODO`/`FIXME`/`PLACEHOLDER`/`coming soon` patterns found in phase-modified files. No empty-return stubs in phase-modified files. The "hardcoded empty values" pattern (`Vec::with_capacity(...)`) at `send_keys.rs:98-99` is initialization for a `for ... in ... iter().filter(|i| !i.ephemeral)` loop that populates it (line 100+) — not a stub.

### Human Verification Required

| Item                                                                                                                                                                              | Test                                                                                                                                                                                                                                          | Expected                                                                                                                                                            | Why human                                                                                                          |
| --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------ |
| Rustdoc reads naturally on `Relation::AssertConcurrent` (VALIDATION.md §Manual-Only)                                                                                              | `cargo doc -p chia-sdk-driver --features chip-0057 --no-deps --open` and read the `Relation` enum page                                                                                                                                        | Prose explains atomicity intent, names CHIP-0057 Pass 2b as load-bearing, warns against accidental refactor with pinning-test reference                             | Grep verifies presence (4 hits); quality of prose requires human eye. None of the gates blocks on this; informational only. |

This is the sole human verification surface identified in VALIDATION.md and it does not block phase closure (the structural rustdoc presence is verified mechanically and cargo doc is clean).

### Gaps Summary

**No gaps.** All 7 ROADMAP §04.1 Success Criteria are satisfied with substantive code (not stubs), wired into the call graph (gate is called; pinning test exercises real `Spends`/`prepare` path; rustdoc references load-bearing consumer + drift-guard), and all SC7 gates pass cleanly:

- Workspace build `--all-features` clean (3m 17s).
- Clippy strict on driver +chip-0057 with `-D warnings` clean.
- `cargo fmt --all --check` clean.
- `cargo machete` clean.
- Full workspace test suite: 2419 passed, 0 failed, 0 ignored — exactly matches the SUMMARY's claimed count.
- Phase 1 grep bans (`mod_by_group_order`, `^use sha2::`, `Sha256::digest`) all empty in SP trees.
- Phase 4 Privacy-warning coverage holds across all 5 SP files.
- FINGERPRINT-01 grep gates (`emit_silent_payment_announcements` absent; opcode-60/61 markers absent in `send_keys.rs`) both empty.

Both phase requirements (SEND-06 re-validated under new binding scheme; FINGERPRINT-01 closed for the first time) are registered in `.planning/REQUIREMENTS.md` with their traceability table rows and the FINGERPRINT-01 entry was inserted as a Plan 04.1-02 Task 4 deviation fix (documented in the SUMMARY as Rule 2 missing-critical-functionality).

The phase delivers exactly what the goal promises: multi-input SP transactions now land at the same on-chain shape as ordinary Sage multi-input wallet transactions, bound only by the existing `Relation::AssertConcurrent` opcode-64 cycle (no SP-uniquely-identifiable marker), with a runtime gate preventing accidental degradation back to no-binding mode and a parametric pinning test guarding the cycle-emission shape against future drift.

---

*Verified: 2026-05-17T19:36:06Z*
*Verifier: Claude (gsd-verifier)*
