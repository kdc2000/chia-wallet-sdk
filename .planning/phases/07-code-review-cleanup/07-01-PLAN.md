---
phase: 07-code-review-cleanup
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - crates/chia-sdk-driver/src/silent_payments/scanner.rs
  - crates/chia-sdk-driver/src/silent_payments/input_hash.rs
  - crates/chia-sdk-driver/src/silent_payments/aggregate.rs
  - crates/chia-sdk-driver/src/silent_payments/one_time.rs
  - crates/chia-sdk-driver/src/silent_payments/send_keys.rs
  - crates/chia-sdk-driver/src/silent_payments/mod.rs
  - crates/chia-sdk-driver/src/silent_payments/types.rs
  - crates/chia-sdk-driver/src/silent_payments/protocol.rs
  - crates/chia-sdk-utils/src/silent_payments/keys.rs
  - crates/chia-sdk-utils/src/silent_payments/labels.rs
  - crates/chia-sdk-utils/src/silent_payments/address.rs
  - crates/chia-sdk-bindings/src/silent_payments.rs
  - examples/silent_payment.rs
  - napi/__test__/silent_payments.spec.ts
  - napi/__test__/silent_payments_e2e.spec.ts
  - pyo3/tests/test_silent_payments.py
  - wasm/__test__/silent_payments.spec.ts
autonomous: true
requirements: [CLEANUP-01]
must_haves:
  truths:
    - "Source comments contain zero references to GSD planning artifacts after this plan completes"
    - "Workspace test suite remains green after comment edits"
    - "Surviving comments cite CHIP-0057 / BIP-352 spec sections, describe technical constraints in plain English, or are deleted when redundant"
  artifacts:
    - path: "crates/chia-sdk-driver/src/silent_payments/scanner.rs"
      provides: "Test-vector pin labels rewritten from 'RESEARCH §10a' style to 'CHIP-0057 TV1' style"
    - path: "crates/chia-sdk-bindings/src/silent_payments.rs"
      provides: "Decision-ID prefixes (D-01, D-02, D-03) removed; rationale rewritten or removed"
  key_links:
    - from: "scanner.rs comments"
      to: "CHIP-0057 spec test vectors"
      via: "comment citations"
      pattern: "CHIP-0057|CHIP \\$|BIP-352"
---

<objective>
Strip 64 grep hits across 18 files where source comments reference GSD planning artifacts (CONTEXT.md, RESEARCH.md, Plan NN-MM, D-NN, Pitfall N, Pattern N). Apply the strip-reread-repair hybrid method (per D-04): Pass 1 deletes mechanical label prefixes; Pass 2 reads each surviving comment and either keeps it, rewrites it to cite the CHIP spec / BIP-352 / plain technical constraint, or deletes it when the code is self-evident.

Purpose: Production code comments should reference durable artifacts (CHIP-0057 spec sections, BIP-352, code itself) — not GSD planning workflow artifacts that have no meaning to upstream maintainers reviewing the PR.

Output: Acceptance grep returns 0 hits across the 7-target file set defined in CLEANUP-01.

**Skip e2e.rs**: Per RESEARCH §1.6 Open Q2 recommendation, the 4 hits in `crates/chia-sdk-driver/src/silent_payments/e2e.rs` are NOT touched by this plan — Plan 05 (CLEANUP-04) deletes that entire file, so cleaning its comments here is wasted work.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/07-code-review-cleanup/07-CONTEXT.md
@.planning/phases/07-code-review-cleanup/07-RESEARCH.md
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Strip GSD references from Rust source comments (driver + utils silent_payments modules)</name>
  <files>crates/chia-sdk-driver/src/silent_payments/scanner.rs, crates/chia-sdk-driver/src/silent_payments/input_hash.rs, crates/chia-sdk-driver/src/silent_payments/aggregate.rs, crates/chia-sdk-driver/src/silent_payments/one_time.rs, crates/chia-sdk-driver/src/silent_payments/send_keys.rs, crates/chia-sdk-driver/src/silent_payments/mod.rs, crates/chia-sdk-driver/src/silent_payments/types.rs, crates/chia-sdk-driver/src/silent_payments/protocol.rs, crates/chia-sdk-utils/src/silent_payments/keys.rs, crates/chia-sdk-utils/src/silent_payments/labels.rs, crates/chia-sdk-utils/src/silent_payments/address.rs</files>
  <read_first>
    - crates/chia-sdk-driver/src/silent_payments/scanner.rs (14 hits — heaviest file; test-vector pin labels like `RESEARCH §10a`)
    - crates/chia-sdk-driver/src/silent_payments/input_hash.rs (4 hits — `Plan 04-04`, `04-RESEARCH.md Section 5`, `Plan 04-03`)
    - crates/chia-sdk-driver/src/silent_payments/aggregate.rs (4 hits — `04-RESEARCH.md §11`, `Plan 04-03`, `Pitfall A`, `Pitfall H`)
    - crates/chia-sdk-driver/src/silent_payments/one_time.rs (3 hits)
    - crates/chia-sdk-driver/src/silent_payments/send_keys.rs (2 hits)
    - crates/chia-sdk-driver/src/silent_payments/mod.rs (2 hits — crate-level rustdoc `//!`)
    - crates/chia-sdk-driver/src/silent_payments/types.rs (1 hit — rustdoc)
    - crates/chia-sdk-driver/src/silent_payments/protocol.rs (1 hit — rustdoc)
    - crates/chia-sdk-utils/src/silent_payments/keys.rs (3 hits)
    - crates/chia-sdk-utils/src/silent_payments/labels.rs (2 hits)
    - crates/chia-sdk-utils/src/silent_payments/address.rs (2 hits)
    - .planning/phases/07-code-review-cleanup/07-RESEARCH.md lines 169-258 (CLEANUP-01 section with rewrite playbook examples)
  </read_first>
  <action>
Apply strip-reread-repair (per D-04) to every comment matching the acceptance grep pattern `CONTEXT\.md|RESEARCH(\.md)?|Plan 0[1-6]-|\bD-0[1-9]\b|Pitfall [0-9]|Pattern [0-9]`. The total across these 11 files is 38 hits.

**Rewrite playbook (verbatim from RESEARCH §CLEANUP-01 "Concrete rewrite playbook"):**

Pattern A — test-vector pin label (most common in scanner.rs):
```rust
// BEFORE: // ─── TV1 pinned bytes (RESEARCH §10a) ──────────────────────────────────
// AFTER:  // ─── TV1 pinned bytes (CHIP-0057 test vector 1) ────────────────────────
```

Pattern B — rustdoc label deletion preserving CHIP cite:
```rust
// BEFORE: //! The labeled-detection branch is added in Plan 03-04 (CHIP §RECV-04 labeled
// AFTER:  //! The labeled-detection branch implements CHIP-0057 §RECV-04 labeled
```

Pattern C — restate technical constraint without Plan numbers:
```rust
// BEFORE:
// genuinely unavoidable here: Plan 03-03 hard-locks the function signature
// (the `&PublicKey, &SecretKey` parameter pair triggers clippy::similar_names)
// and Plan 03-04 references both names directly in the labeled-detection
// branch, so a local rebinding would break the locked signature or break
// Plan 03-04's structural expectations.
// AFTER:
// genuinely unavoidable here: the function signature's `&PublicKey, &SecretKey`
// parameter pair triggers clippy::similar_names, and the labeled-detection
// branch below references both names directly — local rebinding would either
// break the signature or break the labeled-branch's structural expectations.
```

**Bar for keep/rewrite/delete (RESEARCH §Pattern 2):**
- **Keep** if the comment reads standalone after label deletion (e.g., `// Per CHIP §459 ...` after removing the `Plan 03-03:` prefix).
- **Rewrite** if the comment was substantively tied to a GSD doc (e.g., "see RESEARCH §10a for pinned bytes") — cite CHIP-0057 spec, BIP-352, or restate the constraint directly.
- **Delete** if the comment was pure scaffolding meta (e.g., `// PLAN 03-04 APPEND POINT`, `// RESEARCH §10c k=1 detection placeholder`) and the surrounding code is self-evident.

**Rustdoc bar is higher than line-comment bar:** Rustdoc (`///`, `//!`) renders to docs.rs and IDE tooltips. Most rustdoc hits in `mod.rs`, `types.rs`, `protocol.rs` MUST BE REWRITTEN — rewrite to cite CHIP-0057 § or describe the constraint plainly. Do NOT just delete the rustdoc unless the API is fully self-documenting.

**Specific high-care sites:**
- `scanner.rs`'s pin labels (lines with `RESEARCH §10a/c/d/e`): rewrite to `CHIP-0057 test vector N` per Pattern A.
- `scanner.rs`'s `#[allow(clippy::similar_names)]` justification block: apply Pattern C verbatim.
- `aggregate.rs` `Pitfall A` / `Pitfall H` mentions: replace with technical descriptions (e.g., "synthetic-vs-raw key boundary" instead of "Pitfall A").
- `input_hash.rs` `04-RESEARCH.md Section 5`: cite `CHIP-0057 §SEND-02` (lex-min coin_id with aggregated synthetic sender pubkey) directly.
- `mod.rs` crate-level `//!` doc: rewrite to describe the module's responsibility in plain English without naming any GSD plan.

After all edits, run the per-file grep:
```bash
grep -E 'CONTEXT\.md|RESEARCH(\.md)?|Plan 0[1-6]-|\bD-0[1-9]\b|Pitfall [0-9]|Pattern [0-9]' \
  crates/chia-sdk-driver/src/silent_payments/scanner.rs \
  crates/chia-sdk-driver/src/silent_payments/input_hash.rs \
  crates/chia-sdk-driver/src/silent_payments/aggregate.rs \
  crates/chia-sdk-driver/src/silent_payments/one_time.rs \
  crates/chia-sdk-driver/src/silent_payments/send_keys.rs \
  crates/chia-sdk-driver/src/silent_payments/mod.rs \
  crates/chia-sdk-driver/src/silent_payments/types.rs \
  crates/chia-sdk-driver/src/silent_payments/protocol.rs \
  crates/chia-sdk-utils/src/silent_payments/keys.rs \
  crates/chia-sdk-utils/src/silent_payments/labels.rs \
  crates/chia-sdk-utils/src/silent_payments/address.rs
```
Must return 0 lines.

**Do NOT touch `crates/chia-sdk-driver/src/silent_payments/e2e.rs`** — Plan 05 (CLEANUP-04) deletes that file entirely. Cleaning its 4 hits is wasted work per RESEARCH Open Q2 recommendation.

After comment edits, run `cargo build --release -p chia-sdk-driver --features chip-0057` and `cargo build --release -p chia-sdk-utils --features chip-0057` to confirm no rustdoc syntax errors. Run `cargo clippy -p chia-sdk-driver --features chip-0057 --all-targets -- -D warnings` and same for chia-sdk-utils.
  </action>
  <verify>
    <automated>cd /home/kdc/chia-wallet-sdk && bash -c 'cnt=$(grep -E "CONTEXT\.md|RESEARCH(\.md)?|Plan 0[1-6]-|\bD-0[1-9]\b|Pitfall [0-9]|Pattern [0-9]" crates/chia-sdk-driver/src/silent_payments/scanner.rs crates/chia-sdk-driver/src/silent_payments/input_hash.rs crates/chia-sdk-driver/src/silent_payments/aggregate.rs crates/chia-sdk-driver/src/silent_payments/one_time.rs crates/chia-sdk-driver/src/silent_payments/send_keys.rs crates/chia-sdk-driver/src/silent_payments/mod.rs crates/chia-sdk-driver/src/silent_payments/types.rs crates/chia-sdk-driver/src/silent_payments/protocol.rs crates/chia-sdk-utils/src/silent_payments/keys.rs crates/chia-sdk-utils/src/silent_payments/labels.rs crates/chia-sdk-utils/src/silent_payments/address.rs 2>/dev/null | wc -l); echo "hits=$cnt"; [ "$cnt" -eq 0 ]' && cargo build --release -p chia-sdk-driver --features chip-0057 && cargo build --release -p chia-sdk-utils --features chip-0057</automated>
  </verify>
  <acceptance_criteria>
    - `grep -E '...' <11 file list>` returns 0 hits
    - `cargo build --release -p chia-sdk-driver --features chip-0057` exits 0
    - `cargo build --release -p chia-sdk-utils --features chip-0057` exits 0
    - `cargo clippy -p chia-sdk-driver --features chip-0057 --all-targets -- -D warnings` exits 0
    - `cargo clippy -p chia-sdk-utils --features chip-0057 --all-targets -- -D warnings` exits 0
    - File `crates/chia-sdk-driver/src/silent_payments/e2e.rs` is UNCHANGED (still has its 4 hits, deletion deferred to Plan 05)
  </acceptance_criteria>
  <done>
    All 11 Rust source files under silent_payments/ (driver + utils) have 0 hits against the CLEANUP-01 grep pattern. Workspace clippy clean under chip-0057. Surviving comments cite CHIP-0057 / BIP-352 / restate constraints. e2e.rs deliberately skipped.
  </done>
</task>

<task type="auto">
  <name>Task 2: Strip GSD references from binding facade and example</name>
  <files>crates/chia-sdk-bindings/src/silent_payments.rs, examples/silent_payment.rs</files>
  <read_first>
    - crates/chia-sdk-bindings/src/silent_payments.rs (9 hits — `CONTEXT.md D-01/D-02/D-03`, `RESEARCH Pattern 4`, `Pitfall 4`)
    - examples/silent_payment.rs (2 hits — line comments)
    - .planning/phases/07-code-review-cleanup/07-RESEARCH.md lines 224-258 (rewrite playbook)
  </read_first>
  <action>
Apply strip-reread-repair to the binding facade and example. Total: 11 hits across 2 files.

**`crates/chia-sdk-bindings/src/silent_payments.rs` (9 hits — Phase 5 decision-ID heavy):**

The hits are mostly references like `D-01`, `D-02`, `D-03`, `Pattern 4`, `Pitfall 4` — these were decision rationale labels from Phase 5 planning. Replace per the following rewriting rules:

- `D-01` (chip-0057 unconditional deps wiring) → remove the label; if context needs it, describe as "chip-0057 enabled-by-default on the bindings crate; no cargo feature on the facade itself".
- `D-02` (zero-field SilentPayments namespace pattern) → remove; describe as "zero-field static-methods class — `SilentPayments` namespace pattern matching `Constants` / `Clvm` in other facades".
- `D-03` (9-type descriptor scope) → remove or rephrase as "covers the 9 public silent-payments types (SilentPaymentAddress, SilentPaymentKeys, ...)".
- `Pattern 4` (bindy interior mutability via Arc<Mutex<_>>) → describe directly: "LabelRegistry uses Arc<Mutex<HashMap>> because bindy dispatches `&self` always — mutating methods need interior mutability".
- `Pitfall 4` (Vec<(K,V)> tuple types not marshaled by bindy) → describe directly: "bindy cannot marshal Vec<(K,V)> tuple types across napi; the workaround uses 2-field wrapper classes (SilentPaymentRegisteredKey/SecretKey) that convert to IndexMap inside the facade".

The bar is "if a maintainer reviewing the PR reads this comment, do they need to know which GSD decision created it (no), or do they need to know the technical constraint (yes)?" Answer is always: rewrite to the technical constraint.

**`examples/silent_payment.rs` (2 hits — line comments):**

Likely "Stage 1/5" / "Stage 5/5" headers that reference internal plan structure, or comments citing `Plan 06-05`. Read the actual hits and apply Pattern B (preserve technical content, drop the Plan label). Examples are docs for users — keep comments user-focused.

After edits, run:
```bash
grep -E 'CONTEXT\.md|RESEARCH(\.md)?|Plan 0[1-6]-|\bD-0[1-9]\b|Pitfall [0-9]|Pattern [0-9]' \
  crates/chia-sdk-bindings/src/silent_payments.rs \
  examples/silent_payment.rs
```
Must return 0 lines.

After comment edits, run `cargo build --release -p chia-sdk-bindings --all-features` and `cargo build --release --examples --all-features` to confirm syntax integrity.
  </action>
  <verify>
    <automated>cd /home/kdc/chia-wallet-sdk && bash -c 'cnt=$(grep -E "CONTEXT\.md|RESEARCH(\.md)?|Plan 0[1-6]-|\bD-0[1-9]\b|Pitfall [0-9]|Pattern [0-9]" crates/chia-sdk-bindings/src/silent_payments.rs examples/silent_payment.rs 2>/dev/null | wc -l); echo "hits=$cnt"; [ "$cnt" -eq 0 ]' && cargo build --release -p chia-sdk-bindings --all-features && cargo build --release --examples --all-features</automated>
  </verify>
  <acceptance_criteria>
    - `grep -E '...' crates/chia-sdk-bindings/src/silent_payments.rs examples/silent_payment.rs` returns 0 hits
    - `cargo build --release -p chia-sdk-bindings --all-features` exits 0
    - `cargo build --release --examples --all-features` exits 0
    - `cargo clippy -p chia-sdk-bindings --all-features --all-targets -- -D warnings` exits 0
  </acceptance_criteria>
  <done>
    Binding facade and example contain 0 hits against the CLEANUP-01 grep pattern. Binding facade still builds for all three binding targets in subsequent verification.
  </done>
</task>

<task type="auto">
  <name>Task 3: Strip GSD references from binding test files (TS + Py + WASM)</name>
  <files>napi/__test__/silent_payments.spec.ts, napi/__test__/silent_payments_e2e.spec.ts, pyo3/tests/test_silent_payments.py, wasm/__test__/silent_payments.spec.ts</files>
  <read_first>
    - napi/__test__/silent_payments.spec.ts (3 hits — TS test comments)
    - napi/__test__/silent_payments_e2e.spec.ts (2 hits — TS test comments)
    - pyo3/tests/test_silent_payments.py (3 hits — Python test comments)
    - wasm/__test__/silent_payments.spec.ts (3 hits — TS test comments)
  </read_first>
  <action>
Apply strip-reread-repair to the 4 binding test files. Total: 11 hits across 4 files. These are mostly Phase 5 / Phase 6 test comment labels.

For each hit:
- **Strip mechanical prefix** (e.g., `// Plan 05-04 SC2: ...` → `// ...` keeping the technical content).
- **Rewrite if reference is substantive** (e.g., `// Per RESEARCH §3b, ...` → cite CHIP-0057 § directly or describe the technical claim).
- **Delete if the comment is pure scaffolding** (e.g., `// Phase 6 D-04 test — ...` standing alone as a section header → either rewrite as a descriptive section header or drop).

The tests themselves are NOT modified. Only the comments inside them.

After edits, run:
```bash
grep -E 'CONTEXT\.md|RESEARCH(\.md)?|Plan 0[1-6]-|\bD-0[1-9]\b|Pitfall [0-9]|Pattern [0-9]' \
  napi/__test__/silent_payments.spec.ts \
  napi/__test__/silent_payments_e2e.spec.ts \
  pyo3/tests/test_silent_payments.py \
  wasm/__test__/silent_payments.spec.ts
```
Must return 0 lines.

**Test integrity check**: Do not change any test assertions, imports, or fixture data. Comment-only edits.

Then run the full acceptance grep across all 18 files (this task is the closer):
```bash
grep -rE 'CONTEXT\.md|RESEARCH(\.md)?|Plan 0[1-6]-|\bD-0[1-9]\b|Pitfall [0-9]|Pattern [0-9]' \
  crates/*/src/silent_payments/ \
  crates/chia-sdk-bindings/src/silent_payments.rs \
  crates/chia-sdk-driver/src/action_system/send_destination.rs \
  examples/silent_payment.rs \
  napi/__test__/silent_payments*.ts \
  pyo3/tests/test_silent_payments.py \
  wasm/__test__/silent_payments.spec.ts
```

This will return exactly the 4 hits in `crates/chia-sdk-driver/src/silent_payments/e2e.rs` (Plan 01 deliberately skips e2e.rs; Plan 05 deletes it). Document this in the commit message: "4 remaining hits in e2e.rs are addressed by Plan 05 (CLEANUP-04) via file deletion".
  </action>
  <verify>
    <automated>cd /home/kdc/chia-wallet-sdk && bash -c 'cnt=$(grep -E "CONTEXT\.md|RESEARCH(\.md)?|Plan 0[1-6]-|\bD-0[1-9]\b|Pitfall [0-9]|Pattern [0-9]" napi/__test__/silent_payments.spec.ts napi/__test__/silent_payments_e2e.spec.ts pyo3/tests/test_silent_payments.py wasm/__test__/silent_payments.spec.ts 2>/dev/null | wc -l); echo "binding-test hits=$cnt"; [ "$cnt" -eq 0 ]' && bash -c 'cnt=$(grep -rE "CONTEXT\.md|RESEARCH(\.md)?|Plan 0[1-6]-|\bD-0[1-9]\b|Pitfall [0-9]|Pattern [0-9]" crates/*/src/silent_payments/ crates/chia-sdk-bindings/src/silent_payments.rs crates/chia-sdk-driver/src/action_system/send_destination.rs examples/silent_payment.rs napi/__test__/silent_payments*.ts pyo3/tests/test_silent_payments.py wasm/__test__/silent_payments.spec.ts 2>/dev/null | grep -v "src/silent_payments/e2e.rs" | wc -l); echo "non-e2e total hits=$cnt"; [ "$cnt" -eq 0 ]'</automated>
  </verify>
  <acceptance_criteria>
    - `grep -E '...' <4 binding-test files>` returns 0 hits
    - Full CLEANUP-01 acceptance grep, EXCLUDING `e2e.rs` from results (via `grep -v`), returns 0 hits
    - `e2e.rs` still has its 4 hits (will be resolved by Plan 05's file deletion)
    - No test assertion or import was modified — diff for these 4 files contains only comment edits
  </acceptance_criteria>
  <done>
    All non-e2e.rs CLEANUP-01 target files have 0 grep hits. Phase 7 acceptance for CLEANUP-01 is fully achieved once Plan 05 deletes e2e.rs.
  </done>
</task>

</tasks>

<verification>
After all 3 tasks complete:

```bash
# Subset grep — excludes e2e.rs since Plan 05 handles it
grep -rE 'CONTEXT\.md|RESEARCH(\.md)?|Plan 0[1-6]-|\bD-0[1-9]\b|Pitfall [0-9]|Pattern [0-9]' \
  crates/*/src/silent_payments/ \
  crates/chia-sdk-bindings/src/silent_payments.rs \
  crates/chia-sdk-driver/src/action_system/send_destination.rs \
  examples/silent_payment.rs \
  napi/__test__/silent_payments*.ts \
  pyo3/tests/test_silent_payments.py \
  wasm/__test__/silent_payments.spec.ts | grep -v "silent_payments/e2e.rs" | wc -l
# Must return 0

# Per-crate builds
cargo build --release -p chia-sdk-driver --features chip-0057
cargo build --release -p chia-sdk-utils --features chip-0057
cargo build --release -p chia-sdk-bindings --all-features

# Workspace test suite (sanity check — comments-only changes should not affect tests)
cargo test --release --workspace --all-features \
  --exclude chia-wallet-sdk-napi --exclude chia-sdk-derive \
  --exclude chia-wallet-sdk-py --exclude chia-wallet-sdk-wasm \
  --exclude chia-sdk-bindings --exclude bindy --exclude bindy-macro
```

The full CLEANUP-01 acceptance grep returning 0 hits is achieved when this plan + Plan 05 (CLEANUP-04) both land.
</verification>

<success_criteria>
- All 17 non-e2e.rs target files have 0 hits against the CLEANUP-01 grep pattern
- Workspace test suite green (no test regressions from comment edits)
- `cargo clippy -p chia-sdk-driver --features chip-0057 --all-targets -- -D warnings` clean
- `cargo clippy -p chia-sdk-utils --features chip-0057 --all-targets -- -D warnings` clean
- `cargo clippy -p chia-sdk-bindings --all-features --all-targets -- -D warnings` clean
- `cargo fmt --all --check` clean
- e2e.rs still has its 4 hits (handover to Plan 05)
</success_criteria>

<output>
After completion, create `.planning/phases/07-code-review-cleanup/07-01-SUMMARY.md` summarizing:
- Files touched and hit counts before/after
- Notable rewrites (test-vector pin labels, scanner.rs allow justification, mod.rs crate-level rustdoc)
- Confirmation that e2e.rs was deliberately skipped
- Acceptance grep result (must be 0 for non-e2e set)
</output>
