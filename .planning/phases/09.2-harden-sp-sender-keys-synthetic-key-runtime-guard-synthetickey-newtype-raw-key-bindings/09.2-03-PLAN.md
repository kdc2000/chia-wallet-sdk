---
phase: 09.2-harden-sp-sender-keys-synthetic-key-runtime-guard-synthetickey-newtype-raw-key-bindings
plan: 03
type: execute
wave: 3
depends_on: ["09.2-01", "09.2-02"]
files_modified:
  - crates/chia-sdk-bindings/src/action_system.rs
  - crates/chia-sdk-bindings/src/silent_payments.rs
  - bindings/action_system.json
  - napi/__test__/silent_payments_e2e.spec.ts
  - pyo3/tests/test_silent_payments.py
  - wasm/__test__/silent_payments.spec.ts
autonomous: true
requirements: [GUARD-03]
must_haves:
  truths:
    - "Each binding target's with_silent_payment_keys accepts RAW PublicKey/SecretKey and synthesizes internally via derive_synthetic"
    - "A raw-key entry against a synthetic-keyed coin produces a detectable SP coin in napi, pyo3, and wasm"
    - "A deliberately-wrong key (e.g. a coin whose ph != curry_tree_hash(derive_synthetic(registered_pk))) surfaces the typed SilentPaymentKeyNotSynthetic error across the FFI boundary"
  artifacts:
    - path: "crates/chia-sdk-bindings/src/action_system.rs"
      provides: "with_silent_payment_keys facade calling derive_synthetic() on raw inputs before building the IndexMaps"
      contains: "derive_synthetic"
    - path: "bindings/action_system.json"
      provides: "Descriptor entry reflecting raw-key inputs for with_silent_payment_keys"
      contains: "with_silent_payment_keys"
  key_links:
    - from: "chia-sdk-bindings with_silent_payment_keys facade"
      to: "chia_puzzle_types::DeriveSynthetic::derive_synthetic on each raw entry"
      via: "internal synthesis before constructing IndexMap<Bytes32, SyntheticPublicKey/SecretKey>"
      pattern: "derive_synthetic\\("
    - from: "binding tests (napi/pyo3/wasm)"
      to: "GUARD-01 typed error on a wrong key"
      via: "assert SilentPaymentKeyNotSynthetic crosses FFI"
      pattern: "SilentPaymentKeyNotSynthetic|KeyNotSynthetic"
---

<objective>
Make each binding target's (`napi`/`pyo3`/`wasm`) `with_silent_payment_keys` accept RAW `PublicKey`/`SecretKey` and synthesize internally via `derive_synthetic()` (default hidden) before driving the Rust core (GUARD-02 newtype can't cross FFI; GUARD-01 from Plan 02 is the backstop). Update the descriptor + any registered-key shape, document the default-hidden assumption (custom-hidden fails loud via GUARD-01), and update the napi/pyo3/wasm tests to (a) build synthetic-keyed coins so the happy path still detects and (b) prove a wrong key surfaces the typed error.

Purpose: Close GUARD-03 so no silent failure remains in ANY language surface. Raw-key ergonomics for FFI callers, with the runtime guard as the universal safety net.
Output: Facade synthesis + descriptor update + three updated binding test files, all green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/09.2-harden-sp-sender-keys-synthetic-key-runtime-guard-synthetickey-newtype-raw-key-bindings/09.2-CONTEXT.md

<interfaces>
<!-- Current facade + descriptor + the helpers the tests need. Extracted from codebase. -->

From crates/chia-sdk-bindings/src/action_system.rs (current facade — line 76; converts wrapper structs to IndexMap<Bytes32, PublicKey/SecretKey> then calls the SDK):
```rust
pub fn with_silent_payment_keys(
    &self,
    synthetic_pks: Vec<crate::SilentPaymentRegisteredKey>,       // entry: { p2_puzzle_hash, public_key }
    secret_keys: Vec<crate::SilentPaymentRegisteredSecretKey>,   // entry: { p2_puzzle_hash, secret_key }
) -> Result<()> {
    let pk_map: IndexMap<Bytes32, PublicKey> = synthetic_pks.into_iter()
        .map(|entry| (entry.p2_puzzle_hash, entry.public_key)).collect();
    let sk_map: IndexMap<Bytes32, SecretKey> = secret_keys.into_iter()
        .map(|entry| (entry.p2_puzzle_hash, entry.secret_key)).collect();
    self.spends.lock().unwrap().with_silent_payment_keys(pk_map, sk_map);   // SDK signature CHANGED by Plan 01
    Ok(())
}
```
NOTE: Plan 01 changed the SDK `Spends::with_silent_payment_keys` to take `IndexMap<Bytes32, SyntheticPublicKey>` / `IndexMap<Bytes32, SyntheticSecretKey>`. This facade therefore no longer compiles as-is and MUST be updated regardless — wrap each entry via `SyntheticPublicKey::from_raw(&entry.public_key)` / `SyntheticSecretKey::from_raw(&entry.secret_key)` (from_raw = derive_synthetic, default hidden). That single change both fixes the build AND delivers GUARD-03's "synthesize internally."

`SyntheticPublicKey` / `SyntheticSecretKey` are re-exported from `chia_sdk_driver::silent_payments` (Plan 01). Import via `chia_sdk_driver::silent_payments::{SyntheticPublicKey, SyntheticSecretKey}` (or `sdk::silent_payments::...`).

From crates/chia-sdk-bindings/src/silent_payments.rs (wrapper structs — lines 357-372; the field names are `public_key`/`secret_key`, KEEP them — they're now raw, update only the rustdoc which currently says "synthetic_pk"/"synthetic_sk"):
```rust
pub struct SilentPaymentRegisteredKey { pub p2_puzzle_hash: Bytes32, pub public_key: PublicKey }
pub struct SilentPaymentRegisteredSecretKey { pub p2_puzzle_hash: Bytes32, pub secret_key: SecretKey }
```

From bindings/action_system.json (lines 69-74 — descriptor entry; arg TYPES are unchanged since the wrapper struct names stay, but verify no drift):
```json
"with_silent_payment_keys": {
  "args": { "synthetic_pks": "Vec<SilentPaymentRegisteredKey>", "secret_keys": "Vec<SilentPaymentRegisteredSecretKey>" }
}
```

From bindings the TESTS need (all already exposed — no new bindings):
- `PublicKey.deriveSynthetic()` / `SecretKey.deriveSynthetic()` (bls.rs / bls.json)
- `standardPuzzleHash(syntheticPk)` -> Bytes32 (puzzle.rs:349 `standard_puzzle_hash`, puzzles.json:578)
- `Simulator.newCoin(puzzleHash, amount)` -> Coin (simulator.rs:48)

CRITICAL test-restructuring reason: the binding tests currently do `sender = sim.bls(...)` (coin curried over the RAW pk) and register `sender.pk`/`sender.sk`. After this facade synthesizes internally, it registers `derive_synthetic(sender.pk)` whose `curry_tree_hash != sender.puzzleHash` — so GUARD-01 (Plan 02) would REJECT the existing happy-path tests. The fix: build the sender coin at the SYNTHETIC puzzle hash so a raw-key registration round-trips: `syntheticPk = sender.pk.deriveSynthetic(); ph = standardPuzzleHash(syntheticPk); coin = sim.newCoin(ph, amount);` then register RAW `sender.pk`/`sender.sk`. The follow-on standard-spend of the detected coin already uses `onetimeSk.deriveSynthetic()` and is unaffected.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Facade synthesizes raw keys internally + descriptor/doc update</name>
  <files>crates/chia-sdk-bindings/src/action_system.rs, crates/chia-sdk-bindings/src/silent_payments.rs, bindings/action_system.json</files>
  <read_first>
    - crates/chia-sdk-bindings/src/action_system.rs lines 1-18 (imports) + lines 62-94 (with_silent_payment_keys facade)
    - crates/chia-sdk-bindings/src/silent_payments.rs lines 350-372 (the two wrapper structs + their rustdoc)
    - bindings/action_system.json lines 69-75 (descriptor entry)
    - crates/chia-sdk-driver/src/silent_payments/send_keys.rs (Plan 01 newtypes — from_raw signature)
  </read_first>
  <action>
    In `crates/chia-sdk-bindings/src/action_system.rs`:
    - Add `use chia_sdk_driver::silent_payments::{SyntheticPublicKey, SyntheticSecretKey};` (the `chia_sdk_driver as sdk` import is at line 10; either form works — match the file's `sdk::` convention if preferred).
    - Rewrite the two `.map(...)` closures so each entry is synthesized: `(entry.p2_puzzle_hash, SyntheticPublicKey::from_raw(&entry.public_key))` and `(entry.p2_puzzle_hash, SyntheticSecretKey::from_raw(&entry.secret_key))`, collecting into `IndexMap<Bytes32, SyntheticPublicKey>` / `IndexMap<Bytes32, SyntheticSecretKey>`. Pass those to the SDK `with_silent_payment_keys`.
    - Update the method rustdoc (lines ~62-75): state the facade accepts RAW public/secret keys and synthesizes the synthetic keys internally via the default hidden puzzle (the same `derive_synthetic` path the standard-spend convention uses). Document the default-hidden assumption: a custom-hidden-puzzle coin will fail LOUD with `DriverError::SilentPaymentKeyNotSynthetic` at finish (GUARD-01), never silently — such callers must construct the synthetic key themselves and the SDK escape hatch is Rust-only.

    In `crates/chia-sdk-bindings/src/silent_payments.rs` (lines 354-372): update the two wrapper struct rustdoc comments to say `(p2_puzzle_hash, raw public_key)` / `(p2_puzzle_hash, raw secret_key)` and note the facade synthesizes internally (was: "synthetic_pk"/"synthetic_sk"). KEEP the struct names (`SilentPaymentRegisteredKey`/`SilentPaymentRegisteredSecretKey`) and field names (`public_key`/`secret_key`) — they remain accurate and renaming would churn the descriptor + all three test files unnecessarily.

    In `bindings/action_system.json`: the arg TYPES are unchanged (the wrapper struct names stay), so verify the `with_silent_payment_keys` entry (lines 69-74) still matches and needs no edit. If the descriptor↔facade drift script exists (`scripts/sp_descriptor_facade_drift.sh`), it must still report zero drift. No JSON change expected; confirm and note in SUMMARY.

    Inline-fix clippy::pedantic. No new deps. No `#[allow]`.
  </action>
  <acceptance_criteria>
    - `grep -n "SyntheticPublicKey::from_raw" crates/chia-sdk-bindings/src/action_system.rs` returns a match
    - `grep -n "SyntheticSecretKey::from_raw" crates/chia-sdk-bindings/src/action_system.rs` returns a match
    - `grep -n "raw public_key\|raw secret_key\|raw .*key" crates/chia-sdk-bindings/src/silent_payments.rs` returns a match (doc updated)
    - `cargo build --release -p chia-sdk-bindings --all-features` exits 0
    - `cargo build --release -p chia-sdk-bindings -F napi` AND `-F wasm` AND `-F pyo3` each exit 0
    - `cargo clippy -p chia-sdk-bindings --all-features --all-targets -- -D warnings` exits 0
  </acceptance_criteria>
  <verify>
    <automated>cargo build --release -p chia-sdk-bindings --all-features && cargo build --release -p chia-sdk-bindings -F napi && cargo build --release -p chia-sdk-bindings -F wasm && cargo build --release -p chia-sdk-bindings -F pyo3</automated>
  </verify>
  <done>Facade synthesizes both raw maps via from_raw before driving the SDK; rustdoc documents raw-in + default-hidden + loud-failure-on-custom-hidden; wrapper struct docs say raw; descriptor verified driftless; all four binding-crate builds + clippy -D warnings green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Update napi/pyo3/wasm tests — raw-key happy path detects + wrong key surfaces typed error</name>
  <files>napi/__test__/silent_payments_e2e.spec.ts, pyo3/tests/test_silent_payments.py, wasm/__test__/silent_payments.spec.ts</files>
  <read_first>
    - napi/__test__/silent_payments_e2e.spec.ts lines 30-75 (sender = sim.bls; withSilentPaymentKeys registration; the e2e detect+spend flow)
    - pyo3/tests/test_silent_payments.py lines 40-130 (same flow in Python)
    - wasm/__test__/silent_payments.spec.ts lines 50-135 (same flow in TS/wasm)
    - crates/chia-sdk-bindings/src/puzzle.rs lines 349-351 (standard_puzzle_hash) + crates/chia-sdk-bindings/src/simulator.rs lines 48-49 (new_coin)
  </read_first>
  <behavior>
    - napi/pyo3/wasm happy path: build the sender coin at the SYNTHETIC puzzle hash (standardPuzzleHash(sender.pk.deriveSynthetic())) via sim.newCoin, register RAW sender.pk/sender.sk, prepare+spend, farm, build TweakData, scan -> recipient DETECTS the coin (assert detections length >= 1)
    - napi/pyo3/wasm negative: register a raw key against a coin whose synthetic ph does NOT match (e.g. keep the old sim.bls() coin curried over the RAW pk, register sender.pk/sender.sk so the facade synthesizes derive_synthetic(pk) whose curry_tree_hash != bls puzzleHash) -> prepare() throws an error whose message contains the SilentPaymentKeyNotSynthetic signal ("not the synthetic key" / typed variant), and NO spend bundle is produced
  </behavior>
  <action>
    For EACH of the three test files, two changes:

    (1) HAPPY-PATH FIX (required for the existing e2e to keep passing post-facade-synthesis). Replace the sender-coin construction so a raw-key registration round-trips through the new internal synthesis:
    - napi/wasm (TS): `const syntheticPk = sender.pk.deriveSynthetic(); const senderPh = standardPuzzleHash(syntheticPk); const senderCoin = sim.newCoin(senderPh, 1000n);` then `spends = new Spends(clvm, senderPh); spends.addXch(senderCoin);` and register RAW `sender.pk`/`sender.sk` (unchanged registration line). The follow-on spend of the detected coin already uses `onetimeSk.deriveSynthetic()` — unaffected. Update any `sim.bls(...)` height/parent assumptions accordingly (use `sim.newCoin` directly; `sender = sim.bls(...)` may still be called just to get a key pair, then ignore its `.coin`).
    - pyo3 (Python): `synthetic_pk = sender.pk.derive_synthetic(); sender_ph = standard_puzzle_hash(synthetic_pk); sender_coin = sim.new_coin(sender_ph, 1000)` then `spends = Spends(clvm, sender_ph); spends.add_xch(sender_coin)`; register RAW `sender.pk`/`sender.sk`.
    Import `standardPuzzleHash`/`standard_puzzle_hash` from the binding package alongside the existing imports. Assert the recipient detects >= 1 coin (keep the existing scan assertions).

    (2) NEW NEGATIVE TEST per file: a fresh test (`KeyNotSynthetic raw-key surfaces typed error` / `test_raw_key_not_synthetic_errors`) that:
    - builds a coin curried over the RAW pk (the old `sim.bls(...)` coin works directly — its `puzzleHash` is `curry_tree_hash(raw_pk)`),
    - `spends = Spends(clvm, sender.puzzleHash); spends.addXch(sender.coin)`,
    - one `Action.send(Id.xch(), SendDestination.silentPayment(recipientAddress), 100, undefined)`,
    - `spends.withSilentPaymentKeys([RegisteredKey(sender.puzzleHash, sender.pk)], [RegisteredSecretKey(sender.puzzleHash, sender.sk)])` (raw),
    - assert that calling `spends.prepare(deltas, ...)` (or the apply+prepare flow the file uses) THROWS, and the thrown error's message includes a SilentPaymentKeyNotSynthetic marker. Because the facade synthesizes `derive_synthetic(sender.pk)`, whose `curry_tree_hash != sender.puzzleHash`, GUARD-01 fires inside `prepare`. Use the framework's throw-assertion: AVA `t.throws(() => ..., { message: /not the synthetic key|KeyNotSynthetic/i })` for napi/wasm; `with pytest.raises(BaseException, match="not the synthetic key"):` for pyo3 (or assert on the caught exception's str). Single-input case — proves the guard fires across FFI for one input.

    Keep deterministic recipient mnemonics (TV1_MNEMONIC) already in each file. No production-code changes here.
  </action>
  <acceptance_criteria>
    - napi: `cd napi && pnpm build && pnpm test` exits 0 (existing e2e detects via raw-key + new negative test asserts the typed error)
    - wasm: `cd wasm && pnpm test` exits 0
    - pyo3: `cd pyo3 && maturin develop && pytest` exits 0
    - `grep -ri "not the synthetic key\|KeyNotSynthetic\|raw.key.*not.synthetic" napi/__test__/silent_payments_e2e.spec.ts pyo3/tests/test_silent_payments.py wasm/__test__/silent_payments.spec.ts` returns a match in each file
    - `grep -ri "standardPuzzleHash\|standard_puzzle_hash" napi/__test__/silent_payments_e2e.spec.ts pyo3/tests/test_silent_payments.py wasm/__test__/silent_payments.spec.ts` returns a match in each file (synthetic-coin construction)
  </acceptance_criteria>
  <verify>
    <automated>cd /home/kdc/chia-wallet-sdk/napi && pnpm build && pnpm test</automated>
  </verify>
  <done>All three binding suites green; each happy-path e2e now builds a synthetic-keyed coin so raw-key registration detects; each file has a single-input negative test asserting SilentPaymentKeyNotSynthetic crosses the FFI boundary with no bundle produced.</done>
</task>

</tasks>

<verification>
- napi (`pnpm build && pnpm test`), wasm (`pnpm test`), pyo3 (`maturin develop && pytest`) all green.
- `cargo build --release -p chia-sdk-bindings -F napi` / `-F wasm` / `-F pyo3` / `--all-features` all green.
- `cargo clippy -p chia-sdk-bindings --all-features --all-targets -- -D warnings` clean.
- Descriptor↔facade drift script (if present) reports zero drift.
- ZERO new workspace deps; ZERO new `#[allow]`.
</verification>

<success_criteria>
GUARD-03 closed: all three FFI targets accept raw keys and synthesize internally; the raw-key happy path detects a coin in napi/pyo3/wasm; a wrong key surfaces the typed SilentPaymentKeyNotSynthetic error across the FFI boundary (single-input proven); no silent failure remains in any language surface.
</success_criteria>

<output>
After completion, create `.planning/phases/09.2-harden-sp-sender-keys-synthetic-key-runtime-guard-synthetickey-newtype-raw-key-bindings/09.2-03-SUMMARY.md`
</output>
