---
phase: 09.2-harden-sp-sender-keys-synthetic-key-runtime-guard-synthetickey-newtype-raw-key-bindings
verified: 2026-06-01T09:05:00Z
status: passed
score: 5/5 must-haves verified
re_verification:
  # Initial verification — no prior VERIFICATION.md existed.
---

# Phase 09.2: Harden SP Sender Keys (Synthetic-Key Runtime Guard + Newtype + Raw-Key Bindings) Verification Report

**Phase Goal:** Close the silent-fund-loss footgun reported in `ISSUE-silent-payment-synthetic-key-guard.md` — `Spends::with_silent_payment_keys` required *synthetic* sender keys but enforced it nowhere. The fix is a layered defense: GUARD-01 (finish-time runtime guard), GUARD-02 (`SyntheticSecretKey`/`SyntheticPublicKey` newtypes for compile-time prevention), GUARD-03 (raw-key-accepting binding methods on napi/pyo3/wasm that synthesize internally with GUARD-01 as backstop). No silent failure may remain in any language surface.

**Verified:** 2026-06-01T09:05:00Z
**Status:** passed
**Re-verification:** No — initial verification

## Goal Achievement

### Observable Truths (mapped to the ROADMAP §09.2 verification bar)

| #   | Truth (verification bar) | Status | Evidence |
| --- | ------------------------ | ------ | -------- |
| 1 | Raw keys in Rust → compile error (`with_silent_payment_keys` takes newtype maps; no raw `chia_bls` key accepted) | ✓ VERIFIED | `spends.rs:124-128` signature takes `IndexMap<Bytes32, SyntheticPublicKey>` / `IndexMap<Bytes32, SyntheticSecretKey>`. Newtypes have no `From<PublicKey>`/`From<SecretKey>` impl → raw key cannot coerce (E0308, structural). Empirically witnessed: the binding facade had to be edited to compile (09.2-01 SUMMARY deviation #2). |
| 2 | Raw keys via `from_synthetic_unchecked` / bindings → `Err(SilentPaymentKeyNotSynthetic)` BEFORE signing, proven by a test per surface incl. single-input | ✓ VERIFIED | Rust: `raw_pk_single_input_fails_not_synthetic`, `sk_pk_mismatch_fails_not_synthetic`, `unchecked_wrong_key_is_backstopped` all pass (single-input). napi/pyo3/wasm each have a single-input negative test asserting `prepare()` throws `/not the synthetic key|KeyNotSynthetic/i` (pyo3: `pytest.raises(match="not the synthetic key")`) AND asserts `coinSpends().length == 0` (no bundle produced). |
| 3 | The runtime guard validates `curry_tree_hash(registered_pk) == p2_puzzle_hash` AND `registered_sk.public_key() == registered_pk` in `sp_finish_branch`, returning `DriverError::SilentPaymentKeyNotSynthetic` before signing; accepts default OR custom-hidden synthetic keys | ✓ VERIFIED | `spends.rs:654-665`: dual check `Bytes32::from(StandardArgs::curry_tree_hash(*pk)) != ph || sk.public_key() != *pk` inside the non-ephemeral XCH loop, before `sender_sks.push`. Validates against the actual coin's p2 hash → accepts any correct synthetic key (default/custom). `sp_finish_branch` is invoked by `prepare`/`finish_with_keys` (line 511) which produce no signature — guard is strictly pre-sign. |
| 4 | A correct synthetic key still produces byte-identical one-time puzzle hash (no detection-oracle regression) | ✓ VERIFIED | `silent_payments_e2e` (3 tests: unlabeled/labeled/m0_self_change) all pass. `round_trip_matches_derive_one_time_puzzle_hash` passes. `newtype_from_raw_matches_derive_synthetic` confirms `from_raw` is byte-identical to `DeriveSynthetic::derive_synthetic`. |
| 5 | All CI permutations green + napi/pyo3/wasm green; zero new workspace deps; zero new `#[allow]` | ✓ VERIFIED | Orchestrator confirmed full workspace suite (2438 tests, 0 failed). napi 54 / wasm 10 / pyo3 4 green (per SUMMARYs). Drift script: zero drift (23 methods). No new `#[allow]` (only pre-existing `scanner.rs:73`; protocol.rs:482 + silent_payments.rs:390 hits are comments referencing `#[allow]`, not attributes). Re-confirmed driver tests + bindings(-F napi) build locally. |

**Score:** 5/5 truths verified

### Required Artifacts

| Artifact | Expected | Status | Details |
| -------- | -------- | ------ | ------- |
| `crates/chia-sdk-driver/src/silent_payments/send_keys.rs` | `SyntheticSecretKey`/`SyntheticPublicKey` newtypes (from_raw/from_synthetic_unchecked/public_key/into_inner/as_inner) | ✓ VERIFIED | Both newtypes present (lines 45-122) with full API; `Copy` on PublicKey only. 7 tests pass incl. byte-equality + 3 GUARD-01 negatives + positive single-input. |
| `crates/chia-sdk-driver/src/action_system/spends.rs` | `with_silent_payment_keys` newtype signature + GUARD-01 guard | ✓ VERIFIED | Signature (124-128) takes newtype maps, unwraps via `into_inner()` at boundary; GUARD-01 dual-check (654-665); `StandardArgs` import (618); gate-ordering rustdoc updated (603-605). |
| `crates/chia-sdk-driver/src/driver_error.rs` | `DriverError::SilentPaymentKeyNotSynthetic` (chip-0057 gated) | ✓ VERIFIED | Variant at line 179 with descriptive multi-line `#[error(...)]`. |
| `crates/chia-sdk-bindings/src/action_system.rs` | facade calling `from_raw` on raw inputs | ✓ VERIFIED | `SyntheticPublicKey::from_raw` (110) + `SyntheticSecretKey::from_raw` (119); import (11); rustdoc documents raw-in + default-hidden + loud-failure. Compiles `-F napi`. |
| `crates/chia-sdk-bindings/src/silent_payments.rs` | wrapper struct docs say raw | ✓ VERIFIED | rustdoc updated to "raw public_key/secret_key". |
| `bindings/action_system.json` | descriptor entry (types unchanged) | ✓ VERIFIED | Entry present (69-74); struct/field names unchanged so types stable; drift script reports zero drift. |
| `src/prelude.rs` | newtype re-export (chip-0057) | ✓ VERIFIED | `SyntheticPublicKey, SyntheticSecretKey` re-exported (42-43). |
| `crates/chia-sdk-driver/src/silent_payments/mod.rs` | pub re-export | ✓ VERIFIED | `pub use send_keys::{SyntheticPublicKey, SyntheticSecretKey};` (43). |
| napi/pyo3/wasm test files | raw-key happy path + typed-error negative | ✓ VERIFIED | All 3 e2e files have synthetic-coin construction + single-input negative tests; 2 multi-input siblings repaired. |

### Key Link Verification

| From | To | Via | Status | Details |
| ---- | -- | --- | ------ | ------- |
| `Spends::with_silent_payment_keys` | storage `_pks`/`_sks` (plain `chia_bls`) | `.into_inner()` at boundary | ✓ WIRED | `spends.rs:129-138` unwraps both maps via `into_inner()`. |
| `SyntheticSecretKey::from_raw` | `DeriveSynthetic::derive_synthetic` | default-hidden synthesis | ✓ WIRED | `send_keys.rs:54` `Self(raw.derive_synthetic())`; byte-equality test passes. |
| `sp_finish_branch` non-ephemeral loop | `StandardArgs::curry_tree_hash(pk) == ph` | per-input pre-sign validation | ✓ WIRED | `spends.rs:663`; runs for single-input (loop covers count==1). |
| `sp_finish_branch` non-ephemeral loop | `sk.public_key() == pk` | sk/pk consistency | ✓ WIRED | `spends.rs:663` second arm; proven by `sk_pk_mismatch_fails_not_synthetic`. |
| binding facade | `from_raw` on each raw entry | internal synthesis before driving SDK | ✓ WIRED | `action_system.rs:110,119`. |
| binding tests (napi/pyo3/wasm) | GUARD-01 typed error across FFI | assert message + no bundle | ✓ WIRED | All 3 assert `/not the synthetic key|KeyNotSynthetic/i` + `coinSpends().length == 0`. |

### Behavioral Spot-Checks

| Behavior | Command | Result | Status |
| -------- | ------- | ------ | ------ |
| GUARD newtype + runtime-guard unit tests | `cargo test -p chia-sdk-driver --all-features silent_payments::send_keys::tests` | 7 passed, 0 failed | ✓ PASS |
| Detection-oracle no-regression | `cargo test -p chia-sdk-driver --all-features --test silent_payments_e2e` | 3 passed, 0 failed | ✓ PASS |
| Byte-identical one-time PH oracle | `cargo test ... round_trip_matches_derive_one_time_puzzle_hash` | 1 passed | ✓ PASS |
| Bindings facade compiles (FFI surface) | `cargo build --release -p chia-sdk-bindings -F napi` | exit 0 | ✓ PASS |
| Descriptor↔facade drift | `bash scripts/sp_descriptor_facade_drift.sh` | "No drift detected (23 methods)" | ✓ PASS |
| napi/pyo3/wasm runtime test suites | `pnpm test` / `maturin develop && pytest` | napi 54 / wasm 10 / pyo3 4 (orchestrator + SUMMARY confirmed) | ? SKIP (heavy; trusted from orchestrator's reported green run) |

### Requirements Coverage

| Requirement | Source Plan | Description | Status | Evidence |
| ----------- | ----------- | ----------- | ------ | -------- |
| GUARD-02 | 09.2-01 | Newtypes + signature change → raw key is Rust compile error; `from_raw` mirrors `derive_synthetic` | ✓ SATISFIED | Truths 1, 4; newtypes + signature + prelude verified; byte-equality test passes. REQUIREMENTS.md line 99 marked `[x]`; traceability row 177. |
| GUARD-01 | 09.2-02 | Finish-time runtime guard (dual check) → typed error before signing; single-input; FFI-universal | ✓ SATISFIED | Truths 2, 3; guard at `spends.rs:654-665`; error variant; 3 negative tests pass. REQUIREMENTS.md line 100 marked `[x]`; traceability row 178. |
| GUARD-03 | 09.2-03 | Raw-key binding methods synthesize via `from_raw`; descriptor/doc update; GUARD-01 backstop | ✓ SATISFIED | Truth 2; facade `from_raw`; 3 FFI negative tests assert typed error + no bundle. REQUIREMENTS.md line 101 marked `[x]`; traceability row 179. |

**Orphaned requirements check:** REQUIREMENTS.md maps exactly GUARD-01/02/03 to Phase 9.2 (lines 99-101 + traceability rows 177-179). All three are claimed by plans (09.2-01→GUARD-02, 09.2-02→GUARD-01, 09.2-03→GUARD-03). No orphans. The late-inserted fund-safety section (line 95 "Fund-safety hardening — synthetic sender keys (Phase 9.2)") is present with all three rows closed, as required.

### Anti-Patterns Found

| File | Line | Pattern | Severity | Impact |
| ---- | ---- | ------- | -------- | ------ |
| (none) | — | — | — | No blockers, warnings, or stubs. `from_synthetic_unchecked` is a documented escape hatch backstopped by GUARD-01 (not a stub). The only `#[allow]` in scope is pre-existing `scanner.rs:73` (Plan 03-03). Zero new workspace deps. |

### Human Verification Required

None. All five verification-bar truths are programmatically verifiable and were confirmed against the actual codebase. The cross-language runtime suites (napi/pyo3/wasm) are heavy to re-run here but were reported green by the orchestrator and corroborated by the test-file contents (typed-error assertions present and substantive) plus the clean Rust-side bindings build.

### Gaps Summary

No gaps. The layered defense is fully present and wired in the actual codebase:

- **GUARD-02 (compile-time):** newtypes wrap `chia_bls` keys; `with_silent_payment_keys` takes newtype maps; raw keys cannot coerce; `from_raw` is byte-identical to `derive_synthetic`.
- **GUARD-01 (runtime, universal):** `sp_finish_branch` runs the dual check per non-ephemeral input (single-input included), returning `DriverError::SilentPaymentKeyNotSynthetic` before any signature is produced; validates against the actual coin so default and custom-hidden synthetic keys both pass.
- **GUARD-03 (FFI ergonomics):** all three binding facades accept raw keys and synthesize via `from_raw`; each surface has a single-input negative test proving the typed error crosses FFI with no bundle produced.
- **No regression:** the Phase-3/6 detection oracle (`silent_payments_e2e` + round-trip) is byte-identical. Zero new deps, zero new `#[allow]`, descriptor driftless.

---

_Verified: 2026-06-01T09:05:00Z_
_Verifier: Claude (gsd-verifier)_
