---
phase: 9
slug: real-block-tweakdata-bridge-python-relation-binding
status: draft
nyquist_compliant: false
wave_0_complete: false
created: 2026-05-29
---

# Phase 9 — Validation Strategy

> Per-phase validation contract for feedback sampling during execution.

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | cargo test (Rust 1.90.0) + AVA (TS, napi+wasm) + pytest (Python, pyo3) |
| **Config file** | `Cargo.toml` (workspace), `napi/package.json` (AVA), `wasm/package.json` (AVA), `pyo3/pyproject.toml` (pytest) |
| **Quick run command (per change)** | `cargo test --release -p chia-sdk-driver --all-features silent_payments::block_tweak_data` |
| **Full suite command (Rust)** | `cargo test --release --workspace --all-features --exclude chia-wallet-sdk-napi --exclude chia-sdk-derive --exclude chia-wallet-sdk-py --exclude chia-wallet-sdk-wasm --exclude chia-sdk-bindings --exclude bindy --exclude bindy-macro` |
| **Full suite command (napi)** | `cd napi && pnpm install && pnpm build && pnpm test` |
| **Full suite command (wasm)** | `cd wasm && pnpm install && pnpm test` |
| **Full suite command (pyo3)** | `cd pyo3 && maturin develop && pytest` |
| **Lint gates** | `cargo fmt --all -- --files-with-diff --check`, `cargo clippy --workspace --all-features --all-targets`, `cargo machete` |
| **Estimated runtime (Rust quick)** | ~10 seconds |
| **Estimated runtime (Rust full)** | ~180 seconds |
| **Estimated runtime (each binding suite)** | ~90 seconds (napi/wasm), ~60 seconds (pyo3) |

---

## Sampling Rate

- **After every task commit:** Run `cargo test --release -p chia-sdk-driver --all-features silent_payments` + `cargo clippy --workspace --all-features --all-targets` on touched crates
- **After Wave 1 completes:** Run full Rust workspace suite + clippy gate
- **After Wave 2 completes:** Run all three binding suites (napi, wasm, pyo3)
- **Before `/gsd:verify-work`:** All four suites green + `cargo fmt --check` + `cargo machete` green
- **Max feedback latency (Rust):** 180 seconds
- **Max feedback latency (binding parity):** 240 seconds per target

---

## Per-Task Verification Map

> Placeholder rows — planner fills in concrete `{N}-{plan}-{task}` IDs once PLAN.md files exist. Each row binds a task to either an automated command or a Wave 0 file-creation dependency.

| Task ID | Plan | Wave | Requirement | Test Type | Automated Command | File Exists | Status |
|---------|------|------|-------------|-----------|-------------------|-------------|--------|
| 9-01-XX | 01 (BRIDGE-01) | 1 | BRIDGE-01 | unit | `cargo test --release -p chia-sdk-driver --all-features silent_payments::block_tweak_data::tests::test_multi_input_round_trip` | ❌ W0 (new file `crates/chia-sdk-driver/src/silent_payments/block_tweak_data.rs` w/ `mod tests {}`) | ⬜ pending |
| 9-01-XX | 01 (BRIDGE-01) | 1 | BRIDGE-01 | unit | `cargo test --release -p chia-sdk-driver --all-features silent_payments::block_tweak_data::tests::test_pass_2b_pollution_resistance` | ❌ W0 | ⬜ pending |
| 9-01-XX | 01 (BRIDGE-01) | 1 | BRIDGE-01 | unit | `cargo test --release -p chia-sdk-driver --all-features silent_payments::block_tweak_data::tests::test_non_standard_puzzle_skip` | ❌ W0 | ⬜ pending |
| 9-01-XX | 01 (BRIDGE-01) | 1 | BRIDGE-01 | unit | `cargo test --release -p chia-sdk-driver --all-features silent_payments::block_tweak_data::tests::test_identity_element_guard` | ❌ W0 | ⬜ pending |
| 9-01-XX | 01 (BRIDGE-01) | 1 | BRIDGE-01 | unit | `cargo test --release -p chia-sdk-driver --all-features silent_payments::block_tweak_data::tests::test_empty_block` | ❌ W0 | ⬜ pending |
| 9-02-XX | 02 (BRIDGE-02) | 2 | BRIDGE-02 | regression | `cargo test --release -p chia-sdk-driver --all-features --test silent_payments_e2e` (existing Phase 6 oracle — `test_simulator_e2e_unlabeled`, `test_simulator_e2e_labeled`, `test_simulator_e2e_m0_self_change` must stay byte-identical) | ✅ existing | ⬜ pending |
| 9-03-XX | 03 (BRIDGE-03) | 1 | BRIDGE-03 | build+lint | `cargo build --release -p chia-sdk-bindings -F napi && cargo build --release -p chia-sdk-bindings -F wasm && cargo build --release -p chia-sdk-bindings -F pyo3` | ✅ existing crate | ⬜ pending |
| 9-04-XX | 04 (BRIDGE-04) | 1 | BRIDGE-04 | regression | `cd pyo3 && maturin develop && pytest tests/test_silent_payments.py::test_unlabeled_e2e` (existing single-input E2E — must pass without signature edits to test code) | ✅ existing | ⬜ pending |
| 9-05-XX | 05 (BRIDGE-05) | 2 | BRIDGE-05 | binding-surface | After binding rebuild, descriptor presence check: `grep -q tweakDataFromBlockSpends napi/index.d.ts && grep -q tweak_data_from_block_spends pyo3/python/*.pyi` | ❌ W0 (regenerated by build) | ⬜ pending |
| 9-06-XX | 06 (BRIDGE-06) | 2 | BRIDGE-06 | e2e (3 targets) | `cd napi && pnpm test --match "*multi_input*"` AND `cd wasm && pnpm test --match "*multi_input*"` AND `cd pyo3 && pytest tests/test_silent_payments.py::test_multi_input_e2e` | ❌ W0 (3 new test files) | ⬜ pending |
| 9-06-XX | 06 (BRIDGE-06) | 2 | BRIDGE-06 | example | `cargo run --release --example silent_payment --all-features` (existing example, extended with multi-input section per D-12 — must exit 0) | ✅ existing | ⬜ pending |

*Status: ⬜ pending · ✅ green · ❌ red · ⚠️ flaky*

---

## Wave 0 Requirements

Wave 0 = file/scaffold creations that must exist before any test in the verification map can run.

- [ ] `crates/chia-sdk-driver/src/silent_payments/block_tweak_data.rs` — new sibling file containing `tweak_data_from_block_spends` + inline `#[cfg(test)] mod tests {}` shell (BRIDGE-01)
- [ ] `crates/chia-sdk-driver/src/silent_payments.rs` (or `mod.rs`) — `pub mod block_tweak_data;` + re-export of `tweak_data_from_block_spends` (BRIDGE-01)
- [ ] `napi/__test__/silent_payments_multi_input.spec.ts` — new test file with AVA test stubs (BRIDGE-06)
- [ ] `pyo3/tests/test_silent_payments.py::test_multi_input_e2e` — new pytest stub in existing file (BRIDGE-06)
- [ ] `wasm/__test__/silent_payments_multi_input.spec.ts` — new test file with AVA test stubs (BRIDGE-06)
- [ ] `bindings/action_system.json` — `Relation` descriptor entry (BRIDGE-03)
- [ ] `bindings/silent_payments.json` — `SilentPayments.tweakDataFromBlockSpends` entry (BRIDGE-05)
- [ ] **(Research-flagged optional, fold into BRIDGE-06):** `bindings/simulator.json` — `Simulator.block_spends` + `Simulator.block_outputs` descriptor entries if BIND-06's tests need them reachable (per RESEARCH.md Finding 4)

*Frameworks already installed: cargo test (workspace), AVA (napi+wasm pnpm), pytest (pyo3 maturin). No new framework installs needed.*

---

## Manual-Only Verifications

| Behavior | Requirement | Why Manual | Test Instructions |
|----------|-------------|------------|-------------------|
| Generated Python type stub readability | BRIDGE-03, BRIDGE-04 | `.pyi` content correctness is hard to assert mechanically beyond presence checks | After `maturin develop`, open `pyo3/python/chia_wallet_sdk/*.pyi` and visually confirm `class Relation:` with `none()`, `assert_concurrent()`, `is_none()`, `is_assert_concurrent()`, `equals()` factory/methods; confirm `Spends.prepare` shows `relation: Optional[Relation] = None` |
| Generated TypeScript `.d.ts` readability | BRIDGE-03, BRIDGE-04 | Same as above for napi | After `pnpm build`, open `napi/index.d.ts` and visually confirm `class Relation { static none(): Relation; static assertConcurrent(): Relation; ... }` and that `Spends.prepare`'s second arg is `relation?: Relation` |
| Multi-input SP scan output equivalence vs. simulator helper | BRIDGE-02 | Byte-equality already asserted by existing Phase 6 e2e tests, but a sanity diff on log output is faster for spotting subtle drift | After BRIDGE-02 lands, run `cargo test --release -p chia-sdk-driver --all-features --test silent_payments_e2e -- --nocapture` and confirm tweak_point hex strings unchanged vs. pre-refactor baseline (committed in git log) |

*Everything else has automated verification via `cargo test`, AVA, or pytest.*

---

## Validation Sign-Off

- [ ] All tasks have `<automated>` verify or Wave 0 dependencies
- [ ] Sampling continuity: no 3 consecutive tasks without automated verify (all 6 BRIDGE-* rows have automated commands)
- [ ] Wave 0 covers all MISSING references (7 file/descriptor creations enumerated)
- [ ] No watch-mode flags (all commands single-shot)
- [ ] Feedback latency < 240s per binding target, < 180s for Rust
- [ ] `nyquist_compliant: true` set in frontmatter once planner fills concrete task IDs and confirms each row's automated command resolves

**Approval:** pending
