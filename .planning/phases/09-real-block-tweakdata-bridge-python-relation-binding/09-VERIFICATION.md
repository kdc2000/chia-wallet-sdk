---
phase: 09-real-block-tweakdata-bridge-python-relation-binding
verified: 2026-05-29T20:00:00Z
status: passed
score: 6/6 must-haves verified
re_verification:
  previous_status: none
  is_re_verification: false
---

# Phase 9: Real-block TweakData bridge + Python Relation binding — Verification Report

**Phase Goal:** Two surgical additions to the v1 CHIP-0057 surface — (1) extract pure core of `chia-sdk-test::silent_payments::tweak_data_from_simulator_block` into `tweak_data_from_block_spends(coin_spends, additions)` helper in `chia-sdk-driver` with proper Pass 2a + Pass 2b SCC grouping, refactor sim helper to delegate, expose via bindings; (2) bind `chia_sdk_driver::Relation` to pyo3 + napi + wasm via opaque-handle + factories pattern, extend `Spends::prepare` to accept `Option<Relation>` so multi-input SP sends can pass `Relation::AssertConcurrent` from the binding layer.

**Verified:** 2026-05-29
**Status:** passed
**Re-verification:** No — initial verification

## Goal Achievement

### Observable Truths (Success Criteria from ROADMAP)

| #  | Truth (Success Criterion)                                                                                                                                                                       | Status     | Evidence                                                                                                                                                                                                  |
| -- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 1  | BRIDGE-01: `tweak_data_from_block_spends` helper exists with full Pass 2a + Pass 2b SCC grouping + iterative Tarjan + 6 inline tests (incl. Pass 2b pollution attack)                          | ✓ VERIFIED | File `crates/chia-sdk-driver/src/silent_payments/block_tweak_data.rs` (527 lines); helper signature matches; iterative Tarjan present (line 220); all 6 named tests pass: `cargo test ...block_tweak_data::tests` returns `6 passed; 0 failed`. |
| 2  | BRIDGE-02: simulator helper delegates to canonical helper; all 3 Phase 6 e2e tests pass byte-identically                                                                                       | ✓ VERIFIED | `crates/chia-sdk-test/src/silent_payments/tweak_data.rs` shrunk to 65 lines; contains `tweak_data_from_block_spends` delegate call; all 3 Phase 6 oracle tests pass (`test_simulator_e2e_unlabeled`, `_labeled`, `_m0_self_change`); 2 inline simulator-helper tests pass. |
| 3  | BRIDGE-03: `Relation` opaque-handle binding lands in `chia-sdk-bindings::action_system` with 5 methods; descriptor entry in `bindings/action_system.json`; all 3 binding targets build cleanly | ✓ VERIFIED | `pub struct Relation(pub(crate) sdk::Relation)` at `action_system.rs:558`; all 5 methods present (none, assert_concurrent, is_none, is_assert_concurrent, equals); `"Relation":` descriptor at `bindings/action_system.json:293`; `cargo build -p chia-sdk-bindings --all-features` clean. |
| 4  | BRIDGE-04: `Spends.prepare(deltas, relation: Option<Relation>)` extended signature; default behavior unchanged; existing pyo3 `test_unlabeled_e2e` passes without source edits                | ✓ VERIFIED | Signature at `action_system.rs:177` matches; `relation.map_or(sdk::Relation::None, |r| r.0)` mapping at `:185`; descriptor `"relation": "Option<Relation>"` at `bindings/action_system.json:65`; `test_unlabeled_e2e` PASSED with `git diff 1f445a3a~1 1f445a3a -- pyo3/tests/test_silent_payments.py | grep -c '^-[^-]'` returning 0 (zero deletions). |
| 5  | BRIDGE-05: `SilentPayments.tweakDataFromBlockSpends` static-method binding + descriptor; drift script exits 0                                                                                 | ✓ VERIFIED | Facade at `crates/chia-sdk-bindings/src/silent_payments.rs:450`; descriptor at `bindings/silent_payments.json:203`; `napi/index.d.ts` contains `tweakDataFromBlockSpends`; `pyo3/chia_wallet_sdk.pyi:2346` contains `tweak_data_from_block_spends`; `scripts/sp_descriptor_facade_drift.sh` reports "No drift detected (23 methods on both sides)." |
| 6  | BRIDGE-06: Multi-input round-trip tests in napi/pyo3/wasm + multi-input section in `examples/silent_payment.rs` + `Simulator.blockSpends/blockOutputs` binding facade                          | ✓ VERIFIED | All 3 test files exist and pass: pyo3 2/2 (`test_unlabeled_e2e` + `test_multi_input_e2e`), napi 53/53, wasm 9/9; example runs end-to-end printing all 9 stages including "Stage 9/9 — Spent multi-input detected coin..."; `Simulator.block_spends`/`block_outputs` facade methods at `simulator.rs:109`/`:120` with matching descriptors at `bindings/simulator.json:125`/`:131`. |

**Score:** 6/6 truths verified

### Required Artifacts (Level 1 + Level 2 + Level 3)

| Artifact                                                                  | Expected                                                          | Status      | Details                                                                                                                                          |
| ------------------------------------------------------------------------- | ----------------------------------------------------------------- | ----------- | ------------------------------------------------------------------------------------------------------------------------------------------------ |
| `crates/chia-sdk-driver/src/silent_payments/block_tweak_data.rs`          | Helper + iterative Tarjan SCC + 6 inline tests                    | ✓ VERIFIED  | 527 lines; `pub fn tweak_data_from_block_spends` present; `fn iterative_tarjan_scc` present; all 6 named tests present; wired via `mod.rs:37-38` |
| `crates/chia-sdk-driver/src/silent_payments/mod.rs`                       | `mod block_tweak_data; pub use ::tweak_data_from_block_spends;`  | ✓ VERIFIED  | Lines 37-38 contain both declarations                                                                                                            |
| `src/prelude.rs`                                                          | `tweak_data_from_block_spends` re-export under chip-0057          | ✓ VERIFIED  | Line 45 contains `tweak_data_from_block_spends,` in the chip-0057 driver re-export block                                                          |
| `crates/chia-sdk-test/src/silent_payments/tweak_data.rs`                  | Thin delegate adapter                                             | ✓ VERIFIED  | 65 lines; delegates via `tweak_data_from_block_spends(&spends, &outputs).expect(...)`; old algorithm bits (compute_input_hash, StandardLayer::parse_puzzle, synthetic_pks, scalar_multiply) absent |
| `crates/chia-sdk-bindings/src/action_system.rs`                           | Relation opaque-handle + Spends::prepare(Option<Relation>)        | ✓ VERIFIED  | `pub struct Relation(pub(crate) sdk::Relation)` at line 558; all 5 methods; `pub fn prepare(&self, deltas: Deltas, relation: Option<Relation>)` at line 177; `map_or(sdk::Relation::None, |r| r.0)` mapping at line 185 |
| `bindings/action_system.json`                                             | Relation descriptor + Spends.prepare.relation arg                 | ✓ VERIFIED  | JSON valid (`python3 -m json.tool` clean); `"Relation":` at line 293; `"relation": "Option<Relation>"` at line 65                                |
| `crates/chia-sdk-bindings/src/silent_payments.rs`                         | SilentPayments.tweak_data_from_block_spends static method         | ✓ VERIFIED  | `pub fn tweak_data_from_block_spends` at line 450; delegates to `chia_sdk_driver::silent_payments::tweak_data_from_block_spends`                  |
| `bindings/silent_payments.json`                                           | tweak_data_from_block_spends descriptor entry                     | ✓ VERIFIED  | JSON valid; entry at line 203                                                                                                                    |
| `crates/chia-sdk-bindings/src/simulator.rs`                               | block_spends + block_outputs facade methods                       | ✓ VERIFIED  | `pub fn block_spends` at line 109; `pub fn block_outputs` at line 120                                                                            |
| `bindings/simulator.json`                                                 | block_spends + block_outputs descriptor entries                   | ✓ VERIFIED  | JSON valid; both entries at lines 125 and 131                                                                                                    |
| `crates/chia-sdk-bindings/bindy-macro/src/lib.rs`                         | Trailing-Option<T> auto-default in pyo3 codegen                   | ✓ VERIFIED  | Patch at lines 1199-1225 with explicit trailing-position-only restriction (`trailing_start` reverse-iteration boundary); comment confirms "Only emit `=None` for the *trailing* run of Option args" |
| `napi/__test__/silent_payments_multi_input.spec.ts`                       | BRIDGE-06 napi multi-input round-trip test                        | ✓ VERIFIED  | 134 lines; contains `tweakDataFromBlockSpends` AND `Relation.assertConcurrent`; passes in suite                                                  |
| `wasm/__test__/silent_payments_multi_input.spec.ts`                       | BRIDGE-06 wasm multi-input round-trip test                        | ✓ VERIFIED  | 140 lines; contains `tweakDataFromBlockSpends` AND `Relation.assertConcurrent`; passes in suite                                                  |
| `pyo3/tests/test_silent_payments.py`                                      | `test_multi_input_e2e` appended (existing test untouched)         | ✓ VERIFIED  | `def test_multi_input_e2e` present; commit `1f445a3a` was pure additive (100 insertions, 0 deletions); both tests pass                          |
| `examples/silent_payment.rs`                                              | Multi-input section (Stages 6-9) with Relation::AssertConcurrent  | ✓ VERIFIED  | 201 lines; contains `Stage 6/9`, `Relation::AssertConcurrent`, `tweak_data_from_block_spends`; uses 3 `sim.bls(` calls; runs end-to-end printing all 9 stages |
| `napi/index.d.ts` (generated)                                             | tweakDataFromBlockSpends + blockSpends + blockOutputs + Relation  | ✓ VERIFIED  | All surfaces present; `prepare(deltas: Deltas, relation?: Relation | undefined | null): FinishedSpends` at line 2898                              |
| `pyo3/chia_wallet_sdk.pyi` (generated)                                    | tweak_data_from_block_spends + Relation + prepare signature        | ✓ VERIFIED  | `def prepare(self, deltas: Deltas, relation: Optional[Relation] = None)` at line 54; `class Relation:` at line 123; `def tweak_data_from_block_spends` at line 2346 |

### Key Link Verification (Wiring)

| From                                                                | To                                                                                  | Via                                                                            | Status   | Details                                                                                                                                              |
| ------------------------------------------------------------------- | ----------------------------------------------------------------------------------- | ------------------------------------------------------------------------------ | -------- | ---------------------------------------------------------------------------------------------------------------------------------------------------- |
| `block_tweak_data.rs`                                               | `chia_sdk_types::run_puzzle`                                                        | Stage 2b condition extraction                                                  | ✓ WIRED  | `use chia_sdk_types::{Condition, run_puzzle};` + `run_puzzle(&mut allocator, ...)` call in Stage 2b                                                  |
| `block_tweak_data.rs`                                               | `StandardLayer::parse_puzzle`                                                       | Stage 1 defensive standard-puzzle parse                                        | ✓ WIRED  | `StandardLayer::parse_puzzle(&allocator, parsed)` in Stage 1                                                                                          |
| `block_tweak_data.rs`                                               | `chia_sdk_driver::silent_payments::compute_input_hash`                              | Stage 3 per-group input_hash composition                                       | ✓ WIRED  | `use crate::silent_payments::{...compute_input_hash};` + `compute_input_hash(&coin_ids, &a_sum)` in Stage 3                                          |
| `silent_payments/mod.rs`                                            | `block_tweak_data::tweak_data_from_block_spends`                                    | `mod block_tweak_data; pub use ...`                                            | ✓ WIRED  | Lines 37-38 of mod.rs                                                                                                                                |
| `chia-sdk-test/src/silent_payments/tweak_data.rs`                   | `chia_sdk_driver::silent_payments::tweak_data_from_block_spends`                    | Delegate call from simulator adapter                                           | ✓ WIRED  | `use chia_sdk_driver::silent_payments::{TweakData, tweak_data_from_block_spends};` + delegate call                                                   |
| `chia-sdk-bindings/src/action_system.rs::Relation`                  | `chia_sdk_driver::Relation`                                                         | newtype wrapping sdk::Relation                                                 | ✓ WIRED  | `pub struct Relation(pub(crate) sdk::Relation)` newtype + 5 methods delegate to `self.0`                                                              |
| `bindings/action_system.json`                                       | `chia-sdk-bindings/src/action_system.rs` (Relation)                                  | bindy-macro descriptor → facade dispatch                                       | ✓ WIRED  | `"Relation":` JSON block at line 293 dispatches to facade; napi/pyo3/wasm generated stubs all expose `class Relation`                                |
| `Spends::prepare` binding                                           | `chia_sdk_driver::action_system::Spends::prepare`                                    | `relation.map_or(sdk::Relation::None, |r| r.0)` → forwarded as 3rd arg         | ✓ WIRED  | `let relation = relation.map_or(sdk::Relation::None, |r| r.0);` + `spends.prepare(&mut ctx, &deltas.0, relation)?` at line 186                       |
| `SilentPayments.tweak_data_from_block_spends` binding facade        | `chia_sdk_driver::silent_payments::tweak_data_from_block_spends`                    | Delegate + `.into()` to binding-side TweakData                                 | ✓ WIRED  | Facade body calls `chia_sdk_driver::silent_payments::tweak_data_from_block_spends(&coin_spends, &additions)?` then `Ok(driver_td.into())`            |
| `bindings/silent_payments.json::SilentPayments.methods`             | `chia-sdk-bindings/src/silent_payments.rs::tweak_data_from_block_spends`             | bindy-macro descriptor → facade dispatch                                       | ✓ WIRED  | JSON entry at line 203 dispatches to facade; napi `.d.ts` line surfaces `tweakDataFromBlockSpends`; pyo3 `.pyi` line 2346 surfaces it                |
| `Simulator.block_spends/block_outputs` binding facade               | `chia_sdk_test::Simulator::block_spends/block_outputs`                              | `self.0.lock().unwrap().block_spends(height)`                                  | ✓ WIRED  | Both methods present in `simulator.rs:109-122`; thin wrappers                                                                                         |
| `napi/__test__/silent_payments_multi_input.spec.ts`                 | `SilentPayments.tweakDataFromBlockSpends`                                            | End-to-end binding call path through BRIDGE-05                                 | ✓ WIRED  | Test file contains `tweakDataFromBlockSpends` and exercises full round-trip; passes in suite                                                          |
| `pyo3/tests/test_silent_payments.py::test_multi_input_e2e`         | `spends.prepare(deltas, Relation.assert_concurrent())`                              | Multi-input gate satisfaction via BRIDGE-03 + BRIDGE-04                        | ✓ WIRED  | Test passes — confirms full path: Relation factory → opaque handle → bindy dispatch → unwrap to sdk::Relation::AssertConcurrent → driver gate satisfied |
| `examples/silent_payment.rs`                                        | `Relation::AssertConcurrent`                                                         | Rust-side canonical example showing the new flow                               | ✓ WIRED  | Line 162 calls `spends_multi.finish_with_keys(ctx, &deltas_multi, Relation::AssertConcurrent, &multi_pks)?`; example runs printing Stages 6-9         |

### Data-Flow Trace (Level 4)

| Artifact                                                              | Data Variable                                                  | Source                                                                                  | Produces Real Data                                                                                | Status      |
| --------------------------------------------------------------------- | -------------------------------------------------------------- | --------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------- | ----------- |
| `tweak_data_from_block_spends` helper                                 | `tweak_points: Vec<PublicKey>`                                 | Per-group `A_sum * input_hash` scalar_multiply over Stage 1 parsed standard spends     | Yes — multi-input test asserts `tweakData.tweakPoints.length === 1` for a real bundle             | ✓ FLOWING   |
| `tweak_data_from_block_spends` helper                                 | `outputs: Vec<OutputMeta>`                                     | Map over `additions` slice                                                              | Yes — same multi-input test asserts `tweak_data.outputs` non-empty                                | ✓ FLOWING   |
| `Relation` opaque handle                                              | `self.0: sdk::Relation`                                        | Factory constructors `Self(sdk::Relation::None)` / `Self(sdk::Relation::AssertConcurrent)` | Yes — multi-input pyo3 test exercises `Relation.assert_concurrent()` and unwraps to driver-side variant successfully | ✓ FLOWING   |
| `Spends.prepare(deltas, relation)`                                    | `relation: sdk::Relation`                                      | `relation.map_or(sdk::Relation::None, |r| r.0)` from binding-side `Option<Relation>`     | Yes — multi-input tests in all 3 bindings actually call this path and clear the driver gate at `spends.rs:604` | ✓ FLOWING   |
| `SilentPayments.tweakDataFromBlockSpends` binding                     | `TweakData`                                                    | Delegates to driver-side `tweak_data_from_block_spends` + `.into()`                     | Yes — multi-input round-trip tests construct via this entry point and successfully scan           | ✓ FLOWING   |
| `Simulator.blockSpends/blockOutputs` binding                          | `Vec<CoinSpend>` / `Vec<Coin>`                                 | `self.0.lock().unwrap().block_spends(height)`                                            | Yes — used in multi-input tests as input to `tweakDataFromBlockSpends`; non-empty Vec returned    | ✓ FLOWING   |

### Behavioral Spot-Checks (Step 7b)

| Behavior                                                       | Command                                                                                                            | Result                                                                                                   | Status |
| -------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------ | -------------------------------------------------------------------------------------------------------- | ------ |
| BRIDGE-01 6 inline tests pass                                  | `cargo test --release -p chia-sdk-driver --features chip-0057 silent_payments::block_tweak_data::tests`              | 6 passed; 0 failed (incl. `test_pass_2b_pollution_resistance` correctness oracle)                       | ✓ PASS |
| BRIDGE-02 Phase 6 e2e oracle byte-identical                    | `cargo test --release -p chia-sdk-driver --all-features --test silent_payments_e2e`                                  | 3 passed; 0 failed (unlabeled, labeled, m0_self_change)                                                  | ✓ PASS |
| BRIDGE-02 simulator-helper inline tests pass                   | `cargo test --release -p chia-sdk-test --features peer-simulator,chip-0057 silent_payments::tweak_data::tests`       | 2 passed; 0 failed                                                                                       | ✓ PASS |
| BRIDGE-03/04 hard regression bar — pyo3 single-input E2E       | `pytest tests/test_silent_payments.py::test_unlabeled_e2e -v`                                                       | PASSED (with zero source edits to test_unlabeled_e2e; commit 1f445a3a shows 100 insertions, 0 deletions) | ✓ PASS |
| BRIDGE-06 pyo3 multi-input test                                | `pytest tests/test_silent_payments.py::test_multi_input_e2e -v`                                                     | PASSED (0.05s)                                                                                           | ✓ PASS |
| BRIDGE-06 napi full suite                                      | `cd napi && pnpm build && pnpm test`                                                                                | 53 tests passed (52 existing + 1 new multi-input)                                                       | ✓ PASS |
| BRIDGE-06 wasm full suite                                      | `cd wasm && pnpm test`                                                                                              | 9 tests passed (8 existing + 1 new multi-input)                                                          | ✓ PASS |
| BRIDGE-06 example runs end-to-end                              | `cargo run --release --example silent_payment --all-features`                                                       | Exited 0; printed all 9 stages including "Stage 9/9 — Spent multi-input detected coin..."               | ✓ PASS |
| BRIDGE-05 drift script                                         | `bash scripts/sp_descriptor_facade_drift.sh`                                                                        | "No drift detected (23 methods on both sides)."                                                          | ✓ PASS |
| Workspace clippy on chia-sdk-driver under chip-0057            | `cargo clippy -p chia-sdk-driver --features chip-0057 --all-targets -- -D warnings`                                  | Clean (0 warnings)                                                                                       | ✓ PASS |
| Workspace clippy on chia-sdk-bindings under all features       | `cargo clippy -p chia-sdk-bindings --all-features --all-targets -- -D warnings`                                      | Clean (0 warnings)                                                                                       | ✓ PASS |
| rustfmt check                                                  | `cargo fmt --all -- --files-with-diff --check`                                                                      | Clean                                                                                                    | ✓ PASS |
| cargo machete (no unused workspace deps)                       | `cargo machete`                                                                                                     | "didn't find any unused dependencies in this directory. Good job!"                                       | ✓ PASS |
| JSON validity (3 descriptor files)                             | `python3 -m json.tool bindings/{action_system,silent_payments,simulator}.json > /dev/null`                          | All 3 OK                                                                                                 | ✓ PASS |

### Requirements Coverage

| Requirement | Source Plan(s) | Description                                                                                                          | Status      | Evidence                                                                                                                                                              |
| ----------- | -------------- | -------------------------------------------------------------------------------------------------------------------- | ----------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| BRIDGE-01   | 09-01          | `tweak_data_from_block_spends` helper with Pass 2a + Pass 2b grouping + 6 inline tests (incl. pollution attack)      | ✓ SATISFIED | Helper present in `block_tweak_data.rs` (527 lines, iterative Tarjan, 6 tests all pass); REQUIREMENTS.md row marked `[x]`                                            |
| BRIDGE-02   | 09-02          | Simulator helper collapses to ~5-line delegate; Phase 6 e2e tests pass byte-identically                              | ✓ SATISFIED | `tweak_data.rs` shrunk to 65 lines with delegate call; 3 Phase 6 oracle tests + 2 inline tests all pass; REQUIREMENTS.md row marked `[x]`                              |
| BRIDGE-03   | 09-03          | `Relation` opaque-handle binding + descriptor; all 3 binding targets build                                            | ✓ SATISFIED | Struct + 5 methods at `action_system.rs:558+`; descriptor at `bindings/action_system.json:293`; all 3 generated stubs include `Relation` class; REQUIREMENTS.md `[x]`  |
| BRIDGE-04   | 09-04          | `Spends.prepare(deltas, relation: Option<Relation>)` signature extension; pyo3 `test_unlabeled_e2e` passes unchanged | ✓ SATISFIED | Signature at `action_system.rs:177`; descriptor at `bindings/action_system.json:65`; commit `1f445a3a` is pure additive on test file (0 deletions); test PASSES; REQUIREMENTS.md `[x]` |
| BRIDGE-05   | 09-05          | `SilentPayments.tweakDataFromBlockSpends` static-method binding + descriptor; drift script clean                     | ✓ SATISFIED | Facade at `silent_payments.rs:450`; descriptor at `bindings/silent_payments.json:203`; generated stubs expose the method; drift script "No drift detected"; REQUIREMENTS.md `[x]` |
| BRIDGE-06   | 09-06          | 3 cross-binding multi-input tests + example multi-input section + `Simulator.blockSpends/blockOutputs` facade        | ✓ SATISFIED | All 3 test files exist and pass (pyo3 2/2, napi 53/53, wasm 9/9); example runs end-to-end Stages 1/5–9/9; `block_spends`/`block_outputs` facade + descriptors present; REQUIREMENTS.md `[x]` |

**Coverage: 6/6 BRIDGE-* requirements satisfied. No orphaned requirements (all 6 IDs in REQUIREMENTS.md Phase 9 row are claimed by exactly one plan each).**

### Anti-Patterns Found

No blockers. One informational note:

| File                                                 | Line | Pattern  | Severity | Impact                                                                                                                                                                                                                                                                                                                                  |
| ---------------------------------------------------- | ---- | -------- | -------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `crates/chia-sdk-bindings/src/action_system.rs`     | 497  | `D-01`   | ℹ️ Info  | Pre-existing reference from Phase 5 in the `SendDestination` rustdoc (NOT introduced by Phase 9). Documented in 09-03-SUMMARY: "Pre-existing `D-01` in `SendDestination` rustdoc at action_system.rs:496 is unchanged by this plan — that's Phase 5 vintage, out of scope for CLEANUP-01's `Plan 0[1-9]-` regex." CLEANUP-01's enforced grep ban targets `Plan 0[1-9]-`-style references; freestanding `D-01` is not in scope. |

Phase 9 added zero new planning-artifact references to source files. All other phase-9-modified files (`block_tweak_data.rs`, `silent_payments.rs`, `simulator.rs`, `examples/silent_payment.rs`, the 3 binding test files) are clean.

### Human Verification Required

None. All goal-bearing behaviors have automated coverage:
- Cryptographic correctness: 6 inline Rust tests including the Pass 2b pollution attack oracle
- Byte-equality regression: 3 Phase 6 e2e tests
- Cross-binding parity: 3 multi-input round-trip tests (one per target) all pass
- User-facing example: `cargo run --release --example silent_payment --all-features` exits 0 printing all 9 stages
- Hard backwards-compat bar: existing pyo3 `test_unlabeled_e2e` passes with diff-clean test file

### Gaps Summary

No gaps found. Phase 9 fully achieves its stated goal:

1. **Real-block TweakData bridge:** The pure core of the simulator helper is extracted into `tweak_data_from_block_spends(coin_spends, additions)` in `chia-sdk-driver`, implementing full Pass 2a + Pass 2b SCC grouping with iterative Tarjan (no recursion — adversarial-input safe). The simulator helper is refactored to a 5-line delegate, eliminating drift between simulator-only and real-block code paths. The Pass 2b pollution attack test (the canonical correctness oracle) passes. The helper is exposed across all 3 binding targets as `SilentPayments.tweakDataFromBlockSpends`.

2. **Python Relation binding:** `chia_sdk_driver::Relation` is bound to pyo3 + napi + wasm via the opaque-handle + factories pattern, mirroring the `Id` and `SendDestination` precedents. `Spends.prepare` is extended to accept `Option<Relation>` so multi-input SP sends can pass `Relation::AssertConcurrent` from the binding layer. The bindy-macro pyo3 codegen was patched (restricted to trailing-position Options only — middle Options remain required) so existing 1-arg `prepare(deltas)` callers continue to work without any source edits. The hard regression bar (existing pyo3 `test_unlabeled_e2e` passes with diff-clean test file) holds. Three new multi-input round-trip tests (one per binding target) plus a multi-input section in `examples/silent_payment.rs` prove the full Phase 9 surface works end-to-end.

All Phase 9-specific verification checks listed in the verification prompt are green:
- (1) Helper exists with correct shape, iterative Tarjan, 6 tests pass ✓
- (2) Simulator helper delegates; Phase 6 oracle byte-identical ✓
- (3) Relation opaque-handle binding + descriptor; all 3 binding targets build ✓
- (4) `Spends.prepare(Option<Relation>)`; pyo3 `test_unlabeled_e2e` passes unchanged ✓
- (5) `SilentPayments.tweakDataFromBlockSpends` bound; surfaces in napi `.d.ts` AND pyo3 `.pyi` ✓
- (6) Cross-binding tests + example; all suites pass; example exits 0 with Stages 6-9 printed ✓
- (7) No backwards-compat shims (`prepare_legacy`, `#[deprecated]`) ✓
- (8) No new workspace deps; `cargo machete` clean ✓
- (9) CLEANUP-01 — no new planning-artifact refs in Phase 9 source (one pre-existing Phase 5 `D-01` reference noted as out of scope) ✓
- (10) bindy-macro patch is restricted to trailing-position Options (verified via code inspection at lines 1199-1225) ✓
- (11) REQUIREMENTS.md — all 6 BRIDGE-* marked `[x]` ✓
- (12) ROADMAP.md — Phase 9 plan checkboxes all `[x]` ✓
- (13) Nyquist VALIDATION.md compliance — every task ID has automated check (test runs) or explicit file existence (Wave 0) ✓

---

_Verified: 2026-05-29_
_Verifier: Claude (gsd-verifier)_
