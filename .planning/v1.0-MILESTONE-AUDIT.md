---
milestone: v1.0
audited: 2026-06-03T15:07:45Z
status: passed
supersedes: "2026-06-01 audit (status passed, counted 51 requirements). This 2026-06-03 re-audit re-verifies all 13 phases (live build + tests + integration check) after quick-task 260603-c93 (prose-only reword), corrects the requirement count to 52 (the prior 51 missed ACTION-API-01 — its LETTERS-LETTERS-NN id evades the bold-req regex), and adds the DOC-STALE finding. Earlier: 2026-05-30 audit covered Phases 1-9.1 (49 requirements)."
scope: All 13 phases (1, 2, 3, 4, 4.1, 4.2, 5, 6, 7, 8, 9, 9.1, 9.2)
scores:
  requirements: 52/52
  phases: 13/13
  integration: 4/4   # cross-phase seams intact
  flows: 2/2         # single-input + multi-input E2E complete
gaps:
  requirements: []   # none unsatisfied, none orphaned
  integration: []    # no broken seams
  flows: []          # no broken flows
tech_debt:
  - phase: 01-crypto-primitives-workspace-integration
    items:
      - "Pre-existing clippy::pedantic warnings in chia-sdk-daemon/src/client.rs:426-427 (match_same_arms + match_wildcard_for_single_variants). Predates the entire milestone (commit ec1a3517); CI clippy (no -D warnings) exits 0. Disposition: separate 2-line chore PR or add -D warnings to CI."
  - phase: 04.2-unify-sp-send-into-action-send-via-senddestination-enum
    items:
      - "Pre-existing rustdoc private-intra-doc-link on generate_label (chia-sdk-utils/src/silent_payments/mod.rs:40). Only fires under cargo doc, which CI does not run. Origin: Phase 3 reach-through. Disposition: downgrade the intra-doc link to plain code or #[allow(rustdoc::private_intra_doc_links)]."
  - phase: 05-bindings-rust-facade-json-descriptor
    items:
      - "SendDestination::PuzzleHash arm is runtime-exercised only in napi (action_system.spec.ts). wasm and pyo3 SP E2E exercise only the SilentPayment arm at runtime. NOT a wiring break: all three targets generate the PuzzleHash arm from the identical bindings/ JSON via bindy-macro, and the Rust core covers it exhaustively. Carried forward from prior audits. Document as a deferred follow-up if Sage adopts pyo3/wasm in production."
  - phase: 06-simulator-round-trip-bindings-e2e-example
    items:
      - "RESOLVED in Phase 9.1 (ISSUE-3): SendDestination missing_copy_implementations (no-features build) now silenced via #[cfg_attr(not(feature = chip-0057), allow(missing_copy_implementations))]. No longer outstanding."
      - "DOC-STALE (cosmetic, BIND-03 doc aspect): binding-test doc-comment headers in napi/__test__/silent_payments_e2e.spec.ts:22, pyo3/tests/test_silent_payments.py:7, wasm/__test__/silent_payments.spec.ts:24 cite labeled coverage as living in crates/chia-sdk-driver/src/silent_payments/e2e.rs — a file folded away during Phase 7/8 refactor. No code path depends on the string; the referenced coverage exists and passes (tests/silent_payments_e2e.rs::test_simulator_e2e_labeled + scanner.rs::tv3_scan_detects_labeled_k0). Disposition: one-line doc-comment fix, or leave."
doc_hygiene:  # cosmetic only — reconcile in /gsd:complete-milestone, not requirement gaps
  - "ROADMAP.md top-level phase checklist shows stale [ ] for Phases 3, 4, 4.2, 7 (underlying work verified complete via VERIFICATION.md=passed + requirements [x]); detailed phase sections and gsd-tools (all_phases_complete: true) are the source of truth."
  - "ROADMAP.md Phase 1/2/4.1/5 completion dates typo'd as 2007-05-xx (should be 2026-05-xx)."
  - "STATE.md progress bar reads 100%/88% inconsistently due to a cumulative completed-plans counter (60) exceeding the milestone plan estimate (54) — carried in from earlier phases."
nyquist:
  compliant_phases: 11   # 01,02,03,04,04.1,04.2,05,06,07,08,09
  partial_phases: 0
  missing_phases: 2      # 09.1, 09.2 (gap-closure phases, no VALIDATION.md)
  overall: compliant_with_gaps
---

# v1.0 Milestone Audit — CHIP-0057 Silent Payments

**Status: PASSED.** The milestone achieved its definition of done. All 52 functional requirements are satisfied (3-source cross-reference: REQUIREMENTS.md `[x]` + phase VERIFICATION.md tables + SUMMARY rollups), all 13 phases verified `passed`, every cross-phase seam wires together, and the core-value end-to-end flow completes in the live tree for both single-input and multi-input sends across Rust and all three binding targets.

## Definition of Done

**Core value:** A wallet developer can derive a silent-payment address from a mnemonic, display it, send XCH to a silent-payment address, and (once a CHIP-0058 tweak-data source exists) detect incoming silent payments — without re-implementing any cryptography and through the same idiomatic Layer/Primitive/Spends/bindings surface the SDK already uses.

**Verified achieved:** the integration check ran `examples/silent_payment.rs` end-to-end (9 stages, single + multi-input), the 3 Rust simulator E2E tests, the driver chip-0057 suite (31 tests incl. 5 GUARD tests), and the full CI workspace suite (2438 passed, 0 failed) — all green.

## Requirements Coverage (52/52 satisfied)

3-source cross-reference completed for every REQ-ID. All are `satisfied`: phase VERIFICATION.md = `passed`, REQUIREMENTS.md checkbox `[x]` (52 `[x]`, 0 `[ ]`), and each ID appears in its assigned phase's VERIFICATION.md requirements table (orphan check: zero orphans). Individual plan SUMMARY frontmatter is often empty (`requirements_completed`) — a recording artifact, since requirements were rolled up at the PHASE-SUMMARY level and the final plan of each phase. This does not affect coverage: source 5b (VERIFICATION tables) confirms all IDs.

**Count correction:** prior audits reported 51; the true total is **52**. `ACTION-API-01` (Phase 4.2) was undercounted — its `LETTERS-LETTERS-NN` id shape slips past the `**XXX-NN**` bold-requirement regex used to tally. It is present, `[x]`, and verified in the 04.2 VERIFICATION.md table.

| Group | REQ-IDs | Phase(s) | Status |
|-------|---------|----------|--------|
| Crypto primitives | CRYPTO-01, CRYPTO-02 | 1 | ✓ satisfied |
| Crypto test vectors | CRYPTO-03 | 3 | ✓ satisfied |
| Workspace integration | WS-01, WS-02, WS-03 | 1 | ✓ satisfied |
| Address & keys | ADDR-01..06 | 2 | ✓ satisfied |
| Receive primitive | RECV-01..05 | 3 | ✓ satisfied |
| Send side | SEND-01..08 | 4 | ✓ satisfied |
| On-chain fingerprint | FINGERPRINT-01 | 4.1 | ✓ satisfied |
| Public action surface | ACTION-API-01 | 4.2 | ✓ satisfied |
| Bindings (facade + descriptor) | BIND-01, BIND-02 | 5 | ✓ satisfied |
| Bindings (cross-language E2E) | BIND-03 | 6 | ✓ satisfied |
| Simulator integration | SIM-01, SIM-02, SIM-03 | 6 | ✓ satisfied |
| Example | EX-01 | 6 | ✓ satisfied |
| Code-review cleanup | CLEANUP-01..04, CLEANUP-06 | 7 | ✓ satisfied |
| V1 polish | POLISH-01..04 | 8 | ✓ satisfied |
| Real-block bridge | BRIDGE-01..06 | 9 | ✓ satisfied |
| Fund-safety hardening | GUARD-01, GUARD-02, GUARD-03 | 9.2 | ✓ satisfied |

(Phase 9.1 closed 5 maintainer-flagged conformance issues — ISSUE-1..5 — tracked as fixes rather than milestone REQ-IDs; verified `passed`.)

## Phase Verification (13/13 passed)

| Phase | VERIFICATION.md | Nyquist VALIDATION.md |
|-------|-----------------|------------------------|
| 01 Crypto primitives & workspace | ✓ passed | ✓ compliant |
| 02 Address & key types | ✓ passed | ✓ compliant |
| 03 Receive primitive & TV closure | ✓ passed | ✓ compliant |
| 04 Send-side action | ✓ passed | ✓ compliant |
| 04.1 Sage-style binding refactor | ✓ passed | ✓ compliant |
| 04.2 Unify SP send into Action::send | ✓ passed | ✓ compliant |
| 05 Bindings (facade + descriptor) | ✓ passed | ✓ compliant |
| 06 Simulator round-trip + E2E + example | ✓ passed | ✓ compliant |
| 07 Code-review cleanup | ✓ passed | ✓ compliant |
| 08 Second-pass v1 polish | ✓ passed | ✓ compliant |
| 09 Real-block TweakData bridge | ✓ passed | ✓ compliant |
| 09.1 Fix 5 conformance issues | ✓ passed | ⚠ no VALIDATION.md |
| 09.2 Harden SP sender keys (GUARD) | ✓ passed | ⚠ no VALIDATION.md |

## Cross-Phase Integration (4/4 seams intact)

Verified against the live tree (builds + tests run, not SUMMARY-trusted):

1. **Full happy-path compose — INTACT.** mnemonic → `SilentPaymentAddress` → `Action::send(Id::Xch, SendDestination::SilentPayment, …)` → `Spends::with_silent_payment_keys` (synthetic newtypes) → `sp_finish_branch` (GUARD-01 then one-time-PH derivation) → farm → tweak-data extraction → `scan_from_tweaks` → `DetectedSpCoin` → `derive_synthetic` + `StandardLayer` follow-on spend. Exercised by `examples/silent_payment.rs` and `tests/silent_payments_e2e.rs`; both build and pass.
2. **GUARD-02 newtype signature propagation — INTACT.** Every Rust caller of `with_silent_payment_keys` passes the `SyntheticPublicKey`/`SyntheticSecretKey` newtypes; the **only** raw-key entry point is the FFI facade (`chia-sdk-bindings/src/action_system.rs:93-128`), which synthesizes via `from_raw` (GUARD-03) and is backstopped by the GUARD-01 runtime check (`spends.rs:660-665`), exercised in Rust + napi/pyo3/wasm.
3. **Simulator helper delegates to canonical builder — INTACT.** `tweak_data_from_simulator_block` (chia-sdk-test) is a thin adapter over `tweak_data_from_block_spends` (chia-sdk-driver) — single implementation, no simulator-vs-real-block drift.
4. **Descriptor↔facade sync across three targets — INTACT.** `scripts/sp_descriptor_facade_drift.sh` reports zero drift (23 methods both sides); `Action::send` / `Spends.prepare(deltas, Option<Relation>)` / `with_silent_payment_keys` signatures align across napi/pyo3/wasm.

**Core-value E2E verdict: COMPLETE.** No broken seams, no orphaned exports, no wiring gaps.

## Tech Debt (3 active items — all non-blocking)

1. **chia-sdk-daemon pre-existing clippy::pedantic warnings** (`client.rs:426-427`) — predates the entire milestone (commit `ec1a3517`); CI clippy (no `-D warnings`) exits 0. Out of milestone scope. *Disposition:* small chore PR collapsing the match arms, or add `-D warnings` to CI.
2. **chia-sdk-utils rustdoc private-intra-doc-link** (`silent_payments/mod.rs:40`, `generate_label`) — only fires under `cargo doc`, which CI does not run. *Disposition:* drop the intra-doc brackets or `#[allow(rustdoc::private_intra_doc_links)]`.
3. **SendDestination::PuzzleHash arm runtime-exercised only in napi** — wasm/pyo3 SP E2E exercise only the SilentPayment arm at runtime. Not a wiring break (all three generate the arm from identical JSON via bindy-macro; Rust core covers it exhaustively). Carried forward. *Disposition:* add wasm/pyo3 PuzzleHash-arm runtime coverage if Sage adopts those targets in production.

(Previously tracked — the `SendDestination` Copy lint — was **resolved in Phase 9.1 (ISSUE-3)** via `cfg_attr`.)

None of these block upstream merge: all CI permutations green, zero new workspace deps, zero new `#[allow]` across the SP surface beyond the documented `scanner.rs` allow.

## Nyquist Coverage

| Phase | VALIDATION.md | Compliant | Action |
|-------|---------------|-----------|--------|
| 01–09 (11 phases) | exists | true | — |
| 09.1 | missing | — | `/gsd:validate-phase 09.1` (optional) |
| 09.2 | missing | — | `/gsd:validate-phase 09.2` (optional) |

Phases 09.1 and 09.2 are gap-closure phases (conformance fixes + fund-safety hardening) inserted after Phase 8; both verified `passed` but never received a VALIDATION.md. For 09.1 this was a documented plan-time choice (mechanical pre-merge cleanup with concrete grep/build/clippy/test acceptance criteria). This is a validation-coverage discovery, not a requirements gap — both phases' requirements are fully verified. Running `/gsd:validate-phase 09.1` / `09.2` would close the Nyquist coverage if desired before completion; not a blocker.

## Doc Hygiene (cosmetic — reconcile during /gsd:complete-milestone)

- ROADMAP.md top-level phase checklist shows stale `[ ]` for Phases 3, 4, 4.2, 7 (work verified complete; checkboxes never flipped). Source of truth (VERIFICATION.md + `gsd-tools`) confirms all 13 complete.
- ROADMAP.md Phase 1/2/4.1/5 completion dates typo'd `2007-05-xx` (should be `2026-05-xx`).
- STATE.md progress bar inconsistency (cumulative completed-plans 60 > milestone estimate 54 → 100% read).

These are display artifacts; they do not affect requirement coverage or integration.
